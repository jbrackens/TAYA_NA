{
  "name": "@phoenix-ui/utils",
  "version": "0.23.1-build.1",
  "description": "Utils",
  "main": "dist/index.js",
  "esnext": "dist/index.js",
  "files": [
    "dist/**/*"
  ],
  "scripts": {
    "build": "tsc",
    "dist": "tsc",
    "test": "jest --logHeapUsage --maxWorkers=50% --config ./jest.config.js",
    "test:coverage": "jest --coverage --logHeapUsage --maxWorkers=50% --config ./jest.config.js",
    "bootstrap": "yarn install && lerna bootstrap && yarn build"
  },
  "dependencies": {
    "@fingerprintjs/fingerprintjs": "^3.3.3",
    "watch": "^1.0.2"
  },
  "devDependencies": {
    "typescript": "^5.3.3"
  }
}
