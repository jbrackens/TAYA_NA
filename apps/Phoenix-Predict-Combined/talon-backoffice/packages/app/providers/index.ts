import MenuContext, {
  MenuProvider,
  MenuConsumer,
  detaultMenuItems,
} from "./menu";

const Providers = {
  Menu: {
    MenuContext,
    MenuProvider,
    MenuConsumer,
    detaultMenuItems,
  },
};

export default Providers;
