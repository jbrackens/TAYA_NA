# Retired Office Prototype Notes

The old integration guide described beta-hidden prototype surfaces and static
demo data. It is intentionally retired.

Use `FEATURE_MANIFEST.json` plus the package gate output for current office
route status.
