# Loop 365 - Frontend undici Security Remediation

Generated: 2026-06-29 09:24 Europe/Malta

## Summary

Loop 365 removes the `undici` high-severity frontend dependency advisory cluster
from the active Talon/Tiangge audit baseline while preserving inherited jsdom,
cheerio, and Enzyme-adjacent tooling paths.

The high advisory cluster came through:

```txt
enzyme -> cheerio -> undici
@phoenix-ui/office -> jsdom -> undici
@phoenix-ui/app -> isomorphic-dompurify -> jsdom -> undici
```

The remediation is a root Yarn resolution that moves `undici` to patched
same-major version `7.28.0`.

## Changed Surfaces

- `talon-backoffice/package.json`
  - Adds root resolution `"undici": "7.28.0"`.
- `talon-backoffice/yarn.lock`
  - Resolves inherited `undici@^7.19.0`, `undici@^7.21.0`, and
    `undici@^7.25.0` edges to `7.28.0`.
- `revival/06_DEPENDENCY_VULNERABILITY_BASELINE.md`
  - Regenerated with the `undici` cluster removed from both frontend audit
    scopes.
- `revival/artifacts/talon_yarn_audit_2026-03-02.log`
  - Regenerated by `make security-deps`.
- `revival/artifacts/tiangge_player_yarn_audit_2026-03-02.log`
  - Regenerated by `make security-deps`.

## Verification

```sh
yarn install --ignore-engines
yarn why undici
node jsdom + cheerio + undici MockAgent smoke
yarn workspace @phoenix-ui/api-client build
make security-deps
make qa-preservation-modifications
```

Results:

- `yarn why undici` resolves the cheerio/jsdom/isomorphic-dompurify paths to
  `undici@7.28.0`.
- Direct smoke constructed a `JSDOM` document, parsed a small cheerio document,
  and completed an `undici.fetch` request through `MockAgent`.
- `talon-backoffice/yarn.lock` contains
  `undici@7.28.0, undici@^7.19.0, undici@^7.21.0, undici@^7.25.0`.
- `yarn workspace @phoenix-ui/api-client build` passed.
- `make security-deps` passed.
- Parsed Talon and Tiangge player app audit logs have zero `undici` findings.
- `make qa-preservation-modifications` passed: `392` modified artifacts, `89`
  high-risk contract files, `35` large-change files, tracked line churn
  `+31435 / -6245`, and `0` unclassified modified artifacts.

## Security Impact

Before this loop, after Loop 364, the official dependency baseline showed:

- Talon audit summary: `critical 0, high 48, moderate 90, low 21`.
- Tiangge player app audit summary: `critical 0, high 48, moderate 90, low 21`.

After this loop, the official dependency baseline shows:

- Talon audit summary: `critical 0, high 42, moderate 86, low 17`.
- Tiangge player app audit summary: `critical 0, high 42, moderate 86, low 17`.

Unique advisory ids dropped from `16` to `13`.

## Preservation Notes

This remediation keeps jsdom, cheerio, Enzyme, isomorphic-dompurify, and
workspace scripts in place. It applies a same-major `undici` patch and verifies
the affected DOM parsing plus HTTP client behavior directly.

The refreshed preservation map is
`revival/artifacts/preservation_modification_map_20260629_072545.md`.

## Scenario Impact

- Scenario 12 Safety, compliance, and trust boundary: still Partial, but another
  high-severity frontend dependency cluster is removed from both active launch
  frontend audit scopes.

Remaining before RC: the frontend baseline still reports `high 42`, backend JVM
SCA evidence is still missing, complete preservation review is still required,
and final RC audit remains incomplete.
