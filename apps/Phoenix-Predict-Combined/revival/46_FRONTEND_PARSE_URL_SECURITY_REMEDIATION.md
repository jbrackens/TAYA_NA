# Loop 362 - Frontend parse-url Security Remediation

Generated: 2026-06-29 09:09 Europe/Malta

## Summary

Loop 362 removes the remaining critical frontend dependency advisory cluster
from the active Talon/Tiangge audit baseline while preserving the inherited
Lerna workspace tooling.

The critical advisory came through the old Lerna publish/version path:

```txt
lerna -> @lerna/version -> @lerna/github-client -> git-url-parse -> git-up -> parse-url
```

The remediation is a root Yarn resolution that moves `parse-url` to patched
version `8.1.0` and `parse-path` to patched version `7.1.0`. This keeps
`lerna@3.22.1` and the current workspace scripts in place.

## Changed Surfaces

- `talon-backoffice/package.json`
  - Adds root resolutions for `"parse-url": "8.1.0"` and
    `"parse-path": "7.1.0"`.
- `talon-backoffice/yarn.lock`
  - Resolves `parse-url@^6.0.0` to `8.1.0`.
  - Resolves the parser dependency to `parse-path@7.1.0`.
- `revival/06_DEPENDENCY_VULNERABILITY_BASELINE.md`
  - Regenerated with zero critical advisories for both launch frontend scopes.
- `revival/artifacts/talon_yarn_audit_2026-03-02.log`
  - Regenerated by `make security-deps`.
- `revival/artifacts/tiangge_player_yarn_audit_2026-03-02.log`
  - Regenerated by `make security-deps`.

## Verification

```sh
yarn install --ignore-engines
yarn why parse-url
yarn why parse-path
yarn lerna list --all --json
node -e 'const parseUrl=require("parse-url"); console.log(parseUrl("https://github.com/foo/bar.git"))'
yarn workspace @phoenix-ui/api-client build
make security-deps
make qa-preservation-modifications
```

Results:

- `yarn why parse-url` resolves the inherited Lerna path to `parse-url@8.1.0`.
- `yarn why parse-path` resolves the inherited parser dependency to
  `parse-path@7.1.0`.
- `yarn lerna list --all --json` passed and listed the six inherited workspace
  packages.
- Direct CommonJS `require("parse-url")` smoke test returned the expected
  GitHub URL resource/path shape.
- `yarn workspace @phoenix-ui/api-client build` passed.
- `make security-deps` passed.
- Parsed Talon and Tiangge player app audit logs have zero `parse-url` and zero
  `parse-path` findings.
- `make qa-preservation-modifications` passed: `392` modified artifacts, `89`
  high-risk contract files, `35` large-change files, tracked line churn
  `+31387 / -6203`, and `0` unclassified modified artifacts.

## Security Impact

Before this loop, after Loop 361, the official dependency baseline showed:

- Talon audit summary: `critical 2, high 80, moderate 98, low 21`.
- Tiangge player app audit summary: `critical 2, high 80, moderate 98, low 21`.

After this loop, the official dependency baseline shows:

- Talon audit summary: `critical 0, high 78, moderate 94, low 21`.
- Tiangge player app audit summary: `critical 0, high 78, moderate 94, low 21`.

Unique advisory ids dropped from `25` to `23`.

## Preservation Notes

This remediation does not remove Lerna, alter package scripts, replace the
workspace model, or touch application behavior. It patches a vulnerable
transitive URL parser under the inherited Lerna publish/version path and proves
basic Lerna workspace enumeration still works.

The refreshed preservation map is
`revival/artifacts/preservation_modification_map_20260629_071003.md`.

## Scenario Impact

- Scenario 12 Safety, compliance, and trust boundary: still Partial, but the
  active launch frontend dependency baseline now reports zero critical Yarn
  audit advisories.

Remaining before RC: the frontend baseline still reports `high 78`, backend JVM
SCA evidence is still missing, complete preservation review is still required,
and final RC audit remains incomplete.
