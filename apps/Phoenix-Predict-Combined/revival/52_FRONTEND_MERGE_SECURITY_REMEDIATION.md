# Loop 368 - Frontend merge Security Remediation

Generated: 2026-06-29 09:42 Europe/Malta

## Summary

Loop 368 removes the `merge` high-severity frontend dependency advisory cluster
from the active Talon/Tiangge audit baseline while preserving the inherited
`@phoenix-ui/utils`, `watch`, and `exec-sh` developer tooling path.

The high advisory cluster came through:

```txt
@phoenix-ui/utils -> watch -> exec-sh -> merge
@phoenix-ui/app -> @phoenix-ui/utils -> watch -> exec-sh -> merge
```

The remediation is a root Yarn resolution that moves `merge` to patched version
`2.1.1`.

## Changed Surfaces

- `talon-backoffice/package.json`
  - Adds root resolution `"merge": "2.1.1"`.
- `talon-backoffice/yarn.lock`
  - Resolves inherited `merge@^1.2.0` edges to `2.1.1`.
- `revival/06_DEPENDENCY_VULNERABILITY_BASELINE.md`
  - Regenerated with the `merge` cluster removed from both frontend audit
    scopes.
- `revival/artifacts/talon_yarn_audit_2026-03-02.log`
  - Regenerated by `make security-deps`.
- `revival/artifacts/tiangge_player_yarn_audit_2026-03-02.log`
  - Regenerated by `make security-deps`.

## Verification

```sh
yarn install --ignore-engines
yarn why merge
node direct merge recursive smoke
node exec-sh module-load smoke
yarn workspace @phoenix-ui/api-client build
yarn lerna changed --json
make security-deps
make qa-preservation-modifications
```

Results:

- `yarn why merge` resolves the inherited `watch -> exec-sh` path to
  `merge@2.1.1`.
- Direct `merge.recursive` smoke preserved nested merge behavior.
- `exec-sh` module-load smoke verified both callable and promise exports.
- `yarn workspace @phoenix-ui/api-client build` passed.
- `yarn lerna changed --json` completed successfully and listed the three
  expected changed workspace packages.
- `make security-deps` passed.
- Parsed Talon and Tiangge player app audit logs have zero `merge` findings.
- `make qa-preservation-modifications` passed: `392` modified artifacts, `89`
  high-risk contract files, `35` large-change files, tracked line churn
  `+31444 / -6266`, and `0` unclassified modified artifacts.

## Security Impact

Before this loop, after Loop 367, the official dependency baseline showed:

- Talon audit summary: `critical 0, high 33, moderate 86, low 17`.
- Tiangge player app audit summary:
  `critical 0, high 33, moderate 86, low 17`.

After this loop, the official dependency baseline shows:

- Talon audit summary: `critical 0, high 31, moderate 86, low 17`.
- Tiangge player app audit summary:
  `critical 0, high 31, moderate 86, low 17`.

Unique advisory ids dropped from `11` to `10`.

## Preservation Notes

This remediation keeps `@phoenix-ui/utils`, `watch`, `exec-sh`, and workspace
developer tooling in place. It applies a focused Yarn resolution and verifies
the merge API plus the inherited `exec-sh` loading path instead of removing the
tooling chain.

The refreshed preservation map is
`revival/artifacts/preservation_modification_map_20260629_074330.md`.

## Scenario Impact

- Scenario 12 Safety, compliance, and trust boundary: still Partial, but another
  high-severity frontend dependency cluster is removed from both active launch
  frontend audit scopes.

Remaining before RC: the frontend baseline still reports `high 31`, backend JVM
SCA evidence is still missing, complete preservation review is still required,
and final RC audit remains incomplete.
