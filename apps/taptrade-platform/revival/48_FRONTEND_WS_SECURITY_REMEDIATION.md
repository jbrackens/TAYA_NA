# Loop 364 - Frontend ws Security Remediation

Generated: 2026-06-29 09:19 Europe/Malta

## Summary

Loop 364 removes the `ws` high-severity frontend dependency advisory cluster
from the active Talon/Tiangge audit baseline while preserving the inherited mock
server and Jest/jsdom dependency paths.

The high advisory cluster came through:

```txt
@phoenix-ui/mock-server -> ws
jest -> @jest/core -> jest-config -> jest-environment-jsdom -> jsdom -> ws
```

The remediation is a root Yarn resolution that moves `ws` from vulnerable
`7.5.10` to patched same-major version `7.5.11`.

## Changed Surfaces

- `talon-backoffice/package.json`
  - Adds root resolution `"ws": "7.5.11"`.
- `talon-backoffice/yarn.lock`
  - Resolves inherited `ws@^7.0.0` and `ws@^7.3.1` edges to `7.5.11`.
- `revival/06_DEPENDENCY_VULNERABILITY_BASELINE.md`
  - Regenerated with the `ws` cluster removed from both frontend audit scopes.
- `revival/artifacts/talon_yarn_audit_2026-03-02.log`
  - Regenerated by `make security-deps`.
- `revival/artifacts/tiangge_player_yarn_audit_2026-03-02.log`
  - Regenerated by `make security-deps`.

## Verification

```sh
yarn install --ignore-engines
yarn why ws
node WebSocket echo smoke
node jsdom smoke
yarn workspace @phoenix-ui/api-client build
make security-deps
make qa-preservation-modifications
```

Results:

- `yarn why ws` resolves the mock-server and Jest/jsdom paths to `ws@7.5.11`.
- Direct WebSocket server/client echo smoke passed with `ws@7.5.11`.
- Direct `jsdom` smoke passed and exposed `window.WebSocket`.
- `talon-backoffice/yarn.lock` contains `ws@7.5.11, ws@^7.0.0, ws@^7.3.1`.
- `yarn workspace @phoenix-ui/api-client build` passed.
- `make security-deps` passed.
- Parsed Talon and Tiangge player app audit logs have zero `ws` findings.
- `make qa-preservation-modifications` passed: `392` modified artifacts, `89`
  high-risk contract files, `35` large-change files, tracked line churn
  `+31433 / -6234`, and `0` unclassified modified artifacts.

`yarn workspace @phoenix-ui/mock-server dist` was also attempted, but it failed
on an existing TypeScript library issue in
`@types/express-serve-static-core/index.d.ts` (`TS1337`) before exercising
mock-server behavior. That failure is not counted as passing evidence for this
loop.

## Security Impact

Before this loop, after Loop 363, the official dependency baseline showed:

- Talon audit summary: `critical 0, high 54, moderate 90, low 21`.
- Tiangge player app audit summary: `critical 0, high 54, moderate 90, low 21`.

After this loop, the official dependency baseline shows:

- Talon audit summary: `critical 0, high 48, moderate 90, low 21`.
- Tiangge player app audit summary: `critical 0, high 48, moderate 90, low 21`.

Unique advisory ids dropped from `17` to `16`.

## Preservation Notes

This remediation keeps mock-server, Jest, jsdom, and workspace scripts in place.
It applies a same-major patch-level `ws` resolution and verifies the WebSocket
server/client API plus jsdom integration directly.

The refreshed preservation map is
`revival/artifacts/preservation_modification_map_20260629_072028.md`.

## Scenario Impact

- Scenario 12 Safety, compliance, and trust boundary: still Partial, but another
  high-severity frontend dependency cluster is removed from both active launch
  frontend audit scopes.

Remaining before RC: the frontend baseline still reports `high 48`, backend JVM
SCA evidence is still missing, complete preservation review is still required,
and final RC audit remains incomplete.
