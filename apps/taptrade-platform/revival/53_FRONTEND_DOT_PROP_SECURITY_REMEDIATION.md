# Loop 369 - Frontend dot-prop Security Remediation

Generated: 2026-06-29 09:47 Europe/Malta

## Summary

Loop 369 removes the `dot-prop` high-severity frontend dependency advisory
cluster from the active Talon/Tiangge audit baseline while preserving inherited
commitlint and Lerna conventional-changelog tooling.

The high advisory cluster came through:

```txt
@commitlint/config-conventional -> conventional-changelog-conventionalcommits -> compare-func -> dot-prop
@commitlint/cli -> @commitlint/lint -> @commitlint/parse -> conventional-changelog-angular -> compare-func -> dot-prop
```

The remediation uses targeted Yarn path resolutions that move only the
commitlint vulnerable `dot-prop@3.0.0` callers to patched `4.2.1`, while leaving
already-safe Lerna `dot-prop@5.3.0` callers on `5.3.0`.

## Changed Surfaces

- `talon-backoffice/package.json`
  - Adds targeted root resolutions:
    - `"@commitlint/cli/**/dot-prop": "4.2.1"`
    - `"@commitlint/config-conventional/**/dot-prop": "4.2.1"`
- `talon-backoffice/yarn.lock`
  - Resolves the vulnerable commitlint `dot-prop@^3.0.0` edge to `4.2.1`.
  - Preserves existing safe `dot-prop@5.3.0` Lerna conventional-changelog
    callers.
- `revival/06_DEPENDENCY_VULNERABILITY_BASELINE.md`
  - Regenerated with the `dot-prop` cluster removed from both frontend audit
    scopes.
- `revival/artifacts/talon_yarn_audit_2026-03-02.log`
  - Regenerated by `make security-deps`.
- `revival/artifacts/tiangge_player_yarn_audit_2026-03-02.log`
  - Regenerated by `make security-deps`.

## Verification

```sh
yarn install --ignore-engines
yarn why dot-prop
node direct dot-prop get/set/has smoke
printf 'feat: points dependency remediation\n' | yarn commitlint
yarn lerna changed --json
yarn workspace @phoenix-ui/api-client build
make security-deps
make qa-preservation-modifications
```

Results:

- `yarn why dot-prop` shows the vulnerable `3.0.0` caller now hoisted to
  `dot-prop@4.2.1`, with safe Lerna `dot-prop@5.3.0` callers preserved.
- Direct `dot-prop` smoke verified `set`, `get`, and `has` behavior.
- `yarn commitlint` accepted a conventional commit message.
- `yarn lerna changed --json` completed successfully and listed the three
  expected changed workspace packages.
- `yarn workspace @phoenix-ui/api-client build` passed.
- `make security-deps` passed.
- Parsed Talon and Tiangge player app audit logs have zero `dot-prop` findings.
- `make qa-preservation-modifications` passed: `392` modified artifacts, `89`
  high-risk contract files, `35` large-change files, tracked line churn
  `+31447 / -6274`, and `0` unclassified modified artifacts.

## Security Impact

Before this loop, after Loop 368, the official dependency baseline showed:

- Talon audit summary: `critical 0, high 31, moderate 86, low 17`.
- Tiangge player app audit summary:
  `critical 0, high 31, moderate 86, low 17`.

After this loop, the official dependency baseline shows:

- Talon audit summary: `critical 0, high 29, moderate 86, low 17`.
- Tiangge player app audit summary:
  `critical 0, high 29, moderate 86, low 17`.

Unique advisory ids dropped from `10` to `9`.

## Preservation Notes

This remediation keeps commitlint, conventional-changelog, compare-func, Lerna,
and release tooling in place. The resolution is intentionally path-targeted to
avoid downgrading already-safe Lerna `dot-prop@5.3.0` callers.

The refreshed preservation map is
`revival/artifacts/preservation_modification_map_20260629_074826.md`.

## Scenario Impact

- Scenario 12 Safety, compliance, and trust boundary: still Partial, but another
  high-severity frontend dependency cluster is removed from both active launch
  frontend audit scopes.

Remaining before RC: the frontend baseline still reports `high 29`, backend JVM
SCA evidence is still missing, complete preservation review is still required,
and final RC audit remains incomplete.
