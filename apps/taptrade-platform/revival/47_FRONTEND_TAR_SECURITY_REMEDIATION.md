# Loop 363 - Frontend tar Security Remediation

Generated: 2026-06-29 09:14 Europe/Malta

## Summary

Loop 363 removes the largest remaining high-severity frontend dependency
cluster from the active Talon/Tiangge audit baseline while preserving inherited
Lerna and node-gyp tooling paths.

The high advisory cluster came from `tar@4.4.19` through inherited tooling:

```txt
lerna -> @lerna/add -> @evocateur/pacote -> tar
lerna -> @lerna/publish -> @lerna/pack-directory -> @lerna/get-packed -> tar
lerna -> @lerna/bootstrap -> @lerna/run-lifecycle -> npm-lifecycle -> node-gyp -> tar
```

The remediation is a root Yarn resolution that moves `tar` to patched version
`7.5.11`. The package jump is larger than previous parser patches, so this loop
includes direct tar archive create/extract proof and Lerna scoped-build proof.

## Changed Surfaces

- `talon-backoffice/package.json`
  - Adds root resolution `"tar": "7.5.11"`.
- `talon-backoffice/yarn.lock`
  - Resolves inherited `tar@^4.4.x` edges to `7.5.11`.
- `revival/06_DEPENDENCY_VULNERABILITY_BASELINE.md`
  - Regenerated with the tar cluster removed from both frontend audit scopes.
- `revival/artifacts/talon_yarn_audit_2026-03-02.log`
  - Regenerated by `make security-deps`.
- `revival/artifacts/tiangge_player_yarn_audit_2026-03-02.log`
  - Regenerated by `make security-deps`.

## Verification

```sh
yarn install --ignore-engines
yarn why tar
node tar create/extract smoke
yarn lerna run --scope @phoenix-ui/api-client build
make security-deps
make qa-preservation-modifications
```

Results:

- `yarn why tar` resolves inherited Lerna/pacote/node-gyp paths to
  `tar@7.5.11`.
- Direct tar smoke created a `.tgz`, extracted it, and read back the expected
  file content using `tar.c` and `tar.x`.
- The direct smoke confirmed `tar@7.5.11` still exposes `c`, `create`,
  `extract`, `t`, and `x`.
- `yarn lerna run --scope @phoenix-ui/api-client build` passed, proving Lerna
  can still execute a scoped workspace build after the tar resolution.
- `make security-deps` passed.
- Parsed Talon and Tiangge player app audit logs have zero `tar` findings.
- `make qa-preservation-modifications` passed: `392` modified artifacts, `89`
  high-risk contract files, `35` large-change files, tracked line churn
  `+31428 / -6230`, and `0` unclassified modified artifacts.

## Security Impact

Before this loop, after Loop 362, the official dependency baseline showed:

- Talon audit summary: `critical 0, high 78, moderate 94, low 21`.
- Tiangge player app audit summary: `critical 0, high 78, moderate 94, low 21`.

After this loop, the official dependency baseline shows:

- Talon audit summary: `critical 0, high 54, moderate 90, low 21`.
- Tiangge player app audit summary: `critical 0, high 54, moderate 90, low 21`.

Unique advisory ids dropped from `23` to `17`.

## Preservation Notes

This remediation does not remove Lerna, node-gyp, pacote, package scripts, or
workspace build wiring. It patches the vulnerable tar implementation underneath
those inherited tooling paths and proves both direct tar behavior and Lerna
scoped execution still work.

The refreshed preservation map is
`revival/artifacts/preservation_modification_map_20260629_071457.md`.

## Scenario Impact

- Scenario 12 Safety, compliance, and trust boundary: still Partial, but the
  largest remaining high-severity frontend dependency cluster is removed from
  both active launch frontend audit scopes.

Remaining before RC: the frontend baseline still reports `high 54`, backend JVM
SCA evidence is still missing, complete preservation review is still required,
and final RC audit remains incomplete.
