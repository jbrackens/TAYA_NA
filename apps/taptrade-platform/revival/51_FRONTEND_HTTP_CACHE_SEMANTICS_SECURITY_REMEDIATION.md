# Loop 367 - Frontend http-cache-semantics Security Remediation

Generated: 2026-06-29 09:37 Europe/Malta

## Summary

Loop 367 removes the `http-cache-semantics` high-severity frontend dependency
advisory cluster from the active Talon/Tiangge audit baseline while preserving
inherited Lerna publish tooling and Office `got` cache behavior.

The high advisory cluster came through:

```txt
lerna -> @lerna/publish -> @evocateur/npm-registry-fetch -> make-fetch-happen -> http-cache-semantics
```

The remediation is a root Yarn resolution that moves `http-cache-semantics` to
the patched version already used elsewhere in the workspace: `4.2.0`.

## Changed Surfaces

- `talon-backoffice/package.json`
  - Adds root resolution `"http-cache-semantics": "4.2.0"`.
- `talon-backoffice/yarn.lock`
  - Resolves the inherited Lerna `http-cache-semantics@^3.8.1` edge to
    `4.2.0`, matching the existing Office `got` cache dependency version.
- `revival/06_DEPENDENCY_VULNERABILITY_BASELINE.md`
  - Regenerated with the `http-cache-semantics` cluster removed from both
    frontend audit scopes.
- `revival/artifacts/talon_yarn_audit_2026-03-02.log`
  - Regenerated by `make security-deps`.
- `revival/artifacts/tiangge_player_yarn_audit_2026-03-02.log`
  - Regenerated by `make security-deps`.

## Verification

```sh
yarn install --ignore-engines
yarn why http-cache-semantics
node direct http-cache-semantics smoke
node make-fetch-happen + @evocateur/npm-registry-fetch module-load smoke
yarn lerna changed --json
yarn workspace @phoenix-ui/api-client build
make security-deps
make qa-preservation-modifications
```

Results:

- `yarn why http-cache-semantics` resolves both Office `got` and Lerna
  `make-fetch-happen` paths to `http-cache-semantics@4.2.0`.
- Direct `http-cache-semantics` smoke constructed a cache policy and verified
  storable/satisfies behavior.
- `make-fetch-happen` and `@evocateur/npm-registry-fetch` module-load smoke
  passed.
- `yarn lerna changed --json` completed successfully and listed the three
  expected changed workspace packages.
- `yarn workspace @phoenix-ui/api-client build` passed.
- `make security-deps` passed.
- Parsed Talon and Tiangge player app audit logs have zero
  `http-cache-semantics` findings.
- `make qa-preservation-modifications` passed: `392` modified artifacts, `89`
  high-risk contract files, `35` large-change files, tracked line churn
  `+31439 / -6262`, and `0` unclassified modified artifacts.

## Security Impact

Before this loop, after Loop 366, the official dependency baseline showed:

- Talon audit summary: `critical 0, high 36, moderate 86, low 17`.
- Tiangge player app audit summary:
  `critical 0, high 36, moderate 86, low 17`.

After this loop, the official dependency baseline shows:

- Talon audit summary: `critical 0, high 33, moderate 86, low 17`.
- Tiangge player app audit summary:
  `critical 0, high 33, moderate 86, low 17`.

Unique advisory ids dropped from `12` to `11`.

## Preservation Notes

This remediation keeps Lerna, `@evocateur/npm-registry-fetch`,
`make-fetch-happen`, Office `got`, and release tooling in place. It applies a
focused Yarn resolution and verifies both the cache policy API and inherited
Lerna command path instead of replacing the publishing toolchain.

The refreshed preservation map is
`revival/artifacts/preservation_modification_map_20260629_073824.md`.

## Scenario Impact

- Scenario 12 Safety, compliance, and trust boundary: still Partial, but another
  high-severity frontend dependency cluster is removed from both active launch
  frontend audit scopes.

Remaining before RC: the frontend baseline still reports `high 33`, backend JVM
SCA evidence is still missing, complete preservation review is still required,
and final RC audit remains incomplete.
