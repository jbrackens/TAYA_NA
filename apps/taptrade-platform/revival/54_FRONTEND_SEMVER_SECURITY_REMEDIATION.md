# Loop 370 - Frontend semver Security Remediation

Generated: 2026-06-29 09:52 Europe/Malta

## Summary

Loop 370 removes the `semver` high-severity frontend dependency advisory cluster
from the active Talon/Tiangge audit baseline while preserving inherited
commitlint, mock-server, nodemon, and Lerna tooling.

The high advisory cluster came through:

```txt
@commitlint/cli -> @commitlint/lint -> @commitlint/is-ignored -> semver
@phoenix-ui/mock-server -> nodemon -> simple-update-notifier -> semver
```

The remediation uses targeted Yarn path resolutions:

```txt
**/@commitlint/is-ignored/semver -> semver@6.3.1
**/simple-update-notifier/semver -> semver@7.7.3
```

## Changed Surfaces

- `talon-backoffice/package.json`
  - Adds targeted root resolutions for the two vulnerable semver paths.
- `talon-backoffice/yarn.lock`
  - Resolves the commitlint `semver@6.3.0` edge to `6.3.1`.
  - Resolves the mock-server/nodemon `semver@7.0.0` edge to `7.7.3`.
- `revival/06_DEPENDENCY_VULNERABILITY_BASELINE.md`
  - Regenerated with the `semver` cluster removed from both frontend audit
    scopes.
- `revival/artifacts/talon_yarn_audit_2026-03-02.log`
  - Regenerated by `make security-deps`.
- `revival/artifacts/tiangge_player_yarn_audit_2026-03-02.log`
  - Regenerated by `make security-deps`.

## Verification

```sh
yarn install --ignore-engines
yarn why semver
node direct semver/root + simple-update-notifier semver smoke
printf 'feat: points dependency remediation\n' | yarn commitlint
node nodemon + simple-update-notifier module-load smoke
yarn lerna changed --json
yarn workspace @phoenix-ui/api-client build
make security-deps
make qa-preservation-modifications
```

Results:

- `yarn why semver` shows `@commitlint/is-ignored` now resolves through
  hoisted `semver@6.3.1`, and `simple-update-notifier` resolves to
  `semver@7.7.3`.
- Direct semver smoke verified `valid` and `satisfies` behavior for the root
  `6.3.1` copy and the `simple-update-notifier` `7.7.3` copy.
- `yarn commitlint` accepted a conventional commit message.
- `nodemon` plus `simple-update-notifier` module-load smoke passed.
- `yarn lerna changed --json` completed successfully and listed the three
  expected changed workspace packages.
- `yarn workspace @phoenix-ui/api-client build` passed.
- `make security-deps` passed.
- Parsed Talon and Tiangge player app audit logs have zero `semver` findings.
- `make qa-preservation-modifications` passed: `392` modified artifacts, `89`
  high-risk contract files, `35` large-change files, tracked line churn
  `+31455 / -6285`, and `0` unclassified modified artifacts.

One exploratory smoke tried to require
`@commitlint/is-ignored/node_modules/semver`, but Yarn hoisted that path to the
root `semver@6.3.1`; that nested require failed and is not counted as evidence.

## Security Impact

Before this loop, after Loop 369, the official dependency baseline showed:

- Talon audit summary: `critical 0, high 29, moderate 86, low 17`.
- Tiangge player app audit summary:
  `critical 0, high 29, moderate 86, low 17`.

After this loop, the official dependency baseline shows:

- Talon audit summary: `critical 0, high 27, moderate 86, low 17`.
- Tiangge player app audit summary:
  `critical 0, high 27, moderate 86, low 17`.

Unique advisory ids dropped from `9` to `7`.

## Preservation Notes

This remediation keeps commitlint, mock-server, nodemon, simple-update-notifier,
Lerna, and release tooling in place. The resolutions are intentionally
path-targeted so existing safe semver callers stay on their current compatible
major versions.

The refreshed preservation map is
`revival/artifacts/preservation_modification_map_20260629_075401.md`.

## Scenario Impact

- Scenario 12 Safety, compliance, and trust boundary: still Partial, but another
  high-severity frontend dependency cluster is removed from both active launch
  frontend audit scopes.

Remaining before RC: the frontend baseline still reports `high 27`, backend JVM
SCA evidence is still missing, complete preservation review is still required,
and final RC audit remains incomplete.
