# Loop 361 - Frontend form-data Security Remediation

Generated: 2026-06-29 08:59 Europe/Malta

## Summary

Loop 361 removes the `form-data` high/critical advisory cluster from the active
Talon/Tiangge frontend dependency graph while preserving the inherited Jest,
jsdom, and request test dependency path.

The remediation is intentionally narrow: the Talon workspace root Yarn
`resolutions` now pins `form-data` to patched version `2.5.6`, and
`yarn.lock` resolves the old `form-data@~2.3.2` request edge to that patched v2
release. No application behavior, UI route, gateway route, wallet logic, or
office workflow was rewritten for this change.

## Changed Surfaces

- `talon-backoffice/package.json`
  - Adds root resolution `"form-data": "2.5.6"`.
- `talon-backoffice/yarn.lock`
  - Resolves `form-data@~2.3.2` to `2.5.6`.
  - Pulls the patched package's updated transitive metadata.
- `revival/06_DEPENDENCY_VULNERABILITY_BASELINE.md`
  - Regenerated with the current Talon and Tiangge player app audit summaries.
- `revival/artifacts/talon_yarn_audit_2026-03-02.log`
  - Regenerated by `make security-deps`.
- `revival/artifacts/tiangge_player_yarn_audit_2026-03-02.log`
  - Regenerated by `make security-deps`.

## Verification

```sh
yarn install --ignore-engines
yarn why form-data
make security-deps
yarn workspace @phoenix-ui/api-client build
make qa-preservation-modifications
```

Additional audit-log inspection parsed both regenerated Yarn audit logs and
confirmed:

- Talon audit summary: `critical 2, high 80, moderate 98, low 21`.
- Tiangge player app audit summary: `critical 2, high 80, moderate 98, low 21`.
- Both logs report `25` unique advisory ids using the same advisory-id parser as
  `scripts/security/dependency-baseline.sh`.
- Both logs have `0` `form-data` findings.
- `yarn why form-data` resolves the inherited
  `jest -> @jest/core -> jest-config -> jest-environment-jsdom -> jsdom -> request`
  path to `form-data@2.5.6`.
- `talon-backoffice/yarn.lock` contains
  `form-data@2.5.6, form-data@~2.3.2`.
- `yarn workspace @phoenix-ui/api-client build` passed.
- `make qa-preservation-modifications` passed: `392` modified artifacts, `89`
  high-risk contract files, `35` large-change files, tracked line churn
  `+31374 / -6151`, and `0` unclassified modified artifacts.

## Security Impact

Before this loop, the official regenerated dependency baseline showed:

- Talon audit summary: `critical 7, high 85, moderate 98, low 21`.
- Tiangge player app audit summary: `critical 7, high 85, moderate 98, low 21`.

After this loop, the official regenerated dependency baseline shows:

- Talon audit summary: `critical 2, high 80, moderate 98, low 21`.
- Tiangge player app audit summary: `critical 2, high 80, moderate 98, low 21`.

This removes the `form-data` advisory cluster from the launch frontend audit
evidence but does not clear all dependency risk.

## Preservation Notes

This remediation preserves the inherited test/tooling dependency chain instead
of replacing Jest, jsdom, request, or the office/player build system. The only
behavioral boundary touched is package resolution. The remaining high/critical
advisories should continue to be triaged cluster by cluster so production-tested
application logic is not rewritten merely to chase audit counts.

The refreshed preservation map is
`revival/artifacts/preservation_modification_map_20260629_070334.md`.

## Scenario Impact

- Scenario 12 Safety, compliance, and trust boundary: still Partial, but the
  launch frontend dependency baseline is materially lower and the `form-data`
  high/critical advisory cluster is removed from both Talon and Tiangge player
  app audit evidence.

Remaining before RC: the dependency baseline still reports `critical 2` and
`high 80`, backend JVM SCA evidence is still missing, complete preservation
review is still required, and final RC audit remains incomplete.
