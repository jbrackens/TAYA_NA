# Retired Office Prototype Notes

The previous implementation summary documented prototype-only surfaces that are
now hidden from the beta shell. It has been replaced with this marker so source
gates do not treat obsolete prose as production behavior.

Current route status belongs in `FEATURE_MANIFEST.json`.
