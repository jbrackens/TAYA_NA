include "application.base"

#
#
lagom {

  broker.kafka {
    service-name = ""
    brokers = "localhost:9092"
    brokers = ${?KAFKA_BROKERS}
  }

  # Allow auto creation on tables in development
  # Careful, though, we need to make sure any changes get propagated
  persistence.jdbc.create-tables.auto = true
}