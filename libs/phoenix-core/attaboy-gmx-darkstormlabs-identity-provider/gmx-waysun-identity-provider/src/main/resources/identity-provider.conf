identity-provider {
  crypt {
    libsodium-path = "/opt/libsodium.so"
    libsodium-path = ${?IDENTITY_PROVIDER_LIBSODIUM_PATH}
    secret-box-hex-key = ea87def016584b3c84e5cdbea4dcc868ea87def016584b3c84e5cdbea4dcc868
    secret-box-hex-key = ${?IDENTITY_PROVIDER_SECRET_BOX_KEY}
  }
  allow-access-token-authorization = false
  allow-access-token-authorization = ${?ALLOW_ACCESS_TOKEN_AUTHORIZATION}
  event-ingestor {
    base-url = "http://stella.local:8000"
    base-url = ${?IDENTITY_PROVIDER_EVENT_INGESTOR_BASE_URL}
    admin-event-endpoint-path = "/event_ingestor/admin/any/event"
    admin-event-endpoint-path = ${?IDENTITY_PROVIDER_EVENT_INGESTOR_ADMIN_ENDPOINT_PATH}
    publisher {
      username = "gmx_fcdfd690fec743b688fec2184b735a88"
      username = ${?IDENTITY_PROVIDER_PUBLISHER_USERNAME}
      realm = "master"
      realm = ${?IDENTITY_PROVIDER_PUBLISHER_REALM}
      client-id = "myclient"
      client-id = ${?IDENTITY_PROVIDER_PUBLISHER_CLIENT_ID}
      minimum-remaining-seconds-to-expire = 60
      minimum-remaining-seconds-to-expire = ${?IDENTITY_PROVIDER_PUBLISHER_TOKEN_MINIMUM_REMAINING_SECONDS_TO_EXPIRE}
    }
  }
}
