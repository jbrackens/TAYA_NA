# gmx-nifi
# Nifi flows - replacing PC

To start working simply execute `provision.sh`. It will set bare git repo for registry.

__TODO:__ Enable autocommit hook for exporting flows using `easy-nipy`
