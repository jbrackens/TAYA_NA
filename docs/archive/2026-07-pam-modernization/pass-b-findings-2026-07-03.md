> **ARCHIVED 2026-09-06.** Historical record only — this does not describe the current system.
> Written on branch `pam/p0-modernization` (2026-07-02 → 2026-07-06); never merged. The reference
> implementation (345 commits, migrations 057–061) lives at tag `archive/pam-p0-modernization-2026-07-06`.
> Paths here are PRE-REBRAND (`apps/Phoenix-Predict-Combined/go-platform` = `apps/taptrade-platform/go-platform`;
> `talon-backoffice/packages/app` = `frontend/packages/office`) and units are pre-points ("cents", before
> migration 050). Commit hashes cited inside resolve only at that tag.
> For what of this main still lacks, see `docs/licensability-gaps.md`. See `CLAUDE.md` for current architecture.

# PAM Pass B — Spec Reconciliation Findings (2026-07-03)

Produced by the fresh-context reconciliation workflow (7 section readers over
the full spec + all 20 acceptance scenarios, then one adversarial verifier per
candidate gap, each instructed to refute against code, ledger, and spec text).
Counts: **105 requirements satisfied · 50 accounted by decided/deferred/descoped items · 67 confirmed gaps · 3 candidates refuted**.

Ledger GAP-9..GAP-56 are the deduplicated, merged form of the confirmed gaps
below; this file preserves the full per-finding evidence verbatim.

## Refuted candidates (for the record)

- **ALREADY_BUILT** — No per-staff failed-login counter/lockout — only the per-IP login limiter
  - Evidence: services/auth/internal/http/handlers.go:1044-1046 (IsLocked check in live Login), :1062/:1101/:1111/:1123 (RecordFailure per username), :1799-1880 (lockoutTracker: 5 failures -> 15min lock), redis_rate_limiter.go:44-103 (Redis-backed prod variant), handlers.go:523/:547 (wired route /api/v1/auth/login), :1054-1057 (staff via lookupAdminUser covered)
- **NOT_A_REQUIREMENT** — pam-traceability-matrix.md (and the header-referenced pam-feature-functionality-parity.md) were never committed — BOOT-1's five-doc scope did not include them (verified: ls docs/pam/)
  - Evidence: spec.md:769 (§33 Source Traceability Summary — bibliography pointer, no normative requirement) + spec.md:33-47 (Evidence Base cites six absent docs: pam-vendor-research, idefix-source-audit, omega-screenshot-audit, pam-feature-functionality-parity, pam-traceability-matrix, codex-vs-claude-comparison — all equally dangling) + DECISIONS_NEEDED.md:12-17 (BOOT-1 five-doc scope was a deliberate human-authorized selection, RESOLVED at 54139c24)
- **ALREADY_BUILT** — pam-implementation-plan.md was never reframed for the §37 architecture deltas (single Go gateway, custodial off-chain, Codex 14-service plan collapsed) nor reconciled with the 2026-07-02 DECIDED build queue
  - Evidence: docs/pam/pam-implementation-plan.md:5 ("Revised June 2026 to reconcile with the implemented system ... single Go gateway"), :26-28 (custodial off-chain, on-chain = design seed), :45-48 (Codex 14-service plan collapsed into the gateway), :135-137 (original greenfield phasing "superseded by the roadmap above"); PROGRESS_LEDGER.md:88 (BOOT-1 DONE — this reframed version landed at commit 54139c24)

## Confirmed gaps (verifier-graded priority)

### P0

#### No per-customer risk-profile level (low→high) feeding account status/gating — P0-5 covers alerts/cases, not a persistent customer risk rating
- Spec: §12 KYC, AML, Risk, and Compliance
- Evidence: spec.md:204 requirement bullet; kyc_postgres.go:49 stores a static IDV risk_level but no consumer gates on it — pretrade_gate.go:189 and payments/handlers.go withdrawal gate check st.Status only; PROGRESS_LEDGER.md:90 P0-5 scope is event→alert→case→SAR with no customer rating/gating
- Notes: Gap is real but PARTIALLY seeded: a per-user kyc_status.risk_level (low/medium/high/unknown) already exists, set once from the IDV decision and displayed read-only in the office KYC queue (compliance/kyc/page.tsx:182), Profile-360 (PunterProfile.tsx:463), and CSV export (report_exports_handlers.go:74). What is missing is the requirement's operative clause: (a) nothing accrues/updates the rating from AML or surveillance events (the 'Risk points accrue' Scenario 5 premise), (b) no admin override of the rating, and (c) no gate reads it — trade gate checks status=="approved" only, withdrawal gate checks kycStatusPasses(st.Status) only, and grep shows zero RiskLevel consumers outside the compliance package. Builder should extend the existing field/plumbing rather than create a parallel rating store, and wire it as a natural companion to the P0-5 rules pack (thresholds load as data pending the regime decision). P0 because it is a compliance control that gates accounts.

#### RG limit-type coverage gap: only deposit + stake limits exist — no loss, position, exposure, or session limits
- Spec: §13 Responsible Gaming / Responsible Trading
- Evidence: internal/compliance/types.go:44-86 (only DepositLimit+BetLimit; PlayerRestrictions has no loss/position/exposure/session fields); rg_postgres.go:35-51 (only player_bet_limits + player_deposit_limits tables); spec.md:208-210 (§13 requires deposit, loss, position, exposure, session limits, enforced before order placement); spec.md:484-491 (Scenario 6 requires loss/position limit rejection); PROGRESS_LEDGER.md:183 (P1-3 explicitly read-only)
- Notes: Gap confirmed, with one correction to the reconciler's notes: a POST /api/v1/compliance/rg/session-limit route DOES exist (internal/compliance/handlers.go:636-663) but it is a fake-success stub — it returns 201 with an echo of the request, calls no service method, persists nothing, and nothing enforces session duration anywhere. That aggravates rather than refutes the gap: a player who sets a session limit is told it succeeded and it is never honored. Loss/position/exposure limits have zero code presence (office risk page's MarketExposure is platform concentration reporting, not per-user RG). No ledger item covers adding limit types (P1-3 read-only tab; GAP-1 was fail-closed fallbacks). Spec §13 is normative with a bolded enforce-before-order-placement invariant and Scenario 6 depends on it. P0 per the compliance rubric; the deceptive session-limit stub should be fixed or removed in the same work item.

#### Scenario 6 cannot pass as written: loss/position limit types do not exist and self-exclusion does not block login
- Spec: §32 Acceptance Scenario 6: Responsible Trading Controls
- Evidence: compliance/types.go:44-71 (only DepositLimit + BetLimit; no loss/position types anywhere in compliance/, prediction/, or migrations/) + services/auth/internal/http/handlers.go:1035-1110 (Login checks rate-limit/lockout/password/MFA only — no self-exclusion check; only 'exclusion' match in auth is SQL EXCLUDED in mfa.go:77) vs spec.md:208-211 ('loss, position, exposure' limits; 'Self-excluded traders cannot log in or trade') and Scenario 6 spec.md:478-495 Status: Fail
- Notes: Both halves of the composite are genuinely missing. (1) Loss/position limit types do not exist: types.go defines only DepositLimit and BetLimit (stake); the enforce-before-placement seam is live (handlers.go:593 SetComplianceChecker; rg_postgres.go CheckBetAllowed) but has no loss/position type to enforce, so Scenario 6's precondition (spec.md:484) cannot be set. (2) Self-exclusion blocks trading/deposits only (rg_postgres.go:141-150, 397-414 via gateway-side player_restrictions); the auth service never consults it at login and has no compliance/gateway call path. Not covered by any ledger item: P1-3 (d610ecdf) is read-only admin visibility citing Scenario 6, GAP-1..8 and all DECISIONS_NEEDED entries address other concerns. Requirement is normative invariant text in spec §13, an acceptance scenario (§32 Scenario 6, Status: Fail), and the §36 matrix row (spec.md:801 Partial/Fail). P0 per compliance rubric: it is a responsible-gaming regulatory control, and the spec marks the invariant itself.

#### Audit log lacks hash-chaining — tamper evidence rests solely on the DB trigger
- Spec: §24 Audit Logs and Compliance Evidence
- Evidence: spec.md:316 requires "tamper-evident audit log (hash-chained)" and spec.md:691-693 acceptance scenario requires "hash chain validates"; migrations 009_audit_logs.sql and 036_audit_append_only.sql have no hash/prev_hash column (trigger-only via audit_log_append_only()); no chain computation in internal/http/provider_ops_audit_store.go or internal/prediction/sql_admin_repository.go; no PROGRESS_LEDGER.md or DECISIONS_NEEDED.md item covers it (GAP-5 is the auth audit sink, P2-3 relies on the trigger-based audit as-is).
- Notes: All three refutation avenues fail. Not built: only hash code repo-wide is unrelated (webhook HMAC signer.go, password/tx hashes); audit_append_only_test.go confirms the trigger is the sole tamper defense. Not covered: ledger counts §24 Built on audit *coverage* ("every mutation audited"), never on cryptographic tamper evidence. It is a requirement: §24 text plus an explicit §32 acceptance scenario ("Verify hash chain... hash chain validates; modification impossible/detected") that would fail today; §4 Non-Goals excludes nothing audit-related. Mitigation exists (trigger-enforced append-only, tested), so this is the lowest-urgency P0: residual threat is a DB superuser dropping/re-creating triggers undetected, and the spec's regulator-evidence scenario cannot pass. Fix shape: add prev_hash/row_hash columns (sha256 over canonical row + prev), backfill genesis, verify endpoint or CLI for the §32 scenario, across audit_logs, provider_ops_audit_log, alpha_cashier_audit_events.

#### Admin punter suspension is a dead directory flag — punters.status is never read on the trading path, and the status-change route captures no reason and writes no audit
- Spec: §32 Acceptance Scenarios — Scenario 3: Account Status Change
- Evidence: internal/http/prediction_admin_handlers.go:152-177 (status route: no reason, no audit write); internal/prediction/service.go:803-834 + internal/compliance/* (PlaceOrder gates on market state + RG only; compliance package has zero punters-table references); spec.md:433-438 (§32 Scenario 3 requires rejection + who/when/reason audit); spec.md §36 row "Account lifecycle ... Built ... Suspend/activate/self-exclude wired" is the false positive
- Notes: All three refutation avenues fail. (1) ALREADY_BUILT: punters.status is read only by bonus eligibility (internal/bonus/repository.go:197), a surveillance report (internal/surveillance/fraud.go:61), and admin list/detail — never on order placement, session, or auth; auth service has zero suspension handling; no INSERT INTO audit_logs exists in non-test gateway code and migration 036 only enforces append-only, it does not record punters mutations. Corollary confirmed: status=self_excluded via this route writes no player_restrictions row, so even admin-initiated self-exclusion is unenforced by the RG gate. (2) COVERED_BY_ITEM: no DONE/DECIDED/DEFERRED/DESCOPED item in PROGRESS_LEDGER.md or DECISIONS_NEEDED.md covers suspension enforcement or status-change audit (P1-3 is the read-only RG tab on a distinct route; P0-6 is maker-checker for balance adjustments). (3) NOT_A_REQUIREMENT: spec §32 Scenario 3 is a required acceptance scenario (Status: Fail), not an example or §4 non-goal. P0 because suspension is the enforcement teeth for AML/fraud holds and the control is silently ineffective while the UI reports success; points-only launch softens money impact but not the compliance-control failure.

#### Self-excluded players can still log in — auth service has no restriction/self-exclusion awareness
- Spec: §32 Acceptance Scenarios — Scenario 6: Responsible Trading Controls
- Evidence: spec.md §32 Scenario 6 ("after self-exclusion, login and trading blocked", Status: Fail) vs services/auth/internal/http/handlers.go:1442-1470 (lookupUser queries auth_users with no status/restriction filter) and gateway internal/http/handlers.go:867-897 (registerAuthProxy is a plain reverse proxy, no exclusion hook); ErrUserExcluded only wired at payments/handlers.go:128 and compliance/handlers.go:560,612 (deposit/bet only)
- Notes: All three refutation avenues fail. (1) Not built: SetSelfExclusion (compliance/rg_postgres.go:397) writes only player_restrictions.self_excluded, which nothing in the login/session path reads; the sole status='active' login filter (auth handlers.go:1497) is for admin_users staff, not players. (2) Not covered: ledger P1-3 (PROGRESS_LEDGER.md:183) cites Scenario 6 but is explicitly a read-only back-office viewing tab; DECISIONS_NEEDED.md has no login-blocking item and its DEFER/DESCOPE entries (tenant scoping, on-chain settlement) are unrelated. (3) It is a requirement: spec acceptance scenario with explicit expected result and Fail status. P0 as compliance (RG exclusion breach is reportable in most regimes even without wagering), though harm is mitigated since bets/deposits are already blocked — natural to implement jointly with the Scenario-3 suspension gap via an account-status/restriction check at session issuance and validation.

### P1

#### Spec §6 role personas not seeded — only 3 of 9 roles exist (super-admin, operations-manager, customer-support)
- Spec: §6 User Roles
- Evidence: migrations/027_rbac_admin.sql:83-86 is the sole INSERT INTO roles (3 roles only); spec.md §6 lines ~115-126 lists nine personas; spec §36 Progress Matrix marks the §6/§7 RBAC row "Partial"; no PROGRESS_LEDGER.md or DECISIONS_NEEDED.md item covers persona seeding (GAP-7=bonus routes, P0-6=dual-approval mechanics only)
- Notes: All later permission migrations (038/040/041/050/051/052/054/055) grant only to the same three seeded slugs. RBAC engine supports runtime role creation (internal/rbac/service_test.go:454 creates "risk-analyst" in a test), confirming this is a seed-migration + role-permission matrix data pass. Not P0: enforcement exists on all admin routes; the gap is least-privilege/SoD breadth. Finance Approver depends on P0-6 for the approve action; Auditor needs a read-only grant across all resources.

#### No back-office MFA factor-reset/management surface — a locked-out admin (lost device) has no in-product recovery path; no WebAuthn option
- Spec: §11 Identity, Account Lifecycle, and Authentication
- Evidence: services/auth/internal/http/handlers.go:915-981 (only 2fa status/enroll/activate/disable routes exist); mfa.go:183-200 (MFADisable demands a valid current OTP); handlers.go:1081 (enroll-token bootstrap only when !mfaActive — enrolled admin with lost device gets 401, no path); spec.md:796 (§36 row 1: "Still Partial: ... no back-office MFA UI, no WebAuthn"); no ledger/decision item in docs/pam/PROGRESS_LEDGER.md or DECISIONS_NEEDED.md mentions factor reset/recovery/WebAuthn
- Notes: All three refutations fail. (1) Not built: no admin MFA-reset endpoint anywhere — gateway RBAC handlers and office UI have zero MFA references; the RBAC "reset password" does not clear auth_mfa; only auth-service mfa.go/handlers.go touch that table; no WebAuthn/passkey code in go-platform. Recovery for a device-lost admin is manual DB surgery, and since P0-1 made admin MFA unconditional and fatal-to-disable in prod/staging (mfa.go:269-278), even another super-admin cannot recover a locked-out colleague in-product. (2) Not covered: ledger MFA items are P0-1 (enrollment/enforcement), GAP-2 (replay), GAP-3 (secrets at rest), GAP-5 (audit) — none touch reset/recovery or WebAuthn. (3) It is a requirement: §11 (spec.md:188-190) requires admin MFA per vendor auth/MFA parity, and the spec's own §36 matrix names "no back-office MFA UI, no WebAuthn" as the remaining Partial; §4 non-goals exclude nothing here. Caveat: "factor reset/recovery" is an inference from "back-office MFA UI" (spec never literally says "reset"), which is why this is P1 ops, not P0; WebAuthn alone would be P2.

#### No concurrent-session 'steal lock' semantics and no admin session-revocation control
- Spec: §11 Identity, Account Lifecycle, and Authentication
- Evidence: spec.md:190-191 requires "connection locks ('steal lock', Idefix)"; auth Login() at services/auth/internal/http/handlers.go:1140 does store.Put(s) with no displacement of prior sessions; DELETE /api/v1/auth/sessions/{id} at handlers.go:786-820 is self-scoped only (SEC-05 comment), no admin path; zero matching items in docs/pam/PROGRESS_LEDGER.md and DECISIONS_NEEDED.md
- Notes: Partial machinery exists but not the claimed capability: Redis-backed sessions with self-service list/revoke (GET/DELETE /api/v1/auth/sessions, scoped to own sessions per audit SEC-05), logout revocation, and revoke-all-on-password-change (handlers.go:1205, sole DeleteByUserID call site). Missing: (a) steal-lock — new login never displaces existing sessions, unlimited concurrent sessions allowed; (b) admin kick-session — no gateway admin endpoint and no office UI (zero hits in talon-backoffice App Router) lets staff terminate another user's live session, so blocking/compromise response cannot cut an active session. Ledger auth items (P0-1 MFA, GAP-2, GAP-3, GAP-5, GAP-6) do not cover session concurrency or admin revocation. P1 not P0 because lockout, self-revocation, and password-change revoke-all partially mitigate; the steal-lock half alone would be P2 parity, but the missing admin revocation is an ops/security control on live accounts.

#### No volume/registration-driven automated KYC re-trigger rules — only static trading/withdrawal gates exist
- Spec: §12 KYC, AML, Risk, and Compliance
- Evidence: spec.md:195-196 requires "automated triggers (registration, first deposit, threshold, trading volume)"; only static gates exist: pretrade_gate.go:100-105,176-193 (KYC_REQUIRED_FOR_TRADING all-or-nothing block) and payments/handlers.go:43-78 (KYC_WITHDRAWAL_THRESHOLD_CENTS cumulative gate); KYC initiation is player-only (compliance/handlers.go:99-135); no ledger item (PROGRESS_LEDGER.md Done/DECISION ROUND/GAP lists) names KYC trigger rules — P0-5 (ledger line 90) covers aml_alerts/aml_cases only
- Notes: Not built, not covered by any ledger item, and a genuine §12 requirement (§4 Non-Goals only excludes native sanctions/PEP). P1 rather than P0 because the existing static gates fail closed — there is no fail-open compliance hole today; the missing capability is the automated trigger/re-trigger rules engine (registration-initiated KYC and trading-volume re-KYC are wholly absent; the withdrawal-threshold gate partially serves the 'threshold' event as a block rather than a case-opening trigger). Escalates toward P0 once money rails open (native USDT rail in build). Natural build shape: rules-as-data sibling of P0-5 — KYC trigger rules table evaluated on registration events and wallet/trade-volume aggregates, opening KYC cases in the existing P0-3 review queue. Suggest recording as a new GAP-n item citing spec §12 lines 195-196.

#### KYC review UI lacks a request-more-documents action/state (approve/decline only)
- Spec: §12 KYC, AML, Risk, and Compliance
- Evidence: spec.md §12 line 197 ("decline/request-documents" is an explicit requirement) vs kyc_admin_handlers.go:20 AdminDecision(approve bool) and kyc_postgres.go:48 status CHECK ('unverified','pending','approved','declined','blocked') — no request-documents action or state anywhere; PROGRESS_LEDGER.md P0-3 (line 188-189) delivered approve/decline only and no GAP/DECIDED/DEFERRED item covers it
- Notes: All three refutation avenues fail. Not built: office kyc/page.tsx:308-318 has only Approve/Decline buttons; backend decision route is binary. Not covered: DECISION ROUND (P0-4..7, P1-1, P2-1/3/4, GAP-3/6/8) and GAP-1..8 never mention request-documents. Is a requirement: §12 bullet, not excluded by §4 non-goals. Priority P1 rather than P0 because a workaround exists — decline-with-reason plus the player resubmit endpoint (compliance/handlers.go:160 does not block declined users), so no fail-open/security exposure; it is a workflow-completeness extension of P0-3 (one new decision variant, one new status value, UI button).

#### No production KYC IDV vendor selected or wired behind the existing idv.go seam (manual review is the only real path)
- Spec: §12 KYC, AML, Risk, and Compliance
- Evidence: gateway/internal/compliance/idv.go:69-71 (vendorIDVProvider.Review unconditionally errors "not configured ... implement its client" — no vendor client exists in gateway/auth/office); spec.md:197-198 (§12 requires "vendor integration (Sumsub/Trulioo/GBG)"); spec.md:799 (§36 KYC row: "no vendor wired"); PROGRESS_LEDGER.md:86-100 DECISION ROUND — no row names an IDV vendor, and P0-4 (ledger line 195) chose OpenSanctions option (b), explicitly not option (c) which would have bundled IDV via Sumsub/Onfido
- Notes: Gap is confirmed but bounded: KYC enforcement is DB-backed and fail-closed (P0-2), a manual review back office exists (P0-3), and a half-configured vendor fails loud rather than auto-approving, so there is no open compliance hole — the missing piece is automation/scale per §12's automated triggers. Suggest P1 not P0. Note this item is decision-gated like P0-4 was: it needs the human to name the vendor (commercial commitment) before any autonomous build; the code seam (KYC_IDV_PROVIDER + KYC_IDV_API_KEY + IDVProvider interface) is ready for the client drop-in.

#### Self-exclusion does not block login — the auth service has no awareness of RG state or punter status; only trading is blocked
- Spec: §13 Responsible Gaming / Responsible Trading
- Evidence: spec.md:211 ("Self-excluded traders cannot log in or trade") vs services/auth/internal/http/handlers.go:1035-1130 (Login: rate-limit/lockout/password/MFA only) and :1442-1455 (lookupUser selects no status/exclusion state from auth_users)
- Notes: All three refutation avenues fail. (1) Not built: the auth service's only exclusion grep hit is the SQL EXCLUDED keyword (mfa.go:77); all real self_excluded consumers are trade/admin-side (compliance/rg_postgres.go order/deposit gates; prediction_admin_handlers.go:170 UpdatePunterStatus; segmentation). SetSelfExclusion (rg_postgres.go:397-420) writes only player_restrictions — no auth_users propagation, no session revocation; gateway httpx.Auth validates sessions against the exclusion-blind auth service. (2) Not covered: P1-3 is a read-only admin tab (ledger line 183); GAP-1 is the trade-half fail-closed RG service (lines 236/252); DECISION ROUND items and DECISIONS_NEEDED.md contain nothing on login-time exclusion. (3) Is a requirement: spec §13 line 211 is normative, reinforced by scenario at spec.md:489-491 ("after self-exclusion, login and trading blocked"); §4 non-goals exclude only the player-facing trading client. P1 because trading/deposits are blocked per-request fail-closed and launch is points-only; escalates to P0 with real money. Fix should cover BOTH the login gate and revocation of existing sessions on exclusion, fail-closed on RG lookup errors.

#### Tag-gated market eligibility (and its Profile-360 per-market-eligibility view) has no implementation or queued item
- Spec: §15 Prediction Market Account Functions
- Evidence: spec.md:229-232 (§15 invariant "restricted markets gated by jurisdiction/tags") + spec.md:92-93 (PD-5: tags as gating primitive "Not yet implemented"); code: internal/prediction/jurisdiction.go:30-33 (policy = Mode+Countries only, no tags), PunterProfile.tsx:6 (tabs overview|trades|wallet|kyc|limits, no eligibility view); ledger GAP items end at GAP-8, P2-2 is CRM-targeting only
- Notes: All three refutation avenues fail. (1) Not built: per-market gating is jurisdiction-only (MarketJurisdictionPolicy has no tags field); segmentation package has zero consumers in the prediction/order path; the market Tag filter is a browse filter over series tags; Profile-360 has no per-market eligibility tab and no office App-Router page shows it. (2) Not covered: no Done/DECIDED/DEFERRED/DESCOPED ledger item or GAP-1..8 addresses tag-gated market access or the eligibility view; P2-2 crm tags are targeting-only as the reconciler said. (3) It is a requirement: §15 bolded invariant plus PD-5 explicitly marks the tag gating primitive "Not yet implemented"; §32 Scenario 16 (restricted-market invariant) is Status: Fail; §4 non-goals do not exclude it. Priority P1 not P0 because the regulatory floor (jurisdiction gating) is already enforced fail-closed (migration 035 + GeoGate); the missing tag half is a market-integrity/ops control (e.g., insider restriction) plus admin visibility, which is money/ops-grade, not day-one compliance.

#### No admin order view/cancel surface — the only cancel route is player-scoped and there is no audited admin cancel
- Spec: §16 Orders, Trades, Positions, Exposure, and P/L
- Evidence: spec.md:238-239 ("Admin can view and cancel orders (audited)") vs prediction_handlers.go:789-822 (only cancel route, player-scoped) + prediction/service.go:1199-1201 (ownership check, no admin bypass) + prediction_admin_handlers.go:88-95 (admin routes = punters/audit-logs only, no orders)
- Notes: All three refutation avenues fail. (1) Not built: repo-wide grep finds no /admin/orders route in Go/TS/SQL; office UI has no order-cancel action; admin_risk_handlers.go shows only aggregate resting-order counts; P1-4 surveillance is explicitly read-only over the trade/order log; userIDFromRequest has no impersonation seam. Closest mitigation is the market void/close sweep (finalizeRestingExchangeOrder), a whole-market blunt instrument, not per-order admin view/cancel. (2) Not covered: GAP-1..8 are auth/compliance; DECISION ROUND table (BOOT-1, P0-4..7, P1-1, P2-1/3/4, GAP-3/6/8) has nothing on admin order tooling; DECISIONS_NEEDED.md has zero order/cancel mentions. (3) It is a requirement: normative bullet in spec §16, not an example or §4 non-goal. Mild counterpoint: §36 matrix marks "Orders / trades (CLOB) | 16 | Built", but that row describes the CLOB core, not the admin surface. Priority P1 (money/ops incident response; cancel releases margin) — mitigations exist (market void, player suspension) so not P0. Build note: "audited" is part of the requirement, and cancel touches the order/wallet reservation path, so it likely falls under the protected-core human-review convention used for P0-6/P0-7.

#### No insider/abnormal-pattern surveillance detector — P1-4 ships wash/spoof/collusion only
- Spec: §18 Market Integrity, Fraud, and Abuse Monitoring
- Evidence: gateway/internal/surveillance/detectors.go:269-274 (engine default set = wash_self_trade, spoofing, collusion, duplicate_account only) vs spec.md §18 lines 259-260 ("detect wash trading, spoofing, collusion, insider/abnormal patterns"); ledger P1-4 (PROGRESS_LEDGER.md:263) enumerates only wash+spoofing+collusion slices, P1-5 (line 261) only duplicate-account, GAP-6 (lines 248, 258) only device/IP fingerprinting
- Notes: All three refutation avenues fail. (1) Not built: repo-wide grep for insider/anomal/abnormal across gateway, auth, and office UI finds no detection code; the only Detector implementations in the codebase are the four in internal/surveillance. (2) Not covered: no DONE/DECIDED/DEFERRED/DESCOPED ledger or DECISIONS_NEEDED item names insider or generic anomaly detection; GAP-6 is the fraud-leg signal-capture gap, and P0-5 is §12 AML, not §18 market abuse. (3) It is a requirement: insider/abnormal patterns sits in the same bolded §18 requirement list as the three shipped detectors. P1 because it is the fourth leg of the §18 requirement whose siblings were P1, is an incremental detector on the existing alert→case pipeline (new Detector impl + registration in NewEngine's default set), and insider/resolution-front-running is a core prediction-market integrity vector. Natural design question for the builder: what signal defines "abnormal" given available data (e.g., position accumulation shortly before resolution, win-rate outliers, volume/price deviation) — resolver-identity and trade/order logs already exist to support a resolution-timing detector.

#### No bonus-abuse detection rules — duplicate-account detection is the only mitigant
- Spec: §18 Market Integrity, Fraud, and Abuse Monitoring
- Evidence: spec.md:262-264 requires "bonus abuse" detection under §18 Fraud & abuse; detectors.go:269-274 default engine set is wash/spoofing/collusion/duplicate-account only — no bonus-abuse detector; grep "bonus" in internal/surveillance/ = 0 hits; ledger items P1-4/P1-5/P1-6/GAP-6/GAP-7 cover market integrity, dup detection, bonus admin, signal capture, and RBAC respectively — none cover bonus-abuse rules.
- Notes: All three refutation avenues fail. (1) Not built: surveillance engine has exactly four detectors, none bonus-related; bonus/wallet packages have no detection logic (MaxBonusCents caps and wagering are preventive bounds, not monitoring). (2) Not covered: P1-5 duplicate-account detection is explicitly the only adjacent mitigant; GAP-6 (BLOCKED P2) is device/IP signal capture, an enabler not a bonus-abuse rule set; GAP-7/P1-6 are administration not monitoring; DECISION ROUND covers P2-1/P2-3/P2-4 only. (3) Required: "bonus abuse" is an enumerated §18 requirement with sources; §4 non-goals exclude bonus-wagering multipliers as mechanics, not abuse monitoring. P1 because bonus grants are real money and P2-2 campaigns can auto-dispatch grants to segments; fits the existing surveillance Detector interface + alert/case pipeline (e.g., grant-velocity per linked cluster, turnover-gaming near wagering thresholds).

#### Statutory/regulatory report suite unbuilt — regime mapping pending counsel
- Spec: §23 Reporting, Dashboards, and Exports
- Evidence: spec.md:309-311 requires the statutory suite (only the regime mapping is the open question); spec.md §36 matrix row "Reporting & export | 23 | 15 | Partial | Fail | ...no full reporting/export module"; code confirms: gateway internal/http/report_exports_handlers.go:14-27 ships only two operational CSVs (kyc-statuses, surveillance-alerts) and internal/http/reports_handlers.go:11-20 only four dashboard aggregates (one an honest-zeros placeholder); office app/(dashboard)/reports/page.tsx is a redirect stub; PROGRESS_LEDGER.md:262 records P1-6 as "Scenario 15-partial"; no DECISION ROUND/GAP/DEFERRED/DESCOPED item in PROGRESS_LEDGER.md or DECISIONS_NEEDED.md covers statutory reports (P0-5's scope is AML alerts/cases/SAR only).
- Notes: All three refutation avenues fail. Not built: zero matches for statutory/transaction-summary/daily-balance/tax capability across gateway internal packages, the office App-Router tree, and the dead Pages-Router tree; what exists is explicitly operational ("read-only CSV downloads of core operational datasets" per the handler's own comment). Not covered: P1-6 self-declared Scenario 15 partial; stretching P0-5 (AML) would misstate its scope even though both await the same counsel regime sheet (DECISIONS_NEEDED.md P0-5 shows the regime sheet ⏳). Not excluded: section 4 Non-Goals says nothing about reporting, and spec line 59 independently names statutory reporting as in-scope. Priority P1 rather than P0: regulator-facing licensure deliverable, not a day-one enforcement control on a points-only launch with the regime unnamed; correct disposition is the reconciler's — queue as a regime-gated item alongside the P0-5 rules pack so both consume the counsel regime sheet when it arrives.

#### No back-office staff MFA management UI (view status / admin reset of a factor)
- Spec: §25 Admin Operations and Configuration
- Evidence: spec.md:324 (§25 "Core Users management (create/edit staff, status, lock, failed-login, MFA)"); spec.md:796 (§36 row 1: "no back-office MFA UI"); auth handlers.go:909-995 (only self-service /2fa/status|enroll|activate|disable, all bound to cs.UserID or the caller's enrollment token — no admin-target endpoint); zero mfa/totp hits in office app/(dashboard)/users and app/(dashboard)/access-control; PROGRESS_LEDGER.md:180 (P0-1 DONE scope = enforcement + self-service only, no queue/GAP/DEFERRED/DESCOPED item covers admin MFA management)
- Notes: mfa_admin_test.go tests admin *enforcement* (mandatory MFA for admin role), not admin-initiated factor management — do not mistake it for the capability. Priority P1 not P0: the fail-closed security enforcement is delivered; the gap is operational — with MFA mandatory in prod/staging, a staffer who loses their factor can only be recovered via unaudited direct DB deletion of the auth_mfa row. A reset endpoint should be RBAC-gated, audited, and ideally maker-checker (P0-6 class), and reset should funnel the staffer back through the existing forced-enrollment token flow.

#### No payment-methods registry/config screen (same gap as §22 line 294)
- Spec: §25 Admin Operations and Configuration
- Evidence: spec.md:294 (§22 payment-methods registry w/ auto-manual-enabled flags, brand/jurisdiction scope) + spec.md:326 (§25 Payment Methods config screen); nearest ledger item P1-1 scope excludes it (PROGRESS_LEDGER.md:93,216; DECISIONS_NEEDED.md:49-55); only code is a hardcoded 2-item punter-facing list at gateway internal/payments/db_service.go:351-374
- Notes: All three refutation avenues fail. (1) ALREADY_BUILT: no. The only payment-methods code is punter-facing GET /api/v1/payments/methods (internal/payments/handlers.go:251), whose DB implementation returns a HARDCODED two-entry list with the comment "in production, these would come from a payment vault" (db_service.go:351-374); no payment_methods table exists in gateway migrations (grep empty); no admin CRUD routes; the office App-Router tree apps/.../talon-backoffice/packages/office/app/(dashboard)/ has screens for tenants, access-control, compliance, etc. but nothing for payments/payment methods (only stray "payment" hits are proxy.ts, types/punters.ts, a legacy-routes test). internal/cashier, internal/alphacashier, and internal/platformconfig contain no method registry either. (2) COVERED_BY_ITEM: the closest item, P1-1 "Cashier back office" (DECIDED -> buildable), enumerates its scope three separate times as "withdrawals queue + AWA rules engine + deposits view" (PROGRESS_LEDGER.md:93 and :216, DECISIONS_NEEDED.md:49-55) and never includes a payment-methods registry or config screen; P2-1 peripherals (DONE) cover feature-flag store + tenant/brand + jurisdiction admin, not payment methods; no GAP-1..8 item touches it. If P1-1 is built exactly as decided, the registry still won't exist. (3) NOT_A_REQUIREMENT: no — spec.md:293-295 is a first-class §22 bullet requirement and spec.md:326 lists "Payment Methods" among required §25 admin config surfaces; neither is an example or a §4 non-goal. Priority P1 (money/ops): it is the config surface for the cashier the loop is now green-lit to build; recommend folding it into the P1-1 build behind the same CASHIER flag (default OFF, launch-inert per Two-Spec rule), with a DB-backed registry replacing the hardcoded GetPaymentMethods list. Agree with the reconciler: count once, combined with the §22 line-294 twin.

#### No secrets-management solution — env-var based; GAP-3's KMS seam is MFA-key-only
- Spec: §27 Security Requirements
- Evidence: spec.md:344 (§27 lists "secrets management" as a requirement); live code reads secrets via os.Getenv (gateway/internal/http/pretrade_gate.go:77, cmd/gateway/main.go:376); no Vault/KMS/SecretsManager integration in go-platform; only ESO/AWS-SM config is phoenix-backend/deploy/helm/.../external-secret.yaml in the tree spec.md:48 declares "dead legacy"; PROGRESS_LEDGER.md:97 shows GAP-3 scoped to AUTH_MFA_ENC_KEY (MFA-key-only KMS seam); no ledger item mentions general secrets management
- Notes: All three refutation avenues fail. (1) ALREADY_BUILT: live gateway/auth use env vars exclusively; the one genuine secrets-manager config (External Secrets Operator + AWS Secrets Manager helm template) lives in the dead Scala phoenix-backend sportsbook tree — usable as prior art but not wired to the live Go services, which have only Dockerfiles. (2) COVERED_BY_ITEM: GAP-3 covers only TOTP-secret encryption via an env-provided AUTH_MFA_ENC_KEY with a KMS-ready key-source seam (matching the reconciler's "MFA-key-only" characterization); GAP-4 is env fail-closed presence validation, not secret storage/rotation; P2-1 (deferred) and P2-4 (descoped) are unrelated. (3) NOT_A_REQUIREMENT: §27 lists it flatly as a security requirement; §4 non-goals exclude nothing infra-related. Priority reasoning: security-class per the rubric, but P1 rather than P0 — pre-production infra hardening with fail-closed env validation (GAP-4) already mitigating missing-secret failure modes, and the ledger itself ranked the adjacent GAP-3 security item P1 (PROGRESS_LEDGER.md:254). Natural build path: extend GAP-3's key-source seam into a general secret-provider seam and reuse the dead-tree ESO pattern at deploy time.

#### ISO 27001 posture is organizational (ISMS, policies, vendor mgmt) — outside the code loop's remit, unowned
- Spec: §27 Security Requirements
- Evidence: docs/pam/spec.md:344-345 ("ISO 27001-grade posture (vendor baseline)" in §27); no match for "27001"/"ISMS" anywhere in docs/pam/PROGRESS_LEDGER.md or docs/pam/DECISIONS_NEEDED.md; spec.md §36 Progress Matrix maps §27 only to the "Admin auth & RBAC" row (spec.md:796), so the clause has no tracking row
- Notes: All three refutation avenues fail. (1) Not built: the technical §27 controls are built/tracked (P0-1 MFA, RBAC mig 027/038/040, GAP-3 encryption-at-rest, GAP-5 durable audit, GAP-7), but an ISMS/policies/vendor-management posture has no code artifact and cannot have one — grep for ISO 27001/ISMS in gateway, auth, and office code returns nothing. (2) Not covered: no DONE, DECIDED, DEFERRED, or DESCOPED item in the ledger or DECISIONS_NEEDED touches organizational security posture; the only §27 citations are the MFA and RBAC slices. (3) It is a requirement: a standalone semicolon-delimited item in §27 Security Requirements, not in §4 Non-Goals. Priority P1 not P0: the launch-blocking security-code controls of §27 are already done or queued; the residue is founder/ops-track organizational work whose urgency is gated on the open licensing-regime question (LEG-1). Correct disposition is a tracked human-track item (founder/ops), not an autonomous-loop build slot — the reconciler's routing note is accurate.

#### Scenario-evidence debt: no §36 acceptance scenario has been formally re-run and evidenced post-loop
- Spec: §36 Progress Matrix (scenarios touching §22-§28)
- Evidence: docs/pam/spec.md line 382 ("All scenarios begin at status `Fail`; future implementation agents update them") + §36 legend lines 789-792/822-823; the committed §36 matrix still has 22 `| Fail |` scenario cells with zero flipped; PROGRESS_LEDGER.md lines 101-104 build queue contains no evidence-pass item and line 176 defines pass B as a doc-reconciliation walk, not a running-system exercise; grep for "scenario" across talon-backoffice/e2e/ returns no hits (no Playwright spec is mapped to any §32 scenario or its Required Evidence).
- Notes: All three refutation avenues fail. (1) Not already built: capability e2e specs exist (audit-logs, tenants, reports, kyc-review, balance-adjustment, surveillance, auth) but none is mapped to a §32 scenario or produces its Required Evidence (e.g., Scenario 1's audit-log entries + permission-check trace; Scenario 17's tamper-evidence). (2) Not covered by an item: Done items cite "§32 Scenario N" only as build traceability; the DECISION ROUND queue has no evidence-pass item; in-progress PASS B is documentation reconciliation whose output IS this gap claim. (3) It is a requirement: explicit spec text in §32 and the §36 legend, with per-scenario Required Evidence subsections. Caveat for scoping: scenarios 8 (dual-approval leg, P0-6), 18 (DSAR, P2-3), and 1 (brand-scope leg deferred with P2-1; back-office MFA UI missing) cannot fully pass until queued builds land — the evidence pass should score partials honestly or sequence those legs after the builds. P1 not P0: this is acceptance-evidence/process debt over money/ops surfaces gating termination, not a missing live security/compliance control.

#### No reconciliation of the shipped schema against pam-domain-model.md entities/state-machines (the doc only landed at commit 54139c24)
- Spec: §30 Data Model Requirements
- Evidence: spec.md:366-367 (§30 normative); pam-domain-model.md:220-222 Wallet FSM `active↔frozen→closed` with zero grep hits for freeze/frozen in gateway/internal/wallet/ and gateway/migrations/; pam-domain-model.md:206-209 KYC `expired` state absent from internal/compliance/kyc_postgres.go; PROGRESS_LEDGER.md:86-104 DECISION ROUND has no domain-model-reconciliation item
- Notes: All three refutation avenues fail. (1) ALREADY_BUILT: the only reconciliation artifacts — docs/pam/taya-gap-analysis.md and spec.md §36/§37 — are June 2026 snapshots keyed to spec sections/domains, not the domain model's 37 entities/10 FSMs/16 invariants, and they pre-date the loop's 43 commits (they still mark MFA/AML/surveillance Missing). Spot check proves substance: Wallet freeze FSM and required wallet freeze/unfreeze audit events are wholly unimplemented; KYC verified→expired→pending re-verification is unimplemented; neither maps to any DONE/DECIDED/DEFERRED/DESCOPED item. (2) COVERED_BY_ITEM: no ledger or decisions item names a domain-model walk; the in-progress PASS B (PROGRESS_LEDGER.md:176) is section-granular and is the very pass flagging this. The reconciler's five entity→item mappings (P0-5/P0-6/P0-7/P1-1/GAP-6) are correct but incomplete. (3) NOT_A_REQUIREMENT: §30 is normative and delegates to the doc, so entity-level proof is required to close it. Suggested shape: bounded reconciliation pass emitting a coverage table (entity/FSM/invariant → schema+code evidence or new GAP-n); known seeds: wallet freeze, KYC expiry; also verify PaymentMethod, RiskProfile (points/maxCumulativePoints), Case pending_approval state. P1 because the pass itself is verification breadth but its known findings are money/compliance controls; individual findings may escalate to P0.

#### RBAC permission denials are not audited — requireRBACPermission returns 403 with no audit write
- Spec: §32 Acceptance Scenarios — Scenario 1: Admin Login and Permission Enforcement
- Evidence: gateway internal/http/rbac_admin_handlers.go:401-403 (bare httpx.Forbidden, no audit write) vs spec.md:400 ("Audit-log entries for login and denied actions; permission check trace") with Scenario 1 Status=Fail at spec.md:402; GAP-5 scope limited to auth-service auth.* events per PROGRESS_LEDGER.md:256
- Notes: All three refutation avenues fail. (1) Not built: requireRBACPermission (rbac_admin_handlers.go:389-405) and requireAdminRole (admin_handlers.go:365-376) return 403 with no audit; rbac.Service.HasPermission (rbac/service.go:227-237) is a pure read; httpx has no audit middleware — only AccessLog process logging, which the ledger itself rejects as an audit trail (mutable/per-instance). No denial audit event exists anywhere in gateway internal/. (2) Not covered: GAP-5 (DONE) is explicitly auth-service auth.* events (MFA lifecycle, OAuth login denials, logins); GAP-7 audits bonus mutations, not denials; no ledger/DECISIONS item mentions denied-admin-action auditing. (3) It is a requirement: spec §32 Scenario 1 Expected Result 'All attempts audited' (line 398) + Required Evidence line 400, an acceptance scenario at status Fail, not an example or non-goal. Priority P1 rather than P0 because enforcement itself is fail-closed and functional — the gap is a missing forensic/audit record, the same class the project ranked P1 for GAP-5. Cheap fix confirmed viable: append-only audit_logs store (migration 009) already exists and is used for successful mutations; add an audit append in the !ok branch of requireRBACPermission and the non-admin branch of requireAdminRole.

#### Audit log has no hash chain (tamper-evidence) and no structured before/after columns — scenario 17 step 2 cannot pass
- Spec: §32 Acceptance Scenarios — Scenario 17: Audit Log Integrity
- Evidence: spec.md §24 (~line 316: "tamper-evident audit log (hash-chained)" + per-field change tracking) and §32 Scenario 17 step 2 (~lines 691-696); migrations/036_audit_append_only.sql:19-26 shows provider_ops_audit_log has no prev_hash/before/after columns and lines 4-6 concede the superuser session_replication_role bypass; grep for hash_chain/prev_hash across gateway+auth internal/ is empty; office audit-logs/page.tsx:89 never populates dataBefore (dataAfter is a free-text details shim); zero hits for hash-chain/tamper/Scenario 17/CMP-02/03 in docs/pam/PROGRESS_LEDGER.md and DECISIONS_NEEDED.md
- Notes: All three refutation avenues fail. Not built: only the append-only trigger (Scenario 17 step 3) exists; no hash chain, no chain-verification endpoint, no structured before/after (the office UI's dataBefore/dataAfter type is an unwired placeholder). Not covered: no ledger/DECIDED/DEFERRED/DESCOPED item mentions it; GAP-5 (durable auth audit) and P2-3 (DSAR) are adjacent but distinct. Is a requirement: §24 body text mandates hash-chaining and per-field change tracking independently of the acceptance scenario, and §4 non-goals do not exclude it. P1 not P0: the DB-trigger append-only control is a real compensating control against everyone except a superuser insider, so this is audit-evidence hardening, not a launch-blocking money/compliance gate. Suggested scope if actioned: add prev_hash/entry_hash columns + structured before/after JSONB to provider_ops_audit_log (new goose migration, per CLAUDE.md rule), compute chain in the audit store write path (internal/http/provider_ops_audit_store.go), and expose an admin verify endpoint.

### P2

#### Per-country jurisdiction switch matrix missing — signup/login toggles, signup age, per-country deposit limits, per-country KYC/geo/AWA system selection
- Spec: §8 Tenant / Brand / Jurisdiction Model
- Evidence: spec.md §8 lines 144-147 (per-country switch matrix required) + gateway internal/compliance/gate_surface.go lines 7-17 (surfaces = trade/deposit/withdraw only; "Registration ... is deliberately NOT a surface"); no per-country config rows in any migration (035 is per-market overlay, 037 is tenants-only); GAP-8 decision covers only global-allowlist tightening; PROGRESS_LEDGER.md line 211 lists "global jurisdiction admin UI" merely as a candidate P2-1 slice, never built or decided.
- Notes: All three refutation avenues fail. Not built: geo gate is a single global env-var allowlist over trade/deposit/withdraw; auth register handler (services/auth/internal/http/handlers.go:577) has no jurisdiction/age gating; office UI has no jurisdiction page. Not covered: GAP-8 (tightening-only), P2-1 peripherals (feature-flag store + tenant admin), and P1-1 AWA thresholds are fragments, none provide per-country signup/login toggles, signup age, per-country deposit limits, or per-country KYC/geo/AWA system selection. It is a requirement: spec §8 plus §11 "gated by jurisdiction signup rules"; §4 non-goals do not exclude it. Nuance: signup-not-gated is a deliberate code-level decision (gate_surface.go comment referencing docs/compliance/geofencing-kyc.md) but was never recorded as a ledger DECIDED/DESCOPED item, so it documents divergence rather than accounting for the gap. P2 is appropriate: single-jurisdiction, points-only launch (Tiangge §2/§19 forbids money paths) removes launch-time enforcement consequence; signup-age needs counsel input before enforcement anyway.

#### Brand Registry lacks scope dimensions (brand/country/currency) and encrypted-value support — platform_config is a flat KV table
- Spec: §8 Tenant / Brand / Jurisdiction Model
- Evidence: platformconfig.go:52-58 (flat KV schema: config_key/config_value/description/updated_by/updated_at, no scope columns, no encryption; zero aes/gcm/encrypt hits in gateway+auth non-test code) vs spec.md §8 lines 147-149 ("key-value config, scoped all-brands or specific-brand, with country/currency and encryptable values"); no covering item in PROGRESS_LEDGER.md GAP-1..8 (252-257) or DECISIONS_NEEDED.md — GAP-3 (line 80) is TOTP-only, P2-1 deferral (lines 57-62) covers tenant/brand scoping only
- Notes: Gap is REAL but narrower than claimed: the brand-scope dimension is defensibly absorbed by the P2-1 core deferral (single-tenant at launch makes every entry implicitly all-brands; DECISIONS_NEEDED.md:57-62). The unaccounted residual is (a) country/currency scoping on registry entries and (b) encryptable config values. GAP-3's decided AES-GCM work is scoped to auth_mfa TOTP secrets (mfaUpsertPending/mfaGet) and is not yet built anyway; GAP-8 covers geo-allowlist tightening, not per-country registry values. Office tenants page (app/(dashboard)/tenants/page.tsx) is bare CRUD with no registry dimensions. P2 because launch is single-tenant, jurisdiction enforcement is env-driven fail-closed, and no secrets are stored in platform_config today — escalates to P1 the moment ops stores vendor credentials in the flag store, since values would sit plaintext in the DB.

#### No unified cross-domain case center — only per-domain queues exist (surveillance cases, KYC queue, market disputes)
- Spec: §9 Navigation and Information Architecture
- Evidence: spec.md:163 ("8. **Cases** (KYC/AML/fraud/integrity/dispute/withdrawal)") + spec.md:266-271 (§19 structured cases by six types); no `cases` route under talon-backoffice/packages/office/app/(dashboard)/ (only surveillance, disputes, compliance/kyc); surveillance/page.tsx:65 scoped to /api/v1/admin/surveillance/cases (SurvCase, single domain); grep for case_center/CaseType/unified-case across gateway+auth+office = zero hits; ledger P1-4 (PROGRESS_LEDGER.md:263) wrote "its own alert/case tables" and no ledger item cites §9 nav item 8
- Notes: All three refutation avenues fail. (1) Not built: only per-domain queues exist — surveillance cases (P1-4/5), KYC queue (P0-3), market disputes page; no unified hub in UI, gateway packages, or migrations. (2) Not covered: P0-5 adds aml_cases (AML only, DECIDED-queued), P1-1 adds withdrawals queue (DECIDED-queued); no item consolidates cross-domain, none cites §9. (3) It is a requirement: §9 nav item 8 is a numbered top-level section and §19 independently requires structured cases by the six types with status/assignee/priority/SLA/maker-checker; §4 non-goals do not exclude it; §36 matrix itself marks general support cases Missing. P2 because per-domain enforcement exists or is queued — the gap is the consolidating hub (breadth/ergonomics), not a compliance or money-path hole. Sequencing suggestion: build the shared §19 case model alongside P0-5's aml_cases to avoid a third divergent case schema.

#### Segmentation/CRM and notification-template office UIs missing — P2-2 tags/query/campaigns and P1-6 template store are API-only; /campaigns is a guarded redirect stub and no segments/tags/templates nav or page exists
- Spec: §9 Navigation and Information Architecture
- Evidence: spec.md:164 (§9 nav item 9 requires CRM segments/tags/campaigns/communications); office app/(dashboard)/campaigns/page.tsx:4 is redirect("/dashboard") and layout.tsx:35-104 navItems has no segments/campaigns/templates entry, while gateway handlers.go:697 (registerSegmentationAdminRoutes) and handlers.go:256 (registerNotificationTemplateAdminRoutes) prove the API-only backend; PROGRESS_LEDGER.md:145 planned "an office CRM page for tags/query/campaigns" but P2-2 was marked DONE backend-only (line 138) and the post-DECISION-ROUND queue (lines 101-104) omits it
- Notes: All three refutation avenues fail. (1) Backend built and wired, UI absent: no segments/tags/templates/campaigns page exists under app/(dashboard); /campaigns is a guarded redirect stub that tests/app-router-legacy-routes.test.ts pins as a stub (ledger line 275 says use a fresh route, not /campaigns). (2) No ledger item covers the UI: the "office CRM page" tail of Resume-plan item 1 never landed, and the DECISION ROUND queue has no CRM/template-UI item; P1-6 delivered the template editor API only. (3) Spec §9 is a hard nav requirement and §4 non-goals do not exclude it. Scope note: the gap is the segments/tags, campaigns, and communications/template-editor office surfaces only — bonuses (/bonuses) and loyalty (/loyalty) halves of nav item 9 are present. P2 because campaign dispatch is fail-closed in launch mode and the RBAC-gated API is operable; it is breadth/operability, not compliance or money exposure.

#### Maintenance/Config breadth missing: products, payment-methods, consents, password-policy, and third-party exclusion-service admin screens
- Spec: §9 Navigation and Information Architecture
- Evidence: spec.md:166-167 (Nav 11) + spec.md:322-329 (§25 body requirement: Payment Methods, Consents versioning, Password Policy, Exclusion Service) + spec.md:189, 210-213, 351 (independent §11/§13/§28 requirements); no matching admin route in gateway internal/http (full /api/v1/admin/* inventory has no consent/password-policy/exclusion/payment-method-registry route); services/auth/internal/http/handlers.go:1706 hardcoded minPasswordLength=7; PROGRESS_LEDGER.md DECISION ROUND table (lines 86-99) and DECISIONS_NEEDED.md contain no covering item
- Notes: Gap is real but should be narrowed from five residuals to three-and-a-half. (1) PRODUCTS is effectively ALREADY_BUILT for this single-product platform: §25's "Products/market-type catalog" maps to the live-wired catalog admin (/api/v1/admin/{categories,series,events,markets,market-sources,resolution-sources} + office prediction-admin pages) — drop it or record a one-line satisfied-by note. (2) PAYMENT METHODS registry (§22 first bullet: auto/manual/enabled flags, brand/jurisdiction scope) is adjacent to P1-1's charter — the P1-1 decision claims to "satisfy PAM §22" but its enumerated scope (withdrawals queue + AWA + deposits view) omits the registry; fold it into P1-1's flag-gated build rather than minting a new item. (3) CONSENTS versioning (§25/§28), (4) PASSWORD POLICY admin (§11/§25 — today a hardcoded const, though MFA + per-IP lockout are enforced), and (5) EXCLUSION-SERVICE national-register integration (§13 — internal self-exclusion exists via P1-3, third-party integration does not) map to no DONE/DECIDED/DEFERRED/DESCOPED item and are genuinely unaccounted. Priority P2: none blocks the points-only, crypto-native, outside-US launch (money rails flag-gated OFF; exclusion-register and consent regimes are license/counsel-dependent, same class as the pending P2-3 counsel inputs; password-policy config is hardening, not an open hole).

#### Player search limited to email/username ILIKE + status filter — no phone, national-ID, tag, or trading-attribute (volume/exposure) predicates in the office search
- Spec: §10 Player Search and Player Profile 360
- Evidence: sql_admin_repository.go:119,129 (email/username ILIKE + status only); PunterSearch.tsx:55-57 (name/email + status only); segmentation.go:244-250 (Query = TagID/Status/CreatedAfter, no volume/exposure, no office UI consumer); spec.md §10 lines 174-176 requirement text; §36 row 2 still Partial/Fail
- Notes: All three refutation avenues fail. (1) Not built: gateway search has only email/username ILIKE + status; office PunterSearch is client-side name/email + status; P2-2 RunQuery adds tag/status/created-after but is API-only (no office page references segments) and has no volume/exposure criteria; no phone or national_id column exists in any gateway migration or the auth service, and KYC document_id is queryable by user_id only — so phone/national-ID search is also a data-model gap, not just a query gap. (2) Not covered: ledger P0-3/P1-3 fixed §36 row 2's named tab gaps (KYC, Limits); P2-2 is the closest DONE item but explicitly excludes volume/exposure and UI; DECISION ROUND and DECISIONS_NEEDED.md contain nothing on player search. (3) It is a requirement: spec §10 declaratively requires search by user ID, name, email, phone, national ID, tag, and trading attributes; §4 non-goals do not exclude it; §36 grades the area Partial with Scenario 2 Fail. P2 because it is back-office breadth (players remain reachable by ID/email; compliance surfaces exist); implementation note: phone capture and a searchable national-ID projection (from KYC data per P0-3) are prerequisites for two of the predicates.

#### Profile-360 missing Bonuses/Rewards, Notes & Timeline, and Cases tabs (AML/Risk and Payments tabs are accounted by P0-5/P1-1 but neither item explicitly scopes a profile tab)
- Spec: §10 Player Search and Player Profile 360
- Evidence: office/app/components/users/PunterProfile.tsx:6 (tabs are exactly overview|trades|wallet|kyc|limits) vs docs/pam/spec.md:175-178 (§10 requires Bonuses/Rewards, Notes & Timeline, Cases tabs); gateway internal/http/surveillance_admin_handlers.go:65-67,128-129 (case/alert list filters are status/limit/offset only — no per-user query)
- Notes: Gap survives all three refutation avenues but is NARROWER than claimed. (1) Notes correction: the reconciler's "PunterProfile.tsx surfaces none of them" is wrong for Notes — the live Profile-360 screen office/app/(dashboard)/users/[id]/page.tsx already loads /api/v1/admin/punters/{id}/notes (lines 126-132) and renders an Admin Notes card with add-note (lines 370-390); only the unified Timeline half is missing. (2) Bonuses per-user data exists only on the global /bonuses page (P1-6), not on the profile. (3) Cases are genuinely not queryable per-user anywhere. Not covered by any ledger item: P1-6/P1-4 shipped global pages citing §21/§18-19 (P1-6 does not cite §10); no GAP/DEFERRED/DESCOPED item touches Profile-360 tab completeness; §36 row 2's "missing KYC & Limits tabs" note under-enumerated, and is a status snapshot, not a scoping decision. Spec §10 is plain requirement text, not an example or non-goal. P2 because it is back-office breadth: all underlying controls exist on other working admin surfaces, so no compliance/security/money-path exposure. Agree with the reconciler that the P0-5 and P1-1 build briefs should be read to include profile-tab surfacing (AML/Risk, Payments), and the fix here should add: per-user bonus view on the profile, a per-user case filter (surveillance store + handler + tab), and an activity timeline (existing audit/notes/settlement events).

#### Registration does not persist affiliate tag or signup country — GAP-6 (DECIDED) covers IP/user-agent capture only
- Spec: §11 Identity, Account Lifecycle, and Authentication
- Evidence: spec.md:185-186 requires "Registration captures identity, contact, affiliate/IP/country"; auth handlers.go:56-68 registerRequest has no affiliate/country fields and the auth_users INSERT (~:1289) persists neither; punters.country_code (gateway migrations/001_punters.sql:8) is never written — all INSERT INTO punters sites (sql_repository.go:2227, predict_privacy_handlers.go:94, migrate_legacy_loyalty.go:189) write only id/email/status; GAP-6 scope is IP+UA+device only (PROGRESS_LEDGER.md:98,248,258).
- Notes: Requirement is normative in §11; the §4 non-goal (spec.md:70) excludes only a standalone affiliate platform, not registration-time capture. Country piece is record-keeping, not enforcement — the fail-closed GEO_ALLOWED_COUNTRIES gate handles jurisdiction at request time, and GAP-6's comparable signal-capture work was rated P2. Recommend bundling with the GAP-6 punter_signals build (same auth-registration ingestion point): populate punters.country_code (currently a dead column the admin detail view SELECTs as NULL) and add an affiliate/btag attribution field. Slight P1 argument exists via per-country KYC (§12) and AML geo rules, but persistence absence does not currently break an enforced control.

#### No configurable password policy (complexity/rotation/reuse, player + staff) and no account-level failed-login lockout — only main's per-IP login rate limiter exists
- Spec: §11 Identity, Account Lifecycle, and Authentication
- Evidence: Lockout ALREADY_BUILT: services/auth/internal/http/handlers.go:1044-1046,1062,1132 (per-username lockout in live Login, player+staff via admin_users fallback at 1057; 5 fails/1min -> 15min lock, constants at 1801-1804; Redis backend redis_rate_limiter.go:44-102). Residual gap: validatePasswordStrength at handlers.go:1706-1710 is hardcoded length>=7 only — no complexity/rotation/reuse, not configurable; no ledger item in docs/pam/PROGRESS_LEDGER.md or DECISIONS_NEEDED.md mentions password policy; spec.md §11 requires "password policy and lockout (OMEGA Password Policy: player + staff)".
- Notes: The claim as written is substantially refuted and must be narrowed before ledgering. FALSE parts: (1) account-level failed-login lockout exists and is wired in the live auth service Login path (in-memory + Redis backends, tested, applies to both players and back-office staff, triggered by both password and OTP failures, cleared on success); (2) "only main's per-IP limiter exists" is wrong — there is also a per-username rate limiter (10/min) and the lockout, alongside the per-IP spray limiter (30/min, SEC-05). This is the live auth service (same file where P0-1 MFA and the P1-05 merge landed per ledger), not dead code. TRUE residue: no configurable password policy engine — only a hardcoded 7-char minimum at registration/password-change; no complexity classes, rotation/expiry, or reuse/history, and no back-office config surface, while spec §11 cites the OMEGA Password Policy (player + staff). No ledger item (Done/DECISION/GAP/deferred) covers it. Suggested rewrite: "Password policy is hardcoded min-length-7 only; no configurable complexity/rotation/reuse policy (OMEGA Password Policy, player + staff)". P2: config/UI breadth — brute-force defense (lockout + dual rate limits) and bcrypt storage are already sound, so remaining exposure is weak-but-7-char passwords and policy configurability, not an open attack path.

#### No per-country/jurisdiction KYC requirement matrix — KYC flags are global env, not per-country
- Spec: §12 KYC, AML, Risk, and Compliance
- Evidence: spec.md:196-198 (§12 "per-country requirements via OMEGA KYC Status Requirements") + spec.md:145-147 (§8 Brand Country per-country "KYC system" switch, PD-7 marks it unbuilt); code: pretrade_gate.go:100-104 and cmd/gateway/main.go:363 gate KYC on global env flags with no country input; ledger: GAP-8 (PROGRESS_LEDGER.md:99,249) covers geo-allowlist access only, and grep for per-country/multi-jurisdiction/OMEGA KYC across PROGRESS_LEDGER.md + DECISIONS_NEEDED.md returns zero covering items
- Notes: All KYC gating is global env (KYC_ENFORCEMENT, KYC_REQUIRED_FOR_TRADING, KYC_WITHDRAWAL_THRESHOLD_CENTS). Per-country machinery that exists (GeoGate env sets in internal/compliance/geo_gate.go:32-42; migration 035 per-market jurisdiction overlay) gates ACCESS, not KYC document/verification requirements. Nearest ledger items do not cover it: GAP-8 is runtime tightening of the geo access allowlist; P2-1 DEFERRED is brand/tenant multiplicity (its shipped peripherals were feature-flag store + tenant admin; the "global jurisdiction admin UI" slice was named but never built or queued). Spec requires it in both §12 and §8, and PD-7 explicitly marks the per-jurisdiction switchboard Partial/missing, so it is not an example or non-goal. P2 because fail-closed global KYC (P0-2/GAP-1) plus default-block-US geofencing satisfies the single-jurisdiction launch posture (LEG-1); escalate when a second license/jurisdiction is planned.

#### No reality-check mechanism exists (no session-duration reminders anywhere in gateway or player app)
- Spec: §13 Responsible Gaming / Responsible Trading
- Evidence: docs/pam/spec.md §13 lines 208-210 ("cool-off, reality check" listed as required RG capability); no code match for "reality" anywhere in apps/Phoenix-Predict-Combined; nearest neighbor /api/v1/compliance/rg/session-limit (internal/compliance/handlers.go:636-663) is a distinct requirement (session limits) and a non-persisting echo stub; no ledger or decision item in docs/pam/PROGRESS_LEDGER.md or docs/pam/DECISIONS_NEEDED.md covers reality check
- Notes: Gap is real but narrower than claimed. Spec §4 Non-Goals (lines 71-72) puts the player-facing trading UI out of scope, so the in-session reminder popup itself is not this spec's deliverable — the reconciler's "nor player app" framing over-scopes. The unaccounted requirement is the PAM/back-office side: per-trader reality-check interval storage/config (jurisdiction-switchable) exposed on the shared trading API for the client to consume. Nothing of that exists: no schema, no endpoint, no config; the session-limit endpoint is both a different §13 item and a stub that persists nothing. Not covered by P1-3 (admin read-only limits tab) or GAP-1 (fail-closed RG). P2 because it is jurisdiction-dependent RG breadth (LEG-1 targets a financial regime, not a gambling license where reality checks are typically mandated), touches no money path, and the enforced-before-order RG invariants are already handled by other items.

#### No problem-trading flag on the player profile/RG state
- Spec: §13 Responsible Gaming / Responsible Trading
- Evidence: spec.md §13 line ~209 ("problem-trading flag;" as requirement text) vs. rg_postgres.go:51-61 (player_restrictions: blocked/block_reason/self_excluded/self_exclusion_permanent/self_exclusion_until/cool_off_until — no such flag) and PunterProfile.tsx:13 (status: "active"|"suspended"|"inactive"); repo-wide grep for "problem" in gateway/auth Go, migrations 001-055, and office App-Router yields no capability match; PROGRESS_LEDGER.md:183 (P1-3) covers only read-only limits/self-exclusion tab and no ledger/DECISIONS_NEEDED item names the flag.
- Notes: All three refutation avenues fail. Not built: the live RG store (internal/compliance/rg_postgres.go, wired via internal/http/rg_admin_handlers.go and handlers.go) has no problem-trading flag; the generic blocked/block_reason columns are a hard account block, not an RG monitoring marker. Not covered: P1-3 (DONE) is scoped to limits/self-exclusion display; P0-5 is §12 AML cases; no GAP/DECISION item mentions it. Is a requirement: §13 body text, not excluded by §4 non-goals; spec's own §36 matrix marks §13 Partial/Fail. P2 because the §13 enforcement invariants (limits pre-order, self-exclusion login/trade block) are already handled and the flag is a monitoring/state marker with no defined enforcement semantics — best bundled into the §12 risk-profile or P0-5 case-workflow item as the reconciler suggested; promote to P0/P1 if the eventual license regime mandates problem-trader flagging.

#### No national/third-party self-exclusion register integration or seam
- Spec: §13 Responsible Gaming / Responsible Trading
- Evidence: spec.md:211-212 requires "National self-exclusion register integration via OMEGA Exclusion Service pattern"; grep for national/gamstop/register-class terms across gateway internal/, migrations/, auth, and office UI returns zero hits; internal/compliance/services.go RG interface has only internal SetSelfExclusion (operator-level exclusion at rg_postgres.go:397 is not a register integration); PROGRESS_LEDGER.md:183 (P1-3) covers only the operator-level Limits/self-exclusion tab and no DECISIONS_NEEDED/GAP/DESCOPED item mentions a register
- Notes: Operator-level self-exclusion is fully built (rg_postgres.go, /api/v1/compliance/rg/self-exclude, fail-closed variant, office Limits tab P1-3), which could be mistaken for coverage — but the spec line is specifically about external/national register integration, and no seam (interface method, config, stub) exists for it. Not excluded by §4 Non-Goals. P2 because register schemes are per-license mandates and the outside-US crypto-native launch has no pinned license requiring one yet; escalates to P0-compliance the moment a register-mandating license (UK/DK/SE/DE/NL-class) is targeted. Cheap to seam now given the existing fail-closed RG service pattern.

#### Wallet is single-currency points — no currency dimension on wallets/ledger
- Spec: §14 Wallet, Balances, Ledger, and Transactions
- Evidence: spec.md:217-218 (normative §14 bullet: per-currency cash wallets, multi-currency) vs. internal/wallet/service.go:2160-2178 (live wallet_balances is user_id-PK single-row-per-user, wallet_ledger has no currency column). Migration 006's currency_code is on the dead sportsbook tables — p0-7-design.md lines 9-10 confirm the goose wallets/ledger_entries are NOT the live store.
- Notes: All three refutation avenues fail. (1) Not built: zero currency hits in internal/wallet; live runtime schema (ensureSchema) has no currency dimension; even the dead migration-006 wallets table is punter_id UNIQUE (one wallet/user, not per-currency). (2) Not covered: no ledger/decisions item mentions multi-currency; P2-4 descope covers on-chain settlement only; the APPROVED p0-7-design.md accounts/transactions/postings schema contains no currency/asset field (grep empty), so P0-7 as approved does not absorb it. (3) It is a requirement: §14 normative bullet, corroborated by pam-domain-model.md:63-74 (wallet per currencyId, ledger entries carry currencyId); §4 Non-Goals silent; pam-open-questions.md Data Model Q1 flags currency handling as open. Priority P2 because the decided single-asset custodial-USDT launch posture means no day-one money/compliance exposure — but with a hard timing rider: a nullable-then-NOT-NULL currency/asset column must be raised in the P0-7 phase-1 design review, where it costs one column + one decision; after the double-entry ledger holds live money it becomes an expensive money-table retrofit. The reconciler's disposition is confirmed as-is.

#### Case model lacks assignee/SLA/case-notes fields and a general support case type
- Spec: §19 Case Management
- Evidence: spec.md:268-270 (§19 normative: "status, assignee, priority, SLA, linked entities, notes") + spec.md:814 (§36: "general support cases Missing") vs surveillance.go:67-78 (surveillance_cases: title/status/priority/opened_by/resolution only); repo-wide grep for assignee/SLA/case_note/support_case in gateway internal/+migrations/, auth, and office app/(dashboard) returns no live code; ledger P0-5 scope (PROGRESS_LEDGER.md:90) covers aml_cases plumbing only, no case-model fields or general-support type; no GAP-9+ exists
- Notes: All three refutation avenues fail. (1) Not built: only case models are surveillance_cases (no assignee/SLA/case-notes; single resolution string at close, surveillance.go:263), prediction_disputes (market-resolution only, migrations/023), and punter-scoped user_notes (migrations/022, CRM not case-scoped). Office "SLA" hits are dead sportsbook provider-ack-sla audit presets. (2) Not covered: P1-4 (DONE) built the deficient model; P0-5 (DECIDED) is AML-type alert→case→disposition plumbing with no assignee/SLA/notes in scope; GAP-1..8 unrelated. (3) It is a requirement: §19 is normative text, and §36 line 814 marks general support cases Missing/Fail. Two nuances: "general support" as a case TYPE rests on §36's matrix note, not §19's enumerated type list (KYC/AML/fraud/integrity/dispute/withdrawal); and §19's "approval steps" leg is already covered by DECIDED P0-6 (maker-checker), so the gap is correctly scoped to fields + general-support type. Priority P2: points-only launch, no compliance gate or money path depends on these fields today; it is back-office operability breadth. Recommend the P0-5 aml_cases build adopt the fuller case shape (assignee, SLA, per-case notes, generic case type) so it is not retrofitted.

#### Email-only communications — no SMS, push, or in-app channels or channel abstraction
- Spec: §20 Notes, Documents, and Communication History
- Evidence: notify.go:1-4 ("Today the channel is email; the Notifier interface keeps SMS/push a drop-in addition") + templates.go:75 (notification_templates has no channel column) + spec.md §36 matrix row: "Notes & comms | 20 | Partial | Fail | communications = SMTP/log stub"
- Notes: Confirmed on all three avenues. Code: only LogNotifier + SMTPNotifier exist in gateway internal/notify; repo-wide grep finds no SMS/Twilio/FCM/APNS/push implementation; office UI has no channel surface; the ws package is live market-state sync, not a comms channel. Ledger: P1-6 (PROGRESS_LEDGER.md:262) covers only the §20 email template registry; no GAP item, DECISION ROUND row, or DECISIONS_NEEDED entry defers/descopes multi-channel. Spec: §20 line ~277 lists multi-channel as a requirement and §36 marks it Partial/Fail; §4 non-goals do not exclude it. Minor wording correction to the claim: a Notifier interface seam does exist (notify.go:19-22), but it is email-shaped (to/subject/body), so no real multi-channel abstraction. P2 because launch mode forbids player-facing sends (campaign dispatch fail-closed behind platform-config flag, internal/http/handlers.go:691-693) and transactional notices already flow over email/log — breadth, not compliance or money.

#### No per-player sent-communication history store (Idefix PlayerSentContent analog)
- Spec: §20 Notes, Documents, and Communication History
- Evidence: spec.md:278-279 requires "full sent-content history (Idefix PlayerSentContent)"; only DDL in internal/notify is notification_templates (templates.go:75); campaign dispatch returns 501 unwired (segmentation_admin_handlers.go:367-368); resolution sender persists nothing (internal/http/handlers.go:292-297); P1-6 ledger entry (PROGRESS_LEDGER.md:262) delivers templates/exports only, no send log; no GAP/DECISION item covers it
- Notes: Confirmed on all three avenues. (1) Not built: no table or code path records rendered sent content per player — notify has only LogNotifier/SMTP transports + the notification_templates registry; migrations 001-055 contain no sent-content table (052 is perms only); office (dashboard) has no comm-history UI. (2) Not covered: P1-6 cites §20/Scenario 12 but its deliverables are the template store, bonus UI, and CSV exports; the DECISION ROUND table and DECISIONS_NEEDED.md have nothing on sent-content history; P2-2's dispatch worker is an explicit unwired follow-up. (3) Real requirement: §20 bullet requirement plus Scenario 12 step 3 "View communication history" / expected "message sent and logged"; §36 matrix still marks Notes & comms Partial on the communications half. Priority P2 because every player-facing send is currently fail-closed (campaign execute 501/403; resolution notices go to an ops recipient list), so exposure is latent — but this must land before campaign dispatch is ever enabled or player-facing regulatory/settlement notices ship, at which point it upgrades to P1. Natural build shape: a sent_communications table (player_id, channel, template_key, rendered subject/body, recipient, status, correlation id) written at the notify seam, a Profile-360 Communications tab, and wiring into the future dispatch worker.

#### Tag groups and segment-query export are absent from the P2-2 build
- Spec: §21 Segmentation, CRM, Bonuses, Rewards, and Lifecycle
- Evidence: spec.md:283-284 requires "tags + tag groups ... plus a Query Builder for ad-hoc segments/exports"; segmentation_admin_handlers.go registers only tags/query/users/campaigns routes (lines 52-295, no export); report_exports_handlers.go:24-25 exports only kyc-statuses.csv and surveillance-alerts.csv; PROGRESS_LEDGER.md:138 scopes P2-2 DONE as "tags + query builder + campaigns" with no deferral/descope of tag groups or segment export
- Notes: Partial gap: tags and a fully parameterized query builder are built and wired; missing are (a) the tag-group grouping construct (no tag_groups table or code concept anywhere in gateway/auth/office) and (b) CSV/export of segment query results (RunQuery returns JSON only; P1-6 CSV routes are fixed operational datasets, not segment membership). Not covered by any GAP item, decision, or descope note; not a §4 non-goal — the only §21 deferral is Rewards/Loyalty. P2 because it is CRM breadth inside an already-P2 item with no compliance/money-path exposure; a future segment-export endpoint should reuse the P1-6 pattern of auditing exports as sensitive data extractions.

#### No office UI for tags/query-builder/campaigns — P2-2 is backend/API only and /campaigns is a guarded redirect stub
- Spec: §21 Segmentation, CRM, Bonuses, Rewards, and Lifecycle
- Evidence: apps/Phoenix-Predict-Combined/talon-backoffice/packages/office/app/(dashboard)/campaigns/page.tsx (redirect("/dashboard") stub; no crm/segments route exists in the (dashboard) tree; zero 'segments' hits in office app/ or pages/) vs. gateway internal/http/handlers.go:691-698 (registerSegmentationAdminRoutes — backend wired); docs/pam/PROGRESS_LEDGER.md:145 ("Then an office CRM page for tags/query/campaigns" — planned, never landed) against line 138's "P2-2 DONE"
- Notes: All three refutation avenues fail. (1) Not built: P2-2 backend routes are live in handlers.go:691-698 but the office has no tags/query-builder/CRM-campaigns UI; /campaigns is a guard-locked redirect stub (tests/app-router-legacy-routes.test.ts) and the /bonuses page's campaigns table is the P1-6 bonus-engine surface (bonus_handlers.go), not segmentation/CRM. (2) Not covered: ledger marks P2-2 DONE but every described P2-2 artifact (migrations 053/054, parameterized query builder, fail-closed campaign execute — verification #4, ledger:171) is backend-only; the resume plan's "office CRM page" follow-on never landed and the DECISION ROUND build queue (ledger:102-104) omits it; nothing DEFERRED/DESCOPED covers it. (3) It is a requirement: spec is the Back Office spec, §4 non-goals exclude only the player-facing client, §21:283-285 requires tags+tag-groups+Query Builder and CRM campaigns (only Rewards/Loyalty carries a Defer marker). Recommended shape: new /crm route mirroring /exports, per ledger guidance not to reactivate guarded legacy routes. P2: operator breadth, no money/compliance path; dispatch stays fail-closed server-side.

#### No event-triggered lifecycle messaging/journeys — campaigns are one-shot dispatches only
- Spec: §21 Segmentation, CRM, Bonuses, Rewards, and Lifecycle
- Evidence: spec.md:285 ("CRM: campaigns and lifecycle messaging (Idefix campaigns; OMEGA Engage)") + spec.md §36 row 13 ("Segmentation & CRM ... Missing ... no segments/journeys"); PROGRESS_LEDGER.md:138 P2-2 DONE scope is only "tags + query builder + campaigns"; internal/segmentation/segmentation.go:27-29,324-337 (one-shot campaign = tag + notification|bonus action, preview-only, dispatch fail-closed per verification #4 "no send wired")
- Notes: All three refutation avenues fail. (1) Not built: no journey/drip/scheduler/trigger-binding code in segmentation, its handlers, or migrations; internal/notify is transactional ops email (market resolution to NOTIFY_RESOLUTION_TO) not player journeys; bonus publishCampaignLifecycleEvent is campaign admin-state eventing, not event-triggered player messaging; office (dashboard)/campaigns is a guarded redirect stub. (2) Not covered: no GAP-1..8, DECISION ROUND, DEFERRED, or DESCOPED item mentions lifecycle messaging/journeys; only Rewards/Loyalty in §21 is spec-marked Defer/P2. (3) It is a requirement: §21 CRM bullet sourced Idefix (Real)/OMEGA (High), not excluded by §4 non-goals. Caveat: the reconciler's requirement text "Lifecycle messaging (event-triggered journeys)" paraphrases the spec's literal "campaigns and lifecycle messaging"; "journeys" wording comes from the §36 scoring note. P2 because launch-mode prohibits user-facing sends (campaign execute already fail-closed 403/501), so a journeys engine would be inert until launch policy changes — safely deferrable breadth, not compliance or money-path.

#### Scenario 12 cannot pass: no player-send path or sent-content history (notes + template registry halves exist)
- Spec: §32 Scenario 12: Player Communication and Notes (§20)
- Evidence: spec.md:273-279 (§20 requires multi-channel send + full sent-content history) and :589-606 (Scenario 12, Status Fail); zero code hits for sent_content/PlayerSentContent/communication_history in apps/Phoenix-Predict-Combined; only send path is ops-recipient resolution notifier at gateway internal/http/handlers.go:245-247; PROGRESS_LEDGER.md:262 (P1-6) delivered only the template-store half; no GAP/DEFERRED/DESCOPED item covers it
- Notes: Notes half exists (migration 022_user_notes.sql + office users/[id] Add Note); template registry exists (P1-6, internal/notify/templates.go wired at handlers.go:252-257). Missing: per-player addressable send path and a logged sent-content history store/UI. Launch mode fail-closes user-facing sends (segmentation_admin_handlers.go:35 comment), so not launch-blocking — hence P2, though the sent-content-history store leans P1 if messaging is enabled pre-launch for regulatory/settlement notices. Spec §4 non-goals do not exclude back-office player communications.

#### Scenario 13 not exercisable as written: Marketing/CRM actor has no UI (backend query builder/tags/campaigns are DONE via API)
- Spec: §32 Scenario 13: Segmentation and CRM (§21)
- Evidence: office app/(dashboard)/campaigns/page.tsx:3-5 is a redirect-to-dashboard stub (pinned by guard test tests/app-router-legacy-routes.test.ts per PROGRESS_LEDGER.md:275); no segment/tag/query-builder page exists in office pages/, containers/, or app/components; backend IS wired (gateway internal/http/handlers.go:691-698 registerSegmentationAdminRoutes) confirming P2-2 DONE is backend-only (PROGRESS_LEDGER.md:138); spec.md:607-624 (§32 Scenario 13, Status: Fail) + §36 matrix row 'Segmentation & CRM | 21 | 13 | Missing | Fail'
- Notes: Gap is narrower than 'no CRM': the P2-2 backend (tags, parameterized query builder, campaigns with fail-closed dispatch) is DONE with unit + live-Postgres tests and passed verification #4. What is missing is the entire office UI for the Marketing/CRM actor and any Playwright CRM spec (e2e/backoffice/ has only bonuses.spec.ts and reports.spec.ts), so Scenario 13's UI-driven steps cannot be exercised as written. Not COVERED_BY_ITEM: the only ledger mention of the UI is the stale resume-plan tail (PROGRESS_LEDGER.md:145 'Then an office CRM page for tags/query/campaigns') — an untracked planning note, not a DONE/DECIDED/DEFERRED/DESCOPED item; the DECISION ROUND table has no CRM-UI row. Not NOT_A_REQUIREMENT: §4 non-goals exclude only the player-facing UI; §21 + §29 + §32 make the back-office CRM UI required. Build note: the new page must use a fresh route (e.g. /crm), since /campaigns is guard-tested to stay a redirect stub. P2 because it is marketing breadth with no money/compliance exposure and campaign dispatch stays fail-closed in launch mode.

#### No payment-methods registry (flags + brand/jurisdiction scope) — needs build-or-descope note
- Spec: §22 Payments, Deposits, and Withdrawals
- Evidence: spec.md:294-295 (§22 registry requirement, no Defer marker; also §25 line 326 'Payment Methods' config screen and line 160 screen taxonomy); code: no CREATE TABLE payment_methods anywhere in go-platform — only payment_transactions.payment_method TEXT (internal/payments/db_service.go:65) and hardcoded 2-item GetPaymentMethods stub (db_service.go:351-374); no app/(dashboard) route for it; DECISIONS_NEEDED.md:49-55 P1-1 scope = withdrawals queue + AWA + deposits view only; PROGRESS_LEDGER.md:96 P2-4 descope note cites §17/§26 only, not §22 registry
- Notes: All three refutation avenues fail. Not built: PaymentMethod struct in types.go is a user-facing wallet method (lastFour/expiry), not an admin registry with auto/manual/enabled flags + brand/jurisdiction scope; GetPaymentMethods returns hardcoded defaults with a 'would come from a payment vault' comment. Not covered: P1-1 decided scope excludes it; p2-4-descope.md covers on-chain settlement only (no mention of payment methods/registry); P2-1 peripherals cover tenant/brand + jurisdiction admin, not payment methods. It is a requirement: appears as a §22 bullet plus a §25 Maintenance/Config screen and the §26 seed-data expectation; §4 Non-Goals does not exclude it. P2 because launch policy is a single custodial-USDT rail (breadth, not money-path ops); disposition should be a formal descope-or-build note mirroring the p2-4-descope.md precedent, covering both §22 and the §25 'Payment Methods' screen as one item.

#### Multi-PSP fiat deposit connectors absent — needs formal descope note per crypto-native launch policy
- Spec: §22 Payments, Deposits, and Withdrawals
- Evidence: spec.md:296 requirement bullet in §22 (not in §4 Non-Goals, spec.md:66-76); no PSP connector code anywhere (gateway grep stripe/adyen/PSP → none; internal/payments/handlers.go:97-340 is a single generic webhook seam, payment_method free-text at db_service.go:65); docs/pam/designs/p2-4-descope.md cites §17/§26 only — DECISIONS_NEEDED.md:74-75 promised a §22 citation that was never delivered; P1-1 decision (DECISIONS_NEEDED.md:49-55) scopes only withdrawals-queue+AWA+deposits-view, never mentions PSP connectors
- Notes: All three refutation avenues fail. ALREADY_BUILT: no multi-PSP connectors — internal/payments is a live but single-provider fiat seam (one PAYMENTS_WEBHOOK_SECRET HMAC webhook, free-text payment_method, no registry migration, no office PSP config); alphacashier/crypto_rail are crypto rails. COVERED_BY_ITEM: P1-1 (DECIDED) says it 'satisfies PAM §22' but its scope is the back-office surface only (withdrawals queue + AWA + deposits view per PROGRESS_LEDGER.md:216); multi-PSP was never surfaced in the P1-1 brief or decision; P2-4's descope note covers on-chain settlement (§17/§26) and mentions the custodial USDT rail only as a premise. NOT_A_REQUIREMENT: §22 is normative; §4 Non-Goals is silent on PSPs. Remedy is documentation, not code: extend the P2-4 descope note (or a sibling note) to formally descope fiat/multi-PSP citing PAM §22 + §26 and Tiangge §2/§19, matching what DECISIONS_NEEDED.md:74-75 already promised — hence P2. Two corrections for the reconciler: (1) 'only rail is alphacashier' is incomplete — the generic internal/payments deposit/withdraw API is wired (internal/http/handlers.go:611) and the note should acknowledge it; (2) the §26 half is weaker standalone since P2-4 already frames §26 integrations as 'option, not mandate' and §36 grades §26 Built — anchor the combined gap on §22.

#### No finance/payments dashboards (deposit/withdrawal/fee/balance aggregates)
- Spec: §23 Reporting, Dashboards, and Exports
- Evidence: spec.md:306-307 (§23 "Dashboards: finance, trading, compliance, payments" requirement bullet) + spec.md:810 (§36 row 15 grades Reporting & export Partial/Fail: "no full reporting/export module"); office app/(dashboard)/dashboard/page.tsx:61-64 loads only markets+volume stats (no deposit/withdrawal/fee/balance aggregates); app/(dashboard)/reports/page.tsx is a guarded redirect stub; reports_handlers.go:20-67 wallet-reconciliation aggregate has no office UI consumer (grep clean)
- Notes: Not built: live /dashboard is trading-ops only (open markets, settlement queue, 24h volume, top movers); /reports is a redirect stub guard-tested to stay inert; the one real backend finance aggregate (GET /api/v1/admin/wallet/reconciliation, reports_handlers.go) has zero UI consumers and no deposit/withdrawal/fee breakdowns. Not covered: P1-6 (DONE) = CSV exports only, self-labeled "Scenario 15-partial"; P1-1 (DECIDED) = withdrawals queue + AWA + deposits view = §22 ops queues, not §23 aggregate dashboards; no GAP/DEFERRED/DESCOPED item touches §23 dashboards. Priority P2 because it is read-only reporting breadth and the payments half has no data source while money paths are flag-gated OFF (alphacashier boot-blocked; Tiangge §2/§19); escalates toward P1 when the P1-1 cashier rail is enabled — recommend sequencing the finance/payments dashboards alongside or immediately after P1-1, reusing the existing wallet-reconciliation aggregate as the first widget.

#### PDF export absent (CSV only)
- Spec: §23 Reporting, Dashboards, and Exports
- Evidence: spec.md:312 ("Exports: CSV/PDF/data-pull") vs PROGRESS_LEDGER.md:262 (P1-6 delivered CSV-only report_exports_handlers.go + /exports page); zero PDF-generation libs in go.mod or any package.json; only gateway PDF hit is the KYC upload MIME allowlist (kyc_files.go:28)
- Notes: All three refutation avenues fail. Not built: no PDF library or generation code in gateway, auth, or office (dead trees included); kyc_files.go PDF references are upload-acceptance, not export. Not covered: no Done/DECIDED/DEFERRED/DESCOPED or GAP item mentions PDF; ledger final summary explicitly says P1-6 shipped comma-separated-value exports. It is a requirement: §23 bullet lists PDF as an export format; §4 Non-Goals does not exclude exports. Priority P2 (breadth) rather than P1: the export capability and underlying data are already available via the RBAC-gated, audited, injection-safe CSV routes, so PDF is an additional presentation format, not a money/ops or compliance blocker.

#### No structured per-field change tracking — audit rows are action-level with free-form JSONB details
- Spec: §24 Audit Logs and Compliance Evidence
- Evidence: gateway/migrations/009_audit_logs.sql:2-13 (audit_logs = action/resource_type/resource_id/details JSONB — no structured old/new field columns); spec.md:317-318 (§24 requirement: "Combines Idefix per-field `modified{who,when}` change tracking with an OMEGA-style Staff Change Log"), reinforced at spec.md:186-187 (§11 lifecycle "with per-field `modified` audit") and spec.md:435 (acceptance scenario Expected Result "per-field audit recorded", Status: Fail)
- Notes: All three refutation avenues fail. (1) ALREADY_BUILT: no. The three audit stores are all action-level: audit_logs (migration 009, details JSONB), provider_ops_audit_log (migration 036, details TEXT), alpha_cashier_audit_events (migration 030). Greps across gateway internal/* and auth internal/* for old_value/new_value/changed_fields/before-after diff structures in audit writes found nothing; the office UI has an audit-logs page (app/(dashboard)/audit-logs/page.tsx) but no per-field change-history/diff view. (2) COVERED_BY_ITEM: no. docs/pam/PROGRESS_LEDGER.md GAP-1..8 are all other topics (GAP-5 = durable auth audit sink, not diffing; P2-3 DSAR touches audit immutability only); no GAP-9+, and zero mentions of "per-field", "Staff Change Log", or §24 in PROGRESS_LEDGER.md or DECISIONS_NEEDED.md. (3) NOT_A_REQUIREMENT: no. §24's sentence is prescriptive requirement text (not an example), it recurs in §11, and the spec's own scenario at line 435 expects "per-field audit recorded" and is marked Fail — the spec itself records it as unmet. Not excluded by §4 non-goals. Priority P2 per the reconciler's reasoning: the bolded §24 invariant ("every sensitive admin action must create an audit log") IS met — migration 036 even DB-enforces append-only — so this is fidelity/structure (a diff contract + Staff Change Log view), not missing coverage. Caveat: the line-435 scenario's Required Evidence ("who/when/reason") is satisfiable at action level, so the scenario alone doesn't force P1; if a regulator requires field-level evidence exports, revisit as P1.

#### No consent capture/versioning system (§25 + §28 combined gap)
- Spec: §25 Admin Operations and Configuration
- Evidence: spec.md §25 line ~327 ("Consents versioning") + §28 lines 349-354 ("Consent capture and versioning (OMEGA Consents)"); code: only match is an OAuth-decline comment at go-platform/services/auth/internal/http/oauth.go:330 plus static legal copy in phoenix-frontend-brand-viegg privacy pages; ledger: zero "consent" hits in PROGRESS_LEDGER.md and DECISIONS_NEEDED.md, and P2-3 (ledger lines 95/212) is scoped to DSAR export/pseudonymization/retention only
- Notes: All three refutation avenues fail. Not built: no consents table in gateway migrations, no handler wiring, no office UI page; the only code matches are an unrelated OAuth comment and static privacy-policy text in the player frontend. Not covered: P2-3's scope text is DSAR export + pseudonymization + retention schedules and never mentions consents; no other ledger/GAP/DECISION item does. Is a requirement: listed in §25's admin-ops catalog and independently mandated by §28; §4 non-goals do not exclude it; §28's "Needs Legal Review" gates policy details (retention periods, erasure carve-outs), not the capability's existence. Recommend attaching it to the P2-3 counsel workstream: schema (consent type, version, text hash, punter id, granted/revoked timestamps, channel), capture endpoints, and an office admin page for versioned consent texts per brand/jurisdiction.

#### No configurable password policy for players or staff
- Spec: §25 Admin Operations and Configuration
- Evidence: spec.md:327 (§25 "Password Policy (player + staff)") and spec.md:188-190 (§11 "with password policy and lockout") vs. only hardcoded constants: auth handlers.go:1706 (minPasswordLength=7, not configurable) and gateway rbac/service.go:14 (MinPasswordLength=8, staff temp passwords only); zero repo hits for password_policy/PASSWORD_POLICY config, no migration, no office UI page; no ledger or DECISIONS_NEEDED item covers it
- Notes: Gap is real but narrower than the reconciler stated. The note "no password-policy code; bcrypt hashing only" is inaccurate: live-wired hardcoded minimum-length validation exists (players: validatePasswordStrength min 7 chars at auth handlers.go:1706-1710, enforced at registration line 1244 and password change line 1169; staff: MinPasswordLength=8 at gateway rbac/service.go:13-14, enforced at lines 121/297, rbac routes registered at gateway internal/http/handlers.go:331-332). What is missing is the §25 CONFIGURABLE policy: no admin config surface, no config store/table/env var, no complexity/expiry/history/lockout parameters. Not covered by any ledger item (P0-1 admin MFA is a staff-only mitigation, already acknowledged; P2-1/P2-4/GAP-1..8 unrelated; DECISIONS_NEEDED has zero password mentions). Requirement confirmed in spec at four places (lines 167, 188-190, 327, 347), not a §4 non-goal. P2 because the security baseline (bcrypt + min length + per-IP login limiter + mandatory staff MFA) already stands; this is admin-config breadth, not an open security hole. Recommend rescoping the gap title to "Configurable password policy + admin surface (player + staff)".

#### Fiat PSP connectors absent (same combined gap as §22 multi-PSP)
- Spec: §26 Integrations, APIs, and Webhooks
- Evidence: internal/payments/db_service.go:21 ("works out-of-the-box without Stripe/Adyen integration") + spec.md:335-337 (§26 requires PSPs/payment connectors) + docs/pam/designs/p2-4-descope.md (descopes only the on-chain stack, never mentions fiat/PSPs)
- Notes: All three refutation avenues fail. (1) Not built: only rail abstraction is CryptoRail (internal/payments/crypto_rail.go:39, sole impl evmUSDCRail) plus the flag-gated internal/alphacashier crypto rail; db_service.go:21 affirmatively documents the absence of Stripe/Adyen; no PSP/payment-method surface in the office App Router. (2) Not covered: P1-1's decided scope is withdrawals queue + AWA engine + deposits view only (DECISIONS_NEEDED.md), still not in the Done list; the P2-4 descope note covers only non-custodial on-chain settlement — it records the custodial-USDT direction but does not formally descope fiat PSP connectors; no GAP item applies. (3) It is a requirement: spec §26 lines 335-337 list PSPs/payment connectors as required integrations and §22 (~line 296) requires multi-PSP deposits; §4 non-goals do not exclude payments. Mitigations that argue P2 not P1: launch is points-only (Tiangge §2/§19 forbid money paths), founder money strategy is crypto-native custodial USDT (launch policy + P2-4 decision), and §36/§37 grade §26 as Built for webhooks/partner-API — so this is breadth/documentation debt, best closed by extending the P2-4-style descope note to formally record the fiat-PSP omission (and gating any future fiat work on it), exactly as the reconciler recommends. Confirms single combined gap with §22 multi-PSP.

#### Config store has no encrypted-value support; at-rest encryption beyond TOTP secrets unaddressed
- Spec: §27 Security Requirements
- Evidence: spec.md:343 ("encryption in transit and at rest (encryptable config values per OMEGA Brand Registry)" — §27 Security Requirements) vs internal/platformconfig/platformconfig.go:52-58 (config_value TEXT NOT NULL, plaintext; no encrypt/secret/mask anywhere in the package or repo). PROGRESS_LEDGER.md:97+247 scope GAP-3 to TOTP secrets in the auth module (AUTH_MFA_ENC_KEY, mfaUpsertPending/mfaGet) only; the DONE P2-1 config store (ledger lines 29-31, 211) shipped without encryption; GAP-8 (line 249) is about fail-closed geo config, not encryption.
- Notes: All three refutation avenues fail. Not built: zero encryption code in gateway (incl. migrations), auth, or office (dashboard) — the only KMS/encrypt grep hits are prose comments in payments/crypto_rail.go. Not covered: GAP-3 is TOTP-only; no ledger item addresses encryptable config values. It is a requirement: §27 Security Requirements, not descoped by §4. Priority P2 rather than P0/P1 because the store currently holds only feature flags (no secret resides in it today) and the ledger rated the strictly-worse GAP-3 (plaintext second factors) P1 — but this gap should escalate to a blocking prerequisite before any secret-bearing config (PSP keys, webhook secrets, tenant OAuth creds via P2-1/GAP-8 follow-ons) lands in platform_config. Suggested build: reuse GAP-3's AES-GCM + KMS-ready key-source seam, add an is_secret column, mask secret values in List and the admin UI.

#### No consent capture/versioning (combined with §25 Consents — one gap)
- Spec: §28 Privacy, Data Retention, and Data Rights
- Evidence: spec.md:351 ("Consent capture and versioning (OMEGA Consents).") + spec.md:327 ("Consents versioning" in §25); zero consent code — only match in go-platform is an OAuth-flow comment at services/auth/internal/http/oauth.go:330, no migrations/tables/office pages; zero "consent" mentions in PROGRESS_LEDGER.md or DECISIONS_NEEDED.md; P2-3 (PROGRESS_LEDGER.md:212, DECISIONS_NEEDED.md:64) covers only DSAR/retention, not consents.
- Notes: All three refutation avenues fail. Not built: only "consent" hit in code is a social-login OAuth comment; office UI hits are .next build artifacts/node_modules only. Not covered: P2-3 addresses §28's DSAR/retention clauses but never mentions consent capture/versioning; no GAP/DECIDED/DEFERRED/DESCOPED item touches it. It is a requirement: stated plainly in §28 and repeated in §25 and the §30-area traceability (spec.md:704); §4 non-goals exclude nothing privacy-related; "Needs Legal Review" gates the erasure details, not the capability. Priority P2 for consistency with the project's own rating of the sibling §28 workstream (P2-3): build versioned consent-definition storage + capture endpoints + §25 admin editor with category placeholders pending counsel. Reconciler is correct that §25:326-327 and §28:351 are one gap — count once.

#### §29 UI-stack prescription (React 18/MUI v5/Idefix Nx) conflicts with the shipped Next.js+AntD office and no spec-amendment/descope note records the delta
- Spec: §29 UI Requirements
- Evidence: spec.md:358 (§29 requires "React 18 / MUI v5 in the Idefix Nx librarized structure") vs office/package.json:40,47,50 (antd ^5.29.3, next 16.2.9, react 19.2.4); spec.md:857-861 (§37 notes "not the Idefix Nx frontend stack" but says deltas "do not change the requirements but reframe Sections 26 and the implementation plan" — §29 never amended); §36 matrix (spec.md:794-816) has no row citing §29; PROGRESS_LEDGER.md and DECISIONS_NEEDED.md contain zero UI-stack items; docs/pam/designs/ holds only p0-7-design.md, p2-1-design.md, p2-4-descope.md
- Notes: All three refutation avenues fail. (1) ALREADY_BUILT: the shipped office is Next.js 16 + Ant Design 5 + React 19 wired to P8 tokens (CLAUDE.md confirms Pages Router + parallel App Router tree), definitively not §29's React 18/MUI v5/Idefix Nx; and the delta note in §37 explicitly disclaims changing requirements and scopes its reframing to §26 + the implementation plan, so no amendment artifact exists for §29. (2) COVERED_BY_ITEM: no DONE/DECIDED/DEFERRED/DESCOPED item in the ledger or decisions file touches the UI stack; the only descope note is P2-4 (on-chain settlement). (3) NOT_A_REQUIREMENT: §29 is a declarative UI Requirements section sourced to "Product Decision"; §4 Non-Goals excludes only the player-facing trading client. P2 because this is spec-hygiene, not compliance/security (P0) or money/ops (P1): the correct fix is a committed one-page spec amendment/descope note following the p2-4-descope.md pattern, re-anchoring §29's stack line to Next.js+AntD-on-P8-tokens; §29's remaining substantive UI requirements are stack-agnostic and separately tracked. Without it, every future §-by-§ reconciliation pass will re-flag the same conflict, and §36 lacks a §29 row so the matrix cannot account for it.

#### No office-wide list-detail-drawer / filter+run+exportable-grid pattern standard; only the /exports page implements the export-grid half
- Spec: §29 UI Requirements
- Evidence: spec.md:357-359 (§29 requirement); no ledger/decision item cites §29 (P1-6 at PROGRESS_LEDGER.md:262 cites §20/§21/§23 only); zero Drawer matches in packages/office/app/(dashboard); shared DataTable exists (app/components/shared/DataTable.tsx:27-34, no filter/export props) but /exports/page.tsx:13-27 is static CSV links, not a filter+run grid
- Notes: Gap is real but narrower than claimed. (1) The 'ad-hoc per feature' characterization is overstated: a shared DataTable (sort/pagination/onRowClick) is used by ~10 dashboard pages, so the list-grid half has a de facto office-wide standard. (2) What is genuinely missing: any drawer/detail-surface convention (details vary across [id] pages, modals, inline), a shared filter-bar (audit-logs hand-rolls filterInputClassName locally at page.tsx:45-48), and export capability on grids (only 2 static CSV links at /exports). (3) The literal §29 form is attenuated by PD-2 (revised) at spec.md:84-86, which makes the shipped Next.js/antd stack canonical and demotes Idefix Nx/MUI v5 ('drawer' is Idefix-reference UX); the §36 Progress Matrix also has no row tracking §29. Recommend scoping the gap as: extend the existing DataTable standard with a shared filter-bar + CSV-export affordance and pick one detail-surface convention — not a ground-up pattern build. P2: pure UI consistency/breadth, no compliance or money-path impact.

#### No accessibility (a11y) or responsive-layout verification exists for the back office
- Spec: §29 UI Requirements
- Evidence: spec.md:360 ("Accessibility and responsive layout." in §29); zero a11y/responsive/wcag/aria hits in docs/pam/PROGRESS_LEDGER.md and DECISIONS_NEEDED.md; only responsive test is dead-suite (e2e/backoffice/dashboard.spec.ts:161 under playwright.config.ts, which playwright.prediction.config.ts:5-6 labels "legacy sportsbook ... specs target removed surfaces"; its responsive.spec.ts reference doesn't exist on disk); live suite playwright.prediction.config.ts runs Desktop Chrome only, no axe/viewport assertions; no axe/pa11y/lighthouse in any first-party package.json
- Notes: All three refutation avenues fail. (1) Not built: no a11y test tooling in first-party deps; the one responsive Playwright test lives in the dead legacy sportsbook suite targeting removed surfaces, and the legacy config's player-app responsive.spec.ts doesn't even exist; the live prediction suite (e2e/prediction) is functional-only, single desktop viewport. (2) Not covered: no ledger Done/DECISION ROUND/GAP/DEFERRED/DESCOPED item touches a11y or responsive layout. (3) Genuinely required: spec §29 states it declaratively (Product Decision source); §4 non-goals don't exclude it; §36 Progress Matrix has no row mapping §29, corroborating "unaccounted." Priority P2: internal back-office quality/breadth item — no compliance mandate cited, no money-path impact. Reconciler's caveat is accurate: DESIGN.md AA-contrast applies to the player-facing design system, not the office.

#### Seed data has zero compliance/money breadth — no KYC/risk/RG-state trader variety, no brands/jurisdictions, no payment methods, no pending withdrawals (verified: grep of seed_prediction.sql returns 0 for all four)
- Spec: §31 Seed and Demo Data Requirements
- Evidence: spec.md §31 (normative, 'Product Decision' source) vs. cmd/seed/main.go:109-111 loading only seed-data/seed_prediction.sql, whose punters carry only status+country_code (seed_prediction.sql:56; 001_punters.sql has no KYC/risk/RG columns); grep of all cmd/seed/*.go for kyc|jurisdiction|brand|tenant|payment|risk|withdraw = 0 matches; PROGRESS_LEDGER.md and DECISIONS_NEEDED.md contain no seed-data item.
- Notes: One wording correction, not a refutation: seed-data/seed_backoffice_dashboard.sql:76-82 does synthesize 2 pending-withdrawal payment_transactions (n=15, n=30), but the file is wired to nothing (not loaded by cmd/seed, any Makefile target, or scripts) and the rows are dashboard synthetics, not queue items. The cashier-api withdrawal_intent seed is the legacy non-custodial TS package being dropped, out of PAM scope. Partial coverage nuance: P2-1 core DEFERRED (single-tenant, ledger line 94) relaxes only the multi-brand breadth; jurisdictions/KYC/risk/RG states/payment methods/pending withdrawals remain unaccounted. Reconciler's sequencing is right — work this after P0-4/P0-5/P0-6/P1-1 land, as a seed-coverage item keyed to the 20 §32 scenarios, since the schema/state machines needed to seed most of this breadth do not exist yet. P2 per rubric: non-prod demo breadth, not a compliance control or money path.

#### pam-open-questions.md is stale — it predates the 2026-07-02 DECISION ROUND and records none of the answers (verified: doc has no status/answer annotations)
- Spec: §34 Open Questions
- Evidence: docs/pam/pam-open-questions.md (single commit 54139c24, never updated; lines 8-9, 11-13, 28-29, 30-31 still pose questions decided in the 2026-07-02 DECISION ROUND) vs docs/pam/DECISIONS_NEEDED.md P0-4/P2-3/P2-4 entries and PROGRESS_LEDGER.md lines 79-99; requirement at docs/pam/spec.md:773 (§34 "Tracked in /docs/pam-open-questions.md") reinforced by spec.md:767-768 (§33 "Product Decisions / Legal Assumptions are recorded in ... /docs/pam-open-questions.md")
- Notes: All three refutations fail. (1) Doc verified annotation-free: only "answered" section is investigation-era (lines 84-101); git log shows one commit (54139c24, the BOOT-1 landing). (2) No ledger/decisions item covers back-filling this doc — grep hits only BOOT-1 presence lines 123/222; pass-B back-fill note (ledger line 176) covers spec-citation back-fill only. (3) Spec §33/§34 make it a real requirement; §33's claim that product decisions are recorded in this doc is now false for P0-4/P2-3/P2-4. Mitigants keeping it P2: answers are durably recorded in DECISIONS_NEEDED.md + ledger in the same directory; no code/compliance behavior depends on the file. Fix is the reconciler's proposed back-fill/cross-link pass, annotating decided items (microstructure=CLOB built; sanctions=P0-4 OpenSanctions/yente; retention-vs-erasure=P2-3 pseudonymization; wallet/crypto=P2-4 descope + custodial USDT) while preserving genuinely-open counsel items (regulatory regime + thresholds P0-5/P0-6; retention schedule P2-3).

#### Per-player templated-message send + communication-history log missing (template store exists but nothing sends to a player or records a sent-content history)
- Spec: §32 Acceptance Scenarios — Scenario 12: Player Communication and Notes
- Evidence: gateway internal/http/prediction_admin_handlers.go:97-260 (punter subtree = status/notes/settlements/wallet only, no send action); internal/notify/notify.go:106-110 (ops-only NOTIFY_RESOLUTION_TO recipients); no sent-communications table in gateway/migrations/*.sql; PunterProfile.tsx:6 (tabs overview|trades|wallet|kyc|limits, no comms); spec.md:273-279 (§20 requires full sent-content history) and spec.md:590-606 (Scenario 12, status Fail); PROGRESS_LEDGER.md:262 (P1-6 delivered template store/bonus UI/CSV only)
- Notes: All three refutation avenues fail. (1) Not built: notes (step 1) and the template registry (precondition) exist, but there is no per-player send route, no sent-content table, and no history view in API or office UI. (2) Not covered: ledger P1-6 cites §32 Scenario 12 in its spec mapping, but its artifacts satisfy only the template-registry precondition — the citation overstates coverage; no GAP/DECIDED/DEFERRED/DESCOPED item in PROGRESS_LEDGER.md or DECISIONS_NEEDED.md owns the send action or comm-history log (P2-2's fail-closed dispatch note covers campaigns only). (3) It is a requirement: spec §20 normatively requires full sent-content history; §4 non-goals exclude only the player-facing trading client; §36 matrix row is Partial/Fail. Priority P2 because launch mode (Tiangge) forbids user-facing sends, so the launch-safe build is a fail-closed send action + sent_communications table + history tab (log-only sink); escalates to P1 when real sends open. Caveat for the reconciler: the P1-6 ledger entry's unqualified "Scenario 12" citation should be corrected to partial when this gap item is filed.

#### Office CRM UI for tags/segments/campaigns missing — P2-2 is API-only; /campaigns route is a guarded redirect stub
- Spec: §32 Acceptance Scenarios — Scenario 13: Segmentation and CRM
- Evidence: spec.md:160 (§9 nav item 9 "CRM (segments/tags, campaigns, ...)" required back-office section) + spec.md:607-624 (Scenario 13, Actor Marketing/CRM, Status: Fail); office app/(dashboard)/campaigns/page.tsx:1-5 is redirect("/dashboard") pinned by tests/app-router-legacy-routes.test.ts:101; gateway internal/http/handlers.go:691-698 wires segmentation_admin_handlers.go routes (/api/v1/admin/segments/{tags,query,campaigns}) with zero office consumers (grep of office source finds no /api/v1/admin/segments call); PROGRESS_LEDGER.md:138 scopes P2-2 DONE to API, :145 planned "office CRM page" superseded by DECISION ROUND build queue (lines 106-108) which omits it
- Notes: All three refutation avenues fail. API is live and RBAC-gated but UI-less; the only campaigns UI (bonuses page) drives the old bonus-engine /api/v1/admin/campaigns, not P2-2's /api/v1/admin/segments/campaigns; no tag/segment UI exists anywhere in the office package. Ledger never claims the UI (not a claim-audit failure) and no DONE/DECIDED/DEFERRED/DESCOPED item owns it — unowned residue, exactly as the reconciler stated. P2 is correct: marketing/CRM breadth, no compliance or money exposure (campaign dispatch is already fail-closed in launch mode). Build note: the guard test pins /campaigns as a redirect stub, so the new page must use a fresh route (e.g. /crm or /segments), mirroring how P1-6 used /exports instead of /reports.

#### PDF export format missing from the reporting/exports module (CSV only)
- Spec: §32 Acceptance Scenarios — Scenario 15: Reporting and Export
- Evidence: spec.md:654 ('2. Export CSV and PDF', Scenario 15, Status: Fail) + spec.md:312 (§23 'Exports: CSV/PDF/data-pull'); PROGRESS_LEDGER.md:262 marks P1-6 as '§32 Scenarios 12, 14, 15-partial' (CSV slice only); gateway internal/http/report_exports_handlers.go:51 registers CSV-only routes ('admin report-export (CSV) routes registered'), no PDF renderer anywhere in gateway/auth/office
- Notes: All three refutation avenues fail. (1) Not built: report_exports_handlers.go serves only text/csv; the only PDF code is KYC upload mime handling, unrelated to report export. (2) Not covered: zero 'pdf' hits in PROGRESS_LEDGER.md or DECISIONS_NEEDED.md; P1-6 itself concedes Scenario 15 is partial; DEFERRED/DESCOPED items are P2-1 and P2-4 only. (3) It is a requirement: §23 body text and §32 Scenario 15 both mandate PDF; §4 Non-Goals has no export exclusion. P2 because CSV already provides the functional export/evidence path — PDF is bounded format breadth (server-side render over the same aggregate queries), not a compliance or money gap.

#### General support/dispute case-management workflow missing beyond the binary market-resolution dispute queue (no case states/assignment/handoff/linked-entity case history)
- Spec: §32 Acceptance Scenarios — Scenario 19: Support / Dispute Case Workflow
- Evidence: spec.md:719-737 (Scenario 19, Status Fail) + spec.md:266-271 (§19 requires typed cases w/ status/assignee/SLA/linked entities) + spec.md:814 (§36 row: "general support cases Missing"); only case code is surveillance-only (internal/surveillance/surveillance.go:67-78) and binary market disputes (migrations/023_resolution_proposals_disputes.sql:41-47); no ledger item (P0-1..P2-4, GAP-1..8) or DECISIONS_NEEDED entry mentions support cases — P1-4 (ledger line 263) covers only the §19 integrity slice, P0-5 (line 90) only aml_cases
- Notes: All three refutation avenues fail. Code: surveillance_cases has open/investigating/closed_* + priority + opened_by but no case type, assignee, SLA, support intake, or handoff; prediction_disputes is open/upheld/rejected/withdrawn tied to market resolution; office UI has only disputes/ (binary uphold-reject queue) and surveillance/ pages, no support-case page; no ticket/support table in any migration. Ledger: P1-4 cites §19 Case Management but delivered only the integrity slice; P0-5 (queued) is AML cases only; nothing covers Scenario 19. Spec: full acceptance scenario with Status Fail, backed by normative §19; not excluded by §4 Non-Goals. Priority P2 (breadth) rather than P1 because the money-control half of Scenario 19 already exists (dispute adjudication, audited adjustments, P0-6 maker-checker); missing is the workflow shell — typed intake, states, assignment/handoff, linked-entity case history. Natural build: generalize the surveillance case store (add case_type, assignee, polymorphic linked entities) per §19's typed-case requirement.
