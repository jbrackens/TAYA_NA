# PAM Modernization — Progress Ledger

## FINAL SESSION SUMMARY (2026-07-02)

The autonomous loop drove the prediction-market back office through the entire
P0, P1, and P2 backlog and every discovered gap item, reaching the point where
every backlog item is either completed with test evidence or formally blocked
on a human decision; the loop cannot declare full termination only because the
PAM specification file it must reconcile against was never committed to the
repository.

Outcome in one sentence: all of the buildable compliance-and-security work is
done, committed, and verified, and everything that remains is a decision the
human must make (documented in `docs/pam/DECISIONS_NEEDED.md`) or the missing
specification file (item BOOT-1).

Items completed with evidence. In the P0 tier the loop delivered enforced
administrator multi-factor authentication with single-use time-based codes and
denial of administrator sign-in through the social-login path (backlog item
P0-1), fail-closed Know-Your-Customer behavior outside development (item P0-2),
and a Know-Your-Customer review user interface with real document-file storage
plus a customer-profile Know-Your-Customer tab (item P0-3). In the P1 tier it
delivered a manual balance-adjustment user interface (item P1-2), a responsible-
gambling limits and self-exclusion profile tab (item P1-3), a market-integrity
surveillance subsystem covering wash-trading, spoofing, collusion, and
duplicate-account detection with an alerts-and-cases workflow (items P1-4 and
P1-5), and a bonus administration user interface with a database-backed
notification-template store and comma-separated-value export reports (item
P1-6). In the P2 tier it delivered a database-backed feature-flag and
configuration store, a tenant and brand directory administration surface, and a
segmentation and customer-relationship-management subsystem with tags, a query
builder, and campaigns that keep their dispatch action fail-closed for launch
mode (items P2-1 peripherals and P2-2). It then hardened five discovered gap
items: fail-closed Know-Your-Customer, responsible-gambling, and geolocation
compliance fallbacks (item GAP-1), single-use replay protection for time-based
one-time codes (item GAP-2), fail-closed handling of unrecognized deployment
environments (item GAP-4), a durable database-backed authentication audit trail
(item GAP-5), and role-based access control with audit logging on the bonus and
campaign administration routes (item GAP-7).

Commits and tests. The work is forty-three commits on the branch named
`pam/p0-modernization`, of which thirty are feature or fix commits, ending at
commit `24ea88b3`; nothing has been pushed to any remote. Six database
migrations were added, numbered fifty through fifty-five. The gateway service
builds cleanly and its full race-enabled test suite passes across twenty-nine
packages; the authentication service builds cleanly and its race-enabled test
suite passes; the back-office web application's unit-test suite passes at one
hundred thirty-one of one hundred thirty-one, and the back-office end-to-end
Playwright specifications pass for authentication, Know-Your-Customer review,
balance adjustment, surveillance, bonuses, reports, and tenants.

Coverage of new logic. Every new store has table-driven unit tests plus an
opt-in live-Postgres test guarded by a dedicated data-source-name environment
variable; every new administrative route has a test asserting that a non-
privileged caller is refused; every database migration was applied up and then
down and then up again on a disposable scratch database; and five independent
fresh-context verification passes reviewed the accumulated changes, each with a
guardrail lens, a claim-audit lens, and an adversarial lens, with every
resulting finding either fixed in a follow-up commit or recorded as a gap item.

Exact command and state needed to resume. The single action that unblocks the
loop's formal termination is to place the PAM specification documents into the
repository at `REPO_ROOT/docs/pam/` and commit them; the precise copy command is
written at the top of `docs/pam/DECISIONS_NEEDED.md`. Once the file
`docs/pam/spec.md` exists and opens with the heading
`# Enterprise Prediction Market PAM / Back Office Spec`, the next scheduled loop
firing will run termination reconciliation pass B, add specification-section
citations to the record, and either terminate or append new gap items for any
specification scenario not yet satisfied. The remaining blocked items — the
sanctions and anti-money-laundering vendor and regime choices, the maker-checker
approval thresholds, the double-entry ledger migration, the launch-mode cashier
decision, the tenant query-scoping activation, the data-retention policy, the
on-chain settlement decision, and the three smaller flagged gap items — are each
consolidated with options and a recommendation in `docs/pam/DECISIONS_NEEDED.md`
and require only a human answer to proceed.

---

## DECISION ROUND (2026-07-02 evening — human present, all blocked questions answered)

The human answered every open decision interactively (AskUserQuestion round,
one item at a time). This supersedes the BLOCKED state of each item below;
the briefs remain for the record. Evidence: BOOT-1 docs landed at commit
`54139c24`.

| Item | Decision (2026-07-02) | New state |
|---|---|---|
| BOOT-1 | Human authorized copying the five PAM docs from the outer tree; landed + committed `54139c24`. spec.md verified: correct title, §36 @786, §37 @825. | **DONE** |
| P0-4 | OpenSanctions/yente, self-hosted; enforcement at onboarding now (money paths when they open); seam shaped like `alphacashier/screening.go`. Upgrade to commercial when rails open. | **DECIDED → buildable** |
| P0-5 | Regime-agnostic AML plumbing: wallet/ledger event stream → `aml_alerts` → `aml_cases` → disposition/SAR-export workflow; rules load as data when counsel names the regime. | **DECIDED → buildable** |
| P0-6 | Threshold-based maker-checker: manual adjustments ≥ 10,000 cents ($100) need a second approver; settlement always four-eyes. Settlement half = protected core → build under human diff review. | **DECIDED → buildable (adjustments half autonomous; settlement half review-gated)** |
| P0-7 | Double-entry ledger design APPROVED as written (`designs/p0-7-design.md`). Build phase-by-phase; every phase diff gets human review before merge (protected core). Phase 1 = shadow-write (old path stays authoritative). | **APPROVED → review-gated phased build** |
| P1-1 | Build the cashier back office now: withdrawals queue + AWA rules engine, flag-gated OFF, zero launch nav, unreachability test. AWA thresholds default to the P0-6 class ($100), runtime-configurable data. | **DECIDED → buildable** |
| P2-1 core | DEFERRED — single-tenant at launch; dormant `tenant_id` column stays. Revisit when a second brand/tenant is planned. | **DEFERRED (recorded, not buildable)** |
| P2-3 | Build DSAR export + pseudonymization pass now (PII redacted, audit/ledger rows intact under legal-obligation basis); retention periods ship as config placeholders pending counsel. | **DECIDED → buildable** |
| P2-4 | FORMALLY DESCOPED — settlement stays off-chain; money via custodial USDT rail. Descope note committed: `docs/pam/designs/p2-4-descope.md` (`1cced309`) citing PAM §17/§26 + Tiangge §2/§19; resolves ADR-0003/0004. | **DESCOPED — DONE** |
| GAP-3 | App-layer AES-GCM via `AUTH_MFA_ENC_KEY` now; fail-closed if key absent in deployed envs; KMS-ready key-source seam for later. | **DECIDED → buildable** |
| GAP-6 | Capture IP + user-agent at auth events into a new `punter_signals` table; NO client-side fingerprint SDK. Feeds P1-5 dup-detection. | **DECIDED → buildable** |
| GAP-8 | Runtime TIGHTENING only: admins can remove countries from the geo allowlist at runtime (audited); adding a country stays env+redeploy+boot-validation. | **DECIDED → buildable** |

**New build queue (P0→P1→P2 order):** pass B spec reconciliation first (now
unblocked), then P0-4 → P0-5 → P0-6 → P0-7 phase 1 → P1-1 → GAP-3 → P2-3 →
GAP-6 → GAP-8 → P2-4 descope note. Termination pass A is FALSE again (items
moved from BLOCKED to buildable); the loop resumes normal iterations.

## Bootstrap (session 2026-07-02)

### REPO_ROOT
- **REPO_ROOT = `/Users/john/Sandbox/Taya_NA_Predict/Taya_Na_Predict`** (VERIFIED 2026-07-02)
  - Evidence: `git rev-parse --show-toplevel` inside the nested tree returns it; branch `main` at `a53155c3`; the outer tree `/Users/john/Sandbox/Taya_NA_Predict/` returns `fatal: not a git repository`.
  - The outer tree is radioactive per bootstrap rule — not read from or written to, with two disclosed metadata-only exceptions made **before/for** the pinning decision: (1) `ls` of the outer root to identify candidates, (2) `ls` of `outer/docs/` to locate the missing PAM docs for the BOOT-1 decision brief. No file contents were read.
- **Active working tree: `/Users/john/Sandbox/Taya_NA_Predict/pam-worktree`, branch `pam/p0-modernization`** (VERIFIED via `git worktree list` run inside REPO_ROOT)
  - This is a registered linked worktree of REPO_ROOT (same repository, shared object store), NOT the radioactive duplicate. It already carried prior-session PAM work: commit `6914421c` "fix(security): replace fake 2FA toggle with enforced TOTP MFA [P0-1]" (2026-06-27).
  - Interpretation recorded: the prompt's "never touch the other tree" rule targets the outer duplicate *source tree*; a git worktree of the chosen repo is part of the chosen repo. All loop work happens here on branch `pam/p0-modernization`. Nothing is pushed.
  - `main` was merged into `pam/p0-modernization` this session to bring the branch current (main had moved: Tiangge RC spec, auth P1-05 hardening, deploy fixes). Two conflicts in `services/auth/internal/http/handlers.go` resolved by keeping BOTH sides: main's per-IP login limiter + helpers AND P0-1's 3-arg `Login(username, password, otp)`. Auth module `go build` clean, `go test -race ./...` **ok (125.7s)** after resolution.

### Source-of-truth docs (Phase 0.2)
- **UPDATE 2026-07-02 evening: BOOT-1 RESOLVED** — all five docs landed at commit `54139c24` under human authorization (see DECISION ROUND). `spec.md` verified: opens `# Enterprise Prediction Market PAM / Back Office Spec`, `## 36. Progress Matrix` at line 786, `## 37. Reconciliation` at line 825. Historical state below kept for the record.
- `docs/pam/spec.md` — ~~MISSING~~ **PRESENT** (`54139c24`)
- `docs/pam/taya-gap-analysis.md` — ~~MISSING~~ **PRESENT**
- `docs/pam/pam-implementation-plan.md` — ~~MISSING~~ **PRESENT**
- `docs/pam/pam-domain-model.md` — ~~MISSING~~ **PRESENT**
- `docs/pam/pam-open-questions.md` — ~~MISSING~~ **PRESENT**
- Evidence: `docs/pam/` does not exist in the main checkout, the pam-worktree, or **anywhere in git history on any branch** (`git log --all --oneline -- docs/pam` is empty).
- Tiangge launch spec — **VERIFIED** at `REPO_ROOT/spec.md` (512,084 bytes; first line `# Tiangge Spec`; tracked, last touched by commit `7069c7c9` "feat: prepare Tiangge parity RC release"). Note: it entered main *after* the pam branch point, which is one reason the merge above was needed.
- Consequence while BOOT-1 is open: backlog items are implemented from their self-contained inline descriptions + the code itself; PAM-spec §-citations are recorded as "citation pending BOOT-1 unblock"; Termination pass B (spec reconciliation) cannot run and is BLOCKED on BOOT-1.

## Baseline
- auth module: go build OK | go test -race OK (125.7s) — after merge resolution, 2026-07-02
- gateway module: go build OK | go test -race OK (all packages `ok`, exit 0), 2026-07-02
- talon-backoffice office: production build OK (`next build`, 44.35s, exit 0), 2026-07-02
- date: 2026-07-02
- Worktree setup gotcha: a fresh checkout needs `yarn install --frozen-lockfile` at `talon-backoffice/` AND `yarn lerna run build --scope @phoenix-ui/utils --scope @phoenix-ui/api-client --include-dependencies` before any package build — `@phoenix-ui/utils` resolves to `dist/index.js`, which only exists after tsc (same order CI uses in `.github/workflows/frontend-build.yml`).

## Status summary (FINAL, 2026-07-02 — HEAD 24ea88b3, 43 commits since main)
- **P0: 3/7 DONE** (P0-1, P0-2, P0-3) · P0-4/5/6/7 BLOCKED (briefs; P0-7 design note).
- **P1: 5/6 DONE** (P1-2..P1-6) · P1-1 BLOCKED (launch-safe brief).
- **P2: DONE where buildable** — P2-1 peripherals DONE (feature-flag store, tenant admin), P2-1 core BLOCKED (design note), P2-2 DONE (tags + query builder + campaigns); P2-3/P2-4 BLOCKED.
- **GAP-1..8: all DONE or BLOCKED** — GAP-1 (KYC/RG/geo fail-closed) DONE, GAP-2 (TOTP single-use) DONE, GAP-4 (env fail-closed) DONE, GAP-5 (durable auth audit) DONE, GAP-7 (bonus RBAC) DONE; GAP-3/6/8 BLOCKED with briefs.
- Verifications #1-#5 all done; every finding fixed (last: verification #5 major geo/KYC/RG exact-match → `24ea88b3`).
- Green at HEAD: gateway `go build` + `go test -race` 29 pkgs; auth build + `go test -race`; office vitest 131/131; Playwright backoffice specs green.
- ~~TERMINATION: pass A TRUE, pass B BLOCKED on BOOT-1.~~ **SUPERSEDED 2026-07-02 evening (see DECISION ROUND):** BOOT-1 resolved (`54139c24`); all ⚑ decisions answered. Pass A is FALSE again — P0-4, P0-5, P0-6, P0-7 (review-gated), P1-1, P2-3, GAP-3, GAP-6, GAP-8 are now buildable; P2-1 deferred; P2-4 descoped (note pending). Pass B spec reconciliation runs NOW, then the loop works the new queue.

### Resume plan (next scheduled firing picks up here)
1. P2-2 slice 3 — campaigns: a campaign targets a saved segment query and dispatches an action (notification via the template store, or a bonus grant via the bonus engine); RBAC `segments:write`, audited, launch-safe (no user-facing send in launch mode). Then an office CRM page for tags/query/campaigns.
2. GAP-2..8 disposition — each becomes a small build or a full BLOCKED brief:
   Remaining GAP builds (not blocked): GAP-2 TOTP replay protection (last-used-timestep column, auth module), GAP-4 env-allowlist inversion (repo-wide, careful), GAP-5 durable auth audit sink, GAP-7 bonus-admin RBAC retrofit. GAP-3/GAP-6/GAP-8 are now BLOCKED with briefs (⚑ decisions above).
3. Fresh-context verification #4 after 3 more completed items.
4. When buildable backlog is exhausted, write DECISIONS_NEEDED.md and the final summary; termination pass B stays BLOCKED on BOOT-1.

### P1-1 note
- **P1-1 (Cashier/deposits/withdrawals) is ⚑ launch-safe-blocked**: PAM §22 requires it but Tiangge §2/§19 forbid user money exposure. The crypto rail (`internal/alphacashier`) is already flag-gated OFF and boot-refused in prod; what remains (withdrawals queue + AWA rules engine back office) awaits the human's build-now-vs-defer call (brief below).

## Fresh-context verification #5 (2026-07-02, after the GAP hardening)
3 lenses over `1c79bea8..HEAD` (GAP-7 already in-range as of its own commit; GAP-2, GAP-5, GAP-4, GAP-1-geo). Guardrail+build+test lens: PASS (no protected-core; gateway + auth build; suites green). TOTP lens: single-use is SOUND — no replay path; concurrent replay closed by the atomic `UPDATE ... WHERE last_used_step < step` (DB) and the mutex on the stored in-memory record; all error paths fail closed; MFAActivate/MFADisable cannot be leveraged to replay a login code. One accepted minor: a client whose clock LEADS by one step burns the next window's code — correct single-use behavior, ~30s wait, not weakenable. Boot/geo/audit lens: found **[major]** the KYC/RG/geo fallback selectors used exact `production||staging` match while GAP-4 boots non-canonical deployed values (`prod`, `preprod`, ...) as deployed — those would get APPROVING mocks. FIXED commit `24ea88b3` (all three selectors now use the GAP-4 known-dev allowlist via `deployedEnvironment`; tests assert prod/preprod/prd/live/production2 fail closed across KYC, RG, geo). Range note: GAP-7's migration 055 is one commit before the chosen range start — it is committed (`dd529cf6`) and tested, not missing.

## Fresh-context verification #2 (2026-07-02, after P1-2/P1-3/P1-4)
3 lenses over `618b39fa..HEAD`. Guardrail/RBAC/audit/migration/launch-safety all PASS (no prediction/wallet/settlement file touched; every admin route RBAC-gated; every mutation audited; migration 051 goose-splittable with working down; office pages back-office only). Claim audit PASS (all 6 commits + files + tests verified, `go build` + `go test -race` green). Findings fixed in commit `e7302348`:
- [minor] RG per-subject read unaudited → added `rg.subject_viewed` audit (parity with KYC).
- [minor] SpoofingDetector required strictly zero fill → relaxed to <10% filled so a token-fill spoof is still caught.
- [minor] UpdateCaseStatus had no transition guard → closed cases are now terminal (409 ErrCaseClosed); live test added.
- [minor x2] gofmt hygiene (import order, struct alignment) → fixed.
Adversarial lens confirmed the detector SQL is sound (wash excludes AMM/issuance via NULL seller_id; collusion excludes self-trades and de-dupes the pair; InsertAlert idempotent; OpenCase can't create empty/relink dismissed).

## Fresh-context verification #3 (2026-07-02, after P1-5/P1-6)
3 lenses over `e7302348..HEAD`. Guardrails/RBAC/audit/migration/launch-safety all PASS (no protected-core touched; new routes RBAC-gated + audited; migration 052 splittable; /reports still a redirect; CSV cells injection-safe). Claim audit PASS (all commits/files/tests verified, build + `go test -race` green). Two minor findings:
- [minor] RenderOrFallback could emit an empty subject if an operator authored a template whose placeholder resolves empty → FIXED commit `e8c6614d` (empty-render → fallback; live test added).
- [minor] bonus admin routes use coarse `role==admin` gate, not RBAC, and are unaudited; my userId addition marginally widened the read → GAP-7 (pre-existing whole-surface gap; not partially patched).

## Fresh-context verification #4 (2026-07-02, after P2-1 peripherals + P2-2)
3 lenses over `e8c6614d..HEAD`. Guardrails lens: **zero findings** (no protected-core; every admin route RBAC-gated; every mutation audited; migrations 053/054 splittable with working downs; campaign execute fail-closed 403 when flag off, 501 when on, no send wired). Claim audit PASS (10 commits + files + tests verified; query builder fully parameterized; nil config store degrades to always-false gate). Two minor findings, both FIXED:
- [minor] tenant create validated `displayName` but not the slug `id` against launch-prohibited copy → FIXED (slug now runs through `validateLaunchFacingReason`).
- [minor] the ledger had verification #2/#3 + In-progress sections triplicated by copy-append → FIXED (deduplicated to one each).

## In progress
- **[P0-5] AML transaction monitoring — slices 1/3 `77007bb9` + 2/3 `5797b1a0` DONE; slice 3/3 next** (2026-07-03). Decision (2026-07-02): regime-agnostic plumbing — rules load as DATA.
  - Slice 2 evidence: NEW `internal/aml/scanner.go` — `Scanner{store, ledgerDB, interval, caseThresholdPoints}` (mirrors the prediction workers: New/Run(ctx)/tick). `ScanOnce` reads a watermark (`aml_scan_state`), SELECTs `wallet_ledger` rows id>watermark READ-ONLY, `classifyMoneyFlow` (excludes `prediction_*` trading + non-`real` fund; deposit/withdrawal by reason prefix; else adjustment), builds MoneyEvent + per-kind `SubjectWindow` closures (count/sum over the ledger; `kindLedgerPredicate` is the SQL mirror of the classifier), runs `Evaluate`, InsertAlert, auto-opens a case when the subject's open risk-points ≥ threshold (Scenario 5), advances the watermark idempotently. Store gained `Watermark`/`SetWatermark` (monotonic) + `aml_scan_state`. NO wallet writes, NO wallet import (shared *sql.DB, surveillance precedent). Tests: `scanner_test.go` (classifier table incl. trading/bonus exclusion + case-priority), `scanner_live_test.go` (seeds wallet_ledger directly: real deposit fires threshold+aggregate, trading+bonus ignored, 110pts auto-opens a case, re-scan examines 0 new rows). Full gateway `go test -race` exit 0 (30 pkgs, real exit checked); protected-core-clean.
  - **Bug caught in slice 2** (fixed before commit): the window queries built placeholders with `fmt.Sprintf`, but the LIKE patterns contain `%` → format-verb collision mangled the SQL and the swallowed error hid it (aggregate rule saw sum=0). Fixed to fixed `$2`/`$3` placeholders (subject is always `$1`) and now logs the query error instead of swallowing. Lesson recorded.
  - Slice 3 (next): main-wire the scanner (always-on; harmless with zero rules) + admin API (RBAC `compliance:read`/`:write`) for rules/alerts/cases + office AML page + SAR CSV export (like P1-6) + audit on dispositions.
  - Slice 1 evidence: NEW `internal/aml` package — `internal/aml/aml.go` (Store: `aml_rules`/`aml_alerts`/`aml_cases` store-owned ensureSchema mirroring surveillance; UpsertRule/EnabledRules, InsertAlert idempotent-by-dedupe, OpenAlertRiskPoints, OpenCase never-empty, UpdateCaseStatus resolution-required + terminal-guard + SAR reference, GetCase/ListCases/ListAlerts/DismissAlert) + `internal/aml/engine.go` (DB-free `Evaluate`: amount_threshold/velocity/aggregate rules over a `MoneyEvent`+`SubjectWindow`; zero rules → zero alerts; malformed params skipped not panicked; JSON float64 params tolerated). Tests: `engine_test.go` (8 table-driven cases incl. no-rules/disabled/malformed/JSON-number), `aml_live_test.go` (full rule-as-data → evaluate → alert → risk-accrual → case → SAR-file lifecycle with resolution + terminal guards, green on scratch PG16). Full gateway `go test -race` exit 0 (30 pkgs); no protected-core.
  - Slice 2 (next): ingestion — a periodic scanner reads recent `wallet_ledger` money-movement rows READ-ONLY (like the surveillance detectors), builds `MoneyEvent`+`SubjectWindow` from the ledger, runs `Evaluate`, persists alerts, and auto-opens a case when a subject's open risk-points cross a configurable threshold. No wallet writes (protected core untouched). Slice 3: admin API (RBAC `compliance:read`/`:write`) for rules/alerts/cases + office AML page + SAR CSV export (like P1-6 exports) + audit.

## Fresh-context verification #6 (2026-07-03, after GAP-9, GAP-10, P0-4; range c7a504e1..e1fbefc8)
Attempt 1 (workflow wf_fe54844b, 3 Fable-5 lenses) FAILED on Fable-5 credit exhaustion mid-run — recorded honestly, NOT counted as a pass (`confirmed:0` there meant "never completed", not "clean"). Attempt 2 on Opus 4.8: guardrail lens run mechanically (gateway+auth build clean; targeted `go test -race` green; range protected-core-clean); claims-audit + adversarial lenses run as Opus-4.8 agents.
- **Claims-audit lens: PASS** — no discrepancies; every ledger claim (GAP-9 audit assertion, GAP-10 fail-closed-by-env table, P0-4 tipping-off leak checks + `{accepted:true}` shape, live-PG gate matrix) backed by tests that actually assert it. Note: the P0-4 gate matrix is covered by opt-in live-PG tests (honestly disclosed).
- **Adversarial lens: 3 MAJORs found, all FIXED this iteration:**
  - **[MAJOR, GAP-10] Refresh bypass** — `Refresh` re-minted sessions without the restriction check; a player restricted AFTER login kept refreshing for the life of the rotating chain. FIXED: the shared `evaluatePlayerRestriction` gate now runs in `Refresh` (denied → terminate the chain + 403; outage → fail-closed 500 in deployed envs). Tests: `TestRefreshRestrictedPlayerDenied` (+chain-termination), `TestRefreshFailsClosedByEnvOnLookupError`, `TestRefreshAllowedForUnrestrictedPlayer`.
  - **[MAJOR, GAP-10] OAuth bypass** — `oauthSessionGate` blocked only admins; a self-excluded player could sign in via Google/Discord and get a session. FIXED: the same gate now runs in `oauthSessionGate`. Test: `TestOAuthSessionGateRestriction` (restricted/allowed/outage/admin).
  - **[MAJOR latent, P0-4] VerifyIdentity vendor-path bypass** — `AdminDecision` enforced the screening gate but the automated `VerifyIdentity`→`upsertStatus` path could write `approved` without it; latent until a real IDV vendor returns "approved" (today's providers return pending, so not yet live). FIXED: the screening gate now lives at the shared `upsertStatus` approval chokepoint, so BOTH approval paths enforce it (no identity → ErrIdentityRequired; unresolved/hit → ErrScreeningUnresolved). Test: `TestPostgresKYCVerifyIdentityCannotBypassScreeningLive` (no-identity / hit / cleared→approve, live PG).
- **Notes (out of GAP-9/P0-4 literal scope → recorded as GAP items below, not absorbed):** GAP-58 (deposit path uses the unwrapped RG checker — a suspended punter is blocked from ordering but not from depositing), GAP-59 (a reviewer's `hit_confirmed` reverts to a fresh automated verdict on identity re-submission — still blocks approval, but not terminal).
- **[P0-4] Sanctions/PEP screening — COMPLETE** (2026-07-03): slices `3469a03c` (engine) + `2f0d5ff0` (identity intake + screen-on-submit) + `653a51d6` (approval enforcement + reviewer resolution + office surface) — spec §: §12 KYC, AML, Risk, and Compliance (§32 Scenario 4 strengthened).
  - P0 Checkpoint Summary: **What changed:** person-level sanctions/PEP screening is enforced at this system's onboarding gate. Identity (name/DOB/country) is captured at KYC submission and screened immediately (yente or the never-auto-clear manual default; env misconfiguration fails closed to manual); the verdict is persisted as compliance evidence and NEVER disclosed to the subject (tipping-off guard, tested). KYC approval is refused fail-closed unless the verdict is clear or a reviewer explicitly cleared it (audited `kyc.screening_reviewed` with previous/outcome/reason); declines are never gated. Reviewers see identity + verdict + candidate entities on the office KYC page with clear/confirm actions. **Risk:** existing pending KYC subjects have no identity row → their approval is now blocked until they (or the office, via re-submission flow in a follow-up) provide identity — deliberate fail-closed posture, flagged for ops awareness. Vendor-path auto-approvals (future IDV vendor) are NOT yet gated — recorded in GAP-19's scope (the vendor decision). **Review focus:** the AdminDecision gate ordering (identity read → nil → verdict check, errors block), the FROM-self-join previous-value capture in ReviewScreening, and the yente client's fail-closed mapping. **Honest gap:** Playwright for the new office block deferred to GAP-26's scenario-evidence pass (kyc-review spec update needs the live stack + identity seeding).
  - Slice 2 evidence: `kyc_identity` table (store-owned schema) + session-bound `POST /api/v1/compliance/kyc/identity` (screen-on-submit, tipping-off guard — response is exactly `{accepted:true}`, verdict never disclosed to the subject), KYCIdentityStore on Postgres + mock (NOT the fail-closed service → intake 503s when the store is down), re-submission overwrites AND re-screens. Tests: 6 handler tests incl. leak checks + validation matrix; live Postgres roundtrip green on scratch PG16; full gateway race suite 29 pkgs ok.
  - Slice 3 (next iteration): enforcement — the kyc admin AdminDecision approve path REFUSES while the subject's screening verdict is anything but clear (hit/potential_match/unavailable/unscreened all block; a reviewer resolves by explicit clearance or decline); screening verdict + identity surfaced on the office KYC review detail; audit `kyc.screening_reviewed`; e2e. Then P0-4 complete → fresh-context verification #6 dispatches (3 items: GAP-9, GAP-10, P0-4).
  - Slice 1 evidence: `internal/compliance/sanctions.go` (+test) — PersonScreener seam, YenteScreener (/match API, verdict mapping hit/potential/clear, unavailable+error on any failure), manual-review default that never auto-clears, PersonScreenerFromEnv with fail-closed misconfiguration handling. Tests: verdict mapping, request-shape, fail-closed matrix, env table; full gateway `go test -race` exit 0.
  - Slice 2 (next iteration): `kyc_identity` storage via the KYC store's ensureSchema pattern (user_id PK, full_name, date_of_birth, country, screening_status/score/match_ids/provider, screened_at) + session-bound `POST /api/v1/compliance/kyc/identity` (NEW route — avoids changing the KYCService interface) + screener invocation on submission + fail-closed when the KYC store is unavailable. Slice 3: AdminDecision approve REFUSED while screening is unresolved (potential_match/unavailable) or hit; screening verdict surfaced on the office KYC review page; audit `kyc.screening_*` events; Playwright.
  - **Gap re-verified:** zero person-level screening (grep: only alphacashier/cashier ADDRESS screening). **Data discovery:** the system stores NO structured person identity anywhere — punters has email/status only (migration 001), kyc_status/kyc_documents hold doc metadata only (kyc_postgres.go:45-95), the player KYC submit payload is userId+documents (compliance/handlers.go:105-108). Name/DOB exist only inside document binaries. ⇒ the decided "enforce at onboarding" lands as: capture structured identity at KYC submission, screen it, and refuse KYC approval while screening is unresolved/hit — KYC approval IS the onboarding gate in this system (KYC_REQUIRED_FOR_TRADING).
  - **Slices:** (1) THIS: screening engine — `internal/compliance/sanctions.go`: `PersonScreener` seam mirroring the alphacashier fail-closed contract (screening.go:34-47), `YenteScreener` HTTP client (yente /match API; URL/dataset/threshold via SANCTIONS_* env; any error/non-200 → unavailable, NEVER clear), default manual-review screener that never auto-clears, `PersonScreenerFromEnv`. Unit tests with an httptest stub. (2) identity capture: kyc_identity storage (ensureSchema pattern, house style for KYC tables) + submit payload + screen-on-submit. (3) enforcement: AdminDecision approve refused while screening unresolved/hit + office review visibility + Playwright.
  - **Assumptions:** seam shape VERIFIED (alphacashier/screening.go); KYC store pattern VERIFIED (ensureSchema, not goose); yente /match request/response shape ASSUMED from OpenSanctions docs knowledge — resolved by keeping the client minimal (score+match), version-noted in code, with an opt-in live test (`SANCTIONS_YENTE_LIVE_URL`) for when a yente container is available; NOT load-bearing for slice 1 correctness (stubbed), load-bearing only at real deployment.
  - **Migration:** none slice 1. **RBAC/audit:** n/a slice 1 (no routes). **DoD slice 1:** engine + env selection + fail-closed tests green; no protected-core files.

## Done (Pass B queue)
- [GAP-10] Self-exclusion/suspension login block — commit `4818df2f` — files: NEW `services/auth/internal/http/restrictions.go` (+test), `services/auth/internal/http/handlers.go` (Login gate + injectable lookup field) — tests: denial matrix (self_excluded/blocked/status_*), unrestricted pass-through, admin-skip, fail-closed-by-env table (prod/staging/prod-noncanonical 500; dev 200), no-leak-on-bad-credentials, env-allowlist table; full auth `go test -race` ok (263.7s) — spec §: §13 Responsible Gaming / Responsible Trading + §32 Scenario 6.
  - P0 Checkpoint Summary: **What changed:** players who are RG self-excluded (permanent or unexpired temporary — the exact `GetPlayerRestrictions` predicate), RG-blocked, or admin-set to any non-active status are refused a session at login. The auth service reads the gateway-owned `punters` + `player_restrictions` tables via its own DB handle (the `lookupAdminUser` colocated-schema precedent). Gate runs after credential+MFA success (players only); the response is a generic 403, the audit event `auth.login.restricted` carries the machine reason; restricted logins do not count toward lockout. Lookup outages fail closed in deployed envs (GAP-4 allowlist) and open in dev. **Risk:** deployments must colocate the gateway schema with AUTH_DB_DSN — already a de-facto requirement for staff login; an auth-only DB in a deployed env now fails player logins closed rather than silently skipping the exclusion check (deliberate). One extra SELECT per login. Sessions minted BEFORE a restriction lands are not revoked (that is GAP-16 session-revocation territory, already on the roster). **Review focus:** the SQL predicate mirroring rg_postgres.go:475-483, and the gate placement after MFA (nothing leaks to unauthenticated callers — tested).
- [GAP-9] Admin suspension enforcement + reason/audit — commit `0a07a989` — files: NEW `internal/http/prediction_status_gate.go` (+test), `internal/http/handlers.go` (seam wiring), `internal/http/prediction_admin_handlers.go` (+route test), office `users/[id]/page.tsx` + `PunterProfile.tsx` — tests: gate matrix (deny/delegate/infra-error/no-row/atomicity both directions), route reason+audit assertions; gateway `go test -race` 29 pkgs ok; office production build ok; husky vitest 131 ok — spec §: §11 Identity, Account Lifecycle, and Authentication + §32 Scenario 3.
  - P0 Checkpoint Summary: **What changed:** a punter whose admin-set status is suspended/self_excluded/deactivated (or any unknown future value — fail-closed default-deny) can no longer place orders. Enforcement decorates the RG checker on the `SetComplianceChecker` seam, the single chokepoint all `PlaceOrder` callers (player HTTP, bot API, SMM, seeds) already traverse — zero protected-core edits. Status-read outages inherit the protected gate's existing env policy (fail-closed prod/staging, open dev). The status route now demands `users:write`, a mandatory launch-validated reason, and writes `punter.status_changed` audit with previous/status/reason; the office suspend/activate button prompts for the reason. **Risk:** one extra `SELECT status FROM punters` per order in db mode (absent in memory mode, where admin status persistence doesn't exist either); releases/cancels for already-suspended punters still reconcile RG usage (delegation preserved, tested). **Review focus:** the atomicity-preservation pair (`punterStatusOrderGateAtomic` — downgrading an atomic inner would re-open the D-5 TOCTOU) and the `(true,"",err)` infra-ambiguity contract mirroring service.checkComplianceForOrder. **Honest gap:** no Playwright run this iteration — no existing spec exercises the suspend button, and the reason flow's enforcement is fully server-side-tested; the end-to-end evidenced run is exactly GAP-26's Scenario 3 script (runnable now that this fix exists).
  - **Gap re-verified this iteration:** `punters.status` written by `UpdatePunterStatus` (internal/prediction/sql_admin_repository.go:193) and read ONLY by internal/bonus/repository.go:197 — nothing on the order path (grep). Status route (internal/http/prediction_admin_handlers.go:153-177) accepts `{status}` only: no reason, no audit, coarse `requireAdminRole`.
  - **Plan:** (1) NEW `internal/http/prediction_status_gate.go`: `punterStatusOrderGate` decorator over the RG checker on the `SetComplianceChecker` seam (handlers.go:593) — the chokepoint every `PlaceOrder` caller already flows through (service.go:831), so NO protected file is edited. Non-active status → authoritative deny (blocks every env); status-read error → `(true,"",err)` so the protected gate's existing env policy applies (fail-closed prod/staging); `ErrNoRows` → delegate (punter rows are lazily created). Preserves `AtomicBetGate`: atomic inner stays atomic (downgrade would re-open the D-5 TOCTOU), non-atomic never upgraded. (2) Status route: add `requireAdminPermission("users:write")` inside the status case, require + launch-validate `reason`, audit `punter.status_changed` {previous,status,reason} via `recordProviderOpsAuditAction`. (3) Office `PunterProfile` suspend/activate gains a required reason input (route now 400s without) + Playwright update.
  - **Assumptions (all VERIFIED):** ComplianceChecker iface = CheckBetAllowed/RecordBet/ReleaseBet (service.go:78-93); AtomicBetGate optional (service.go:105); rgService wired at handlers.go:593; Postgres+Mock RG are atomic, FailClosed RG is not (grep); `requireAdminPermission` falls back to role when RBAC unwired (rbac_admin_handlers.go:418); audit sink = `recordProviderOpsAuditAction` (admin_handlers.go:148, house pattern per P0-3); allowed statuses = active/suspended/self_excluded/deactivated (prediction_admin_handlers.go:70); surveillance precedent uses `walletService.DB()` for prediction-table reads (handlers.go:560).
  - **Migration:** none. **RBAC:** users:write. **Audit:** punter.status_changed. **Tests:** wrapper table-driven matrix (allow/deny/error/no-row/atomic-preservation), status-route reason+audit tests, office build + Playwright balance-adjustment/profile spec.
  - **DoD:** suspended/self_excluded/deactivated punter's order is denied in every env at the seam; status change requires reason and writes before/after audit; gateway build + `go test -race` green; no `internal/prediction|wallet` file in diff; office build + relevant Playwright green.

## PASS B — spec reconciliation COMPLETE (2026-07-03)
- Run as a fresh-context multi-agent workflow: 7 section readers over the full spec (§1-§37 incl. all 20 §32 acceptance scenarios) against the ledger + decisions, then one adversarial verifier per candidate gap instructed to REFUTE against code, ledger, and spec text. First run's verify stage was wiped by a session limit (recorded honestly, not counted); the resumed run completed clean.
- **Counts: 105 requirements satisfied · 50 accounted (decided/deferred/descoped) · 67 candidate gaps CONFIRMED · 3 refuted.** Full per-finding evidence: `docs/pam/pass-b-findings-2026-07-03.md`. Refuted for the record: staff failed-login lockout (already built — per-IP limiter + account lockout on main), pam-traceability-matrix.md absence (not a requirement — header reference only), implementation-plan reframing (already recorded via §37 itself).
- The 67 confirmed findings dedupe/merge into **GAP-9..GAP-56** below (semantic duplicates across section- and scenario-readers merged; each entry cites the § and the findings file carries the evidence).
- Spec citations back-filled on all DONE items (`1ddf7c53`). Pass B verdict: **NOT zero-gap → the loop returns to the Loop per the Termination Condition.** Pass A is also false (decision round unblocked 9 items). Termination re-check happens when the queue is exhausted again.

### Pass B build queue (P0→P1→P2; one item/slice per iteration)
1. ~~GAP-9~~ **DONE** `0a07a989` (2026-07-03)
2. ~~GAP-10~~ **DONE** `4818df2f` (2026-07-03)
3. ~~P0-4~~ **DONE** `3469a03c`+`2f0d5ff0`+`653a51d6` (2026-07-03) → verification #6 due
4. **P0-5** AML plumbing, rules-as-data (decided; design must express risk-point accrual → case-at-threshold, Scenario 5)
5. **GAP-12** (P0) customer risk-profile rating — sequence right after P0-5 (shares the risk-scoring substrate)
6. **GAP-11** (P0) RG limit-type breadth (loss/position/session)
7. **P0-6** maker-checker (adjustments half autonomous; settlement half review-gated)
8. **GAP-13** (P0) audit hash-chaining
9. **P0-7 phase 1** shadow-write double-entry (review-gated, protected core)
10. **P1-1** cashier back office (decided, flag-OFF + unreachability test)
11. **GAP-3** TOTP secret encryption (decided) → then P1 gaps GAP-14..27 (roster below), P2-3 DSAR, GAP-6, GAP-8, then P2 gaps GAP-30..56.

## Done
- [P0-1] Real admin MFA — commits `6914421c` (slice 1, prior session: TOTP machinery — enroll/activate/disable/status endpoints, `auth_mfa` table via ensureUserSchema, login OTP verification for active factors) and `268157b8` (slice 2, this session: forced enrollment for admins + OAuth admin denial) — files: `services/auth/internal/http/{handlers,mfa,totp,oauth}.go` + `{handlers,totp,mfa_admin}_test.go` — tests: full auth suite `go test -race` ok (174.3s) incl. new `TestMFAAdminRequiredFromEnv`, `TestAdminLoginRequiresEnrollmentWhenFlagOn` (9-step end-to-end journey), `TestAdminLoginLegacyWhenFlagOff`, `TestEnrollTokenExpiryAndRotation`, `TestOAuthSessionGateDeniesAdmins` — spec §: §11 Identity, Account Lifecycle, and Authentication + §27 Security Requirements (§32 Scenario 1).
  - P0 Checkpoint Summary: **What changed:** privileged (admin-role) logins now require an active TOTP factor. Un-enrolled admins presenting a correct password get a single-use, 10-minute enrollment token (honored only by /2fa/enroll + /2fa/activate via X-MFA-Enrollment-Token) instead of a session; after activation they log in with password + OTP. Enforcement is unconditional in production/staging (setting AUTH_MFA_REQUIRED_FOR_ADMINS=false there is a fatal boot error) and opt-in in dev. OAuth callbacks refuse to mint sessions for admin accounts, closing the verified-email auto-link bypass. **Risk:** enrollment-token bootstrap is trust-on-first-use — an attacker who already holds a valid admin password can enroll their own factor; that is no worse than the pre-change posture (password alone logged them in) but provisioning-time enrollment is recommended; tokens are in-memory (single-instance auth service — restart just re-issues). Dev/e2e flows are unchanged because dev defaults to off. **Review focus:** the login-gate ordering in `Login` (enrollment check sits between the fail-closed MFA lookup and the active-factor OTP verification), and `mfaPrincipalFromRequest` (token path must not widen what a session can do — it authorizes enroll/activate only, and activation consumes the token).

- [P1-3] Profile-360 Limits/self-exclusion tab — commit `d610ecdf` — files: gateway `internal/http/rg_admin_handlers.go` (+test) + `handlers.go` wiring; office `PunterProfile.tsx` (Limits tab) + `users/[id]/page.tsx` (loadRG) + `balance-adjustment.spec.ts` (Limits render test) — tests: gateway internal/http + internal/compliance `go test -race` ok; office build ok; Playwright backoffice 7/7 — spec §: §13 Responsible Gaming / Responsible Trading + §10 Player Search and Player Profile 360 (§32 Scenarios 2, 6). New admin route `GET /api/v1/admin/rg/restrictions?userId=` gated `compliance:read`; distinct path (the `/admin/punters/` prefix handler owns that subtree). Read-only; limit-setting is a player action / P0-6 maker-checker concern.
- [P1-2] Manual balance-adjustment UI — commit `bb710f52` — files: office `components/users/BalanceAdjustment.tsx` (+index), `users/[id]/page.tsx`, `e2e/backoffice/balance-adjustment.spec.ts` — tests: office build ok; Playwright real 1.00-pt credit green — spec §: §14 Wallet, Balances, Ledger, and Transactions + §25 Admin Operations and Configuration (§32 Scenario 8). Drives existing audited `finances:write` wallet routes; confirm-step + required reason (four-eyes = blocked P0-6).
- [P0-2] Remove KYC mock-fallback — commit `5adf223c` — files: `gateway/internal/compliance/fail_closed.go` (+`KYCFallbackForEnv`), `fail_closed_test.go` (new), `gateway/internal/http/handlers.go` (wiring), `gateway/cmd/gateway/main.go` (boot check), `main_test.go` + `edge_auth_boot_test.go` (baselines + new refusal/acceptance tables) — tests: full gateway `go test -race` ok, 26 packages — spec §: §12 KYC, AML, Risk, and Compliance (§32 Scenario 4).
  - P0 Checkpoint Summary: **What changed:** production and staging can no longer run KYC on the in-memory mock. If the Postgres KYC store fails to initialize, deployed environments degrade to the fail-closed service (refuses verifications, reports non-approved status); a deployment without a DB-backed wallet store (which carries the KYC/RG schema) now refuses to boot in production AND staging — previously only production was covered, and only by the wallet service itself. Dev behavior is unchanged. **Risk:** a mid-flight Postgres schema failure in prod now turns KYC actions into denials instead of silent mock approvals — operationally louder (slog.Error + fail-closed) but compliance-correct. Existing boot-validation tests needed the new baseline keys (`WALLET_STORE_MODE=db`), which is expected churn, not weakened coverage. **Review focus:** the boot check's mode aliases (db/sql/postgres) mirror `wallet.NewServiceFromEnv` exactly; `KYCFallbackForEnv` is the single chokepoint both fallback branches route through. Note: `go vet` on cmd/gateway flags pre-existing json-tag nits in `migrate_legacy_loyalty.go` (untouched by this diff; vet is not a repo gate).

- [P0-3] KYC admin review UI + document binary storage — commits `f6501f0a` (slice 1: gateway storage + review API), `b95fe9a0` (slice 2: office review screen + Playwright harness repair), `ad6e84c4` (slice 3: Profile-360 KYC tab) — files: gateway `internal/compliance/{kyc_files,kyc_postgres,mock_compliance,handlers}.go` + `internal/http/kyc_admin_handlers.go` + migration 050; office `app/(dashboard)/compliance/kyc/page.tsx`, `app/(dashboard)/users/[id]/page.tsx`, `app/components/users/PunterProfile.tsx`, `app/(dashboard)/layout.tsx`; e2e `kyc-review.spec.ts` + harness fixes — tests: gateway `go test -race` 26 pkgs ok; migration 050 up/down clean on scratch Postgres; opt-in live Postgres roundtrip test green; office production builds ok; Playwright backoffice 5/5 green against the live local stack — spec §: §12 KYC, AML, Risk, and Compliance + §10 Player Search and Player Profile 360 + §20 Notes, Documents, and Communication History (§32 Scenarios 2, 4).
  - P0 Checkpoint Summary: **What changed:** identity documents are now stored as real binaries (BYTEA, content-type allowlist, 8 MiB cap, sha256, ownership-bound attach; the fail-closed KYC service refuses uploads); the back office gained a KYC Review queue with per-user detail, audited document viewing (`kyc.document_viewed`), and approve/decline; the user Profile-360 gained a read-only KYC tab. New `compliance:read` permission gates all review reads (migration 050); decisions remain `compliance:write`. **Risk:** document binaries in Postgres grow the DB (8 MiB cap bounds it; a move to object storage is a future scaling decision, not a compliance one); the review reads are metadata except the audited binary view. The e2e harness repair changed shared fixtures (`loginAsAdmin` now drives the real auth service) — the old mock-login path was dead code since the App Router sweep, and any spec that relied on it was already failing. **Review focus:** `mfaPrincipal`-style ownership check in `AttachDocumentFile` (cross-user attach must be not-found), the `kycAdminStore` consumer interface, and the launch-safety posture (all new surfaces are back-office; the player KYC surface stays behind `NEXT_PUBLIC_FEATURE_KYC`, untouched).

## Blocked / awaiting decision
- [P0-4] Sanctions/OFAC + PEP screening — ⚑ vendor choice.
  - **Verified state:** the repo screens crypto WALLET ADDRESSES in the alpha cashier (`internal/alphacashier/screening.go`, fail-closed with enforcement-ack flags — audit CMP-01) but has NO person-level screening: nobody checks names/DOB against OFAC/UN/EU sanctions or PEP lists at onboarding or on money movement.
  - **Question:** which screening vendor/data source, and does enforcement extend to existing users retroactively (re-screening cadence)?
  - **Options:** (a) ComplyAdvantage or Refinitiv World-Check — commercial API, PEP + adverse media included, fastest to integrate behind the existing IDV-provider-style seam; recurring cost. (b) OpenSanctions/yente — self-hosted matcher over open OFAC/UN/EU data, no per-check cost, PEP coverage weaker, matching quality tuning is on us. (c) Defer person-screening to the KYC IDV vendor (Sumsub/Onfido bundle it) — one vendor bill, but couples two controls and stays observe-only until that vendor is chosen.
  - **Recommendation:** (b) now for launch-mode (no money paths → screening gates onboarding only) with the seam shaped like `alphacashier/screening.go`, upgrade to (a) when fiat/crypto rails open.
  - **Unblock:** name the vendor/source + the enforcement points (onboarding mandatory; deposit/withdrawal when those exist per Two-Spec rule).
- [P0-5] AML transaction monitoring + case/SAR workflow — ⚑ rule set / regime.
  - **Verified state:** no monitoring engine exists (grep across gateway: only cashier screening comments reference AML). The launch product is points-only (Tiangge §2/§19), so live AML rules would currently observe play-money flows.
  - **Question:** which regulatory regime's rule set and thresholds (structuring windows, velocity, aggregate limits), and what filing workflow (SAR-equivalent) does the target license expect?
  - **Options:** (a) Build a rule engine now with placeholder thresholds — risks encoding the wrong regime and rework. (b) Build the PLUMBING now (event stream from wallet/ledger into an `aml_alerts` + `aml_cases` store with a review UI) and load regime rules when counsel names them. (c) Wait entirely.
  - **Recommendation:** (b) — the alert→case→disposition workflow is regime-independent and satisfies the "operable back office" bar; rules land as data, not code.
  - **Unblock:** regime + threshold sheet from compliance counsel; then (b)'s rule pack becomes a normal backlog item.
- [P0-6] Dual-approval (maker-checker) for settlement + high-value manual adjustments — ⚑ thresholds.
  - **Verified state:** no maker-checker exists anywhere in admin paths (grep). Settlement finalize and wallet credit/debit admin routes act on a single `finances:write`/`settlements:resolve` holder.
  - **Question:** which actions require four-eyes, and above what thresholds? (Settlement always? Manual adjustments above N cents? KYC overrides?)
  - **Options:** (a) Universal four-eyes on settlement finalize + any manual balance adjustment — simplest to reason about, slows ops. (b) Threshold-based: adjustments ≥ X require a second approver, settlement always requires one — matches common practice. (c) Role-split only (proposer role ≠ approver role) without amounts.
  - **Recommendation:** (b) with X proposed at 10,000 points-cents equivalents until the human sets a real number; implementation as a `pending_admin_actions` table + approve endpoint gated on a distinct permission + audit both legs. NOTE: touches settlement paths → even after threshold approval, the settlement half stays protected-core and needs human review of the diff.
  - **Unblock:** approve the action list + thresholds; confirm the settlement-side change may proceed under protected-core review.
- [P0-7] Double-entry ledger migration — ⚑ PROTECTED CORE. Full design note written to `docs/pam/designs/p0-7-design.md` (current single-entry `wallet_ledger` verified in code; target accounts/transactions/postings schema; 4-phase migration with shadow-write + backfill + verified cutover; idempotency-key mapping; testing gates). BLOCKED on the four decisions listed in the note (account taxonomy, goose vs runtime DDL, sequencing vs P1-1, bonus-history mapping).
- [P2-1] Activate multitenancy — SPLIT. **Core query/auth scoping = PROTECTED CORE → design note `docs/pam/designs/p2-1-design.md`, BLOCKED** (adding `AND tenant_id=$` to prediction/wallet SQL is a settlement/money-path edit; a wrong predicate leaks or drops a tenant's book/balance). Migration 037 already shipped the dormant `tenant_id` column everywhere but nothing reads it. **Peripheral pieces buildable outside protected core** (not blocked): DB-backed feature-flag/config store, tenant/brand admin UI, global jurisdiction admin UI — candidate P2-1 slices. Unblock (core): human approval of the RLS+WHERE isolation model + tenant-claim source.
- [P2-3] Data-retention/DSAR tooling — ⚑ legal. BLOCKED: requires a retention schedule + legal basis per data category, reconciled with the DB-trigger append-only audit (which by design cannot be deleted). Question: what is the retention period per category (KYC docs, audit log, wallet ledger, PII) and does DSAR erasure carve out the immutable audit/ledger (standard: pseudonymize PII references, retain financial/audit records under the legal-obligation basis)? Recommendation: build a DSAR export + a pseudonymization pass that redacts PII columns while preserving audit/ledger row integrity; needs counsel to set periods + confirm the audit carve-out. Unblock: retention schedule + erasure-vs-immutability policy from legal.
- [P2-4] Non-custodial on-chain settlement — ⚑ MAJOR DECISION, never start. BLOCKED per backlog. This is a build-vs-descope architecture decision (contracts/relayer/bridge vs. formal descope) gating ADR-0003/0004 and interacting with the launch-policy memory (crypto-native, outside-US, off-chain TBD). No design note authored autonomously beyond this pointer; needs the founder's build/descope call and, if build, a full protected-core design review. Unblock: founder decision to build (with scope) or formally descope.
- [P1-1] Cashier/deposits UI + withdrawals queue + AWA auto-approval rules engine — ⚑ launch-safe-blocked (Two-Spec conflict).
  - **Conflict:** PAM spec §22 (Payments/Deposits/Withdrawals) requires a full cashier back office; Tiangge §2/§19 hard-prohibit any user-facing money path (no fiat, no crypto, no withdrawals, no cashout, no redeemable value) in launch mode. Per the Two-Spec Precedence Rule this must be built launch-safe: flag-gated OFF, zero launch navigation, routes/jobs inert unless the flag is enabled, with an unreachability test.
  - **Verified state:** an `internal/alphacashier` package already exists and is flag-gated OFF (`ALPHA_CASHIER_ENABLED`, refused at boot in prod/staging per `cmd/gateway/main.go` — a deposit/withdrawal/crypto rail that the boot check actively BLOCKS in deployed envs). So the launch-safety boundary for the *rail* is already enforced. What P1-1 adds on top is the BACK-OFFICE operational surface: a withdrawals review queue + an AWA (auto-withdrawal-approval) rules engine (limits/thresholds/day-schedule/manual-review triggers) + a deposits view.
  - **Question for the human:** should the withdrawals-queue + AWA rules-engine back office be built now behind the same `ALPHA_CASHIER_ENABLED` (or a new `CASHIER_BACKOFFICE_ENABLED`) flag — default OFF, no launch nav, unreachability test — or deferred until a post-launch money-enabled phase is scheduled? Building it now is launch-safe and satisfies §22, but it is dead weight while Tiangge forbids money paths.
  - **Options:** (a) Build now, flag-gated OFF, with an unreachability test proving the office nav + routes are absent when the flag is off — satisfies §22, licensable-ready, but unused at launch. (b) Defer to a scheduled money-enabled milestone (recommended if launch is imminent and no money phase is dated) — record as descoped-until-milestone. (c) Build only the withdrawals-queue data model + AWA rules engine in the gateway (no UI) now, UI later.
  - **Recommendation:** (a) if the licensing timeline needs §22 demonstrably present; (b) if launch is the near-term priority and money is genuinely out of scope until a dated phase. Needs the human's read on the licensing-vs-launch timeline.
  - **Unblock:** a decision on (a)/(b)/(c) + the AWA threshold/schedule policy (which is itself the same class as P0-6's ⚑ thresholds).
- [BOOT-1] PAM source-of-truth docs missing from the repository
  - **Question:** the five PAM docs (`spec.md`, `taya-gap-analysis.md`, `pam-implementation-plan.md`, `pam-domain-model.md`, `pam-open-questions.md`) were never committed to the repo. A directory named `pam` exists at `/Users/john/Sandbox/Taya_NA_Predict/docs/pam/` — inside the outer non-git tree this loop is forbidden to read. Are those the authoritative docs, and should they be moved into the repo?
  - **Options:** (a) Human moves/copies them: `mkdir -p <REPO_ROOT>/docs/pam && cp /Users/john/Sandbox/Taya_NA_Predict/docs/pam/*.md <REPO_ROOT>/docs/pam/` then commits — loop picks them up next iteration and gains citations + termination pass B. (b) Human authorizes the loop to read the outer `docs/pam/` in place (one-line instruction suffices). (c) Docs are regenerated from scratch — slow, not recommended.
  - **Recommendation:** (a). It is one command and removes all ambiguity.
  - **Unblock criteria:** the five files exist non-empty under `REPO_ROOT/docs/pam/` (in the pam-worktree after a merge/rebase, or committed on a branch this worktree can merge), `spec.md` opening with `# Enterprise Prediction Market PAM / Back Office Spec` and containing `## 36. Progress Matrix` and `## 37. Reconciliation`.

## Stall reports
- (2026-07-02, resumed firing) FIXED-POINT HOLD — not a defect stall. This loop firing re-read the ledger, re-verified BOOT-1 (the PAM spec `docs/pam/spec.md` is still absent — checked with `ls`, still missing), and confirmed no human commits landed since HEAD `7f529343`. Re-scanned the full backlog: every item P0-1..P2-4 and every GAP-1..8 is DONE (with evidence) or BLOCKED (with a brief/design note), so termination pass A remains TRUE and there is no unblocked item to select at step 2 of the Loop. Termination pass B (spec reconciliation) cannot run without `docs/pam/spec.md` (BOOT-1). Sanity check: `go build ./...` is clean in both the gateway and auth modules at HEAD — nothing rotted. No commit produced this firing because there is no buildable or blockable work remaining; producing one would mean re-doing a completed item or fabricating scope, both guardrail violations. The loop is holding at its documented fixed point and can only be advanced by the human supplying the spec (BOOT-1) or answering a blocked decision in `docs/pam/DECISIONS_NEEDED.md`. The 30-minute schedule remains armed; each firing will re-check BOOT-1 and resume the instant the spec appears.

## Doc drift
- (2026-07-02) [loop-prompt:Phase 0.2] — documented: PAM docs live under `REPO_ROOT/docs/pam/` — actual: `docs/pam` has never existed in git history (evidence: empty `git log --all -- docs/pam`); a `pam` directory exists only in the outer non-git tree — action: BOOT-1 blocked item with move instructions; loop continues on inline backlog + Tiangge spec.
- (2026-07-02) [loop-prompt:P0-1] — documented: "replace the in-memory 2FA toggle" gap is open — actual: prior session already landed TOTP MFA on `pam/p0-modernization` (`6914421c`, files `services/auth/internal/http/{handlers,mfa,totp}.go` + tests) — action: iteration 1 re-verifies it against the DoD instead of re-implementing.

## Fresh-context verification (2026-07-02, post P0-1..3)
Three independent lenses (compliance posture / ledger-claim audit / adversarial correctness) over `main..HEAD` + this ledger. Claim audit PASSED: all commits/files/tests verified to exist and re-ran green. Findings and dispositions:
- **[major, convergent] RG mock fallback reachable in prod/staging** (`internal/http/handlers.go` RG init-error branch) — FIXED this session (see GAP-1 status below): `RGFallbackForEnv` + `FailClosedResponsibleGamblingService` (denies all stake/deposit checks, errors on mutations), both wiring branches routed through it, table-driven tests added.
- **[minor] no nosniff on document binary route** — FIXED (`X-Content-Type-Options: nosniff` + test).
- **[minor] per-subject KYC detail read unaudited** — FIXED (`kyc.subject_viewed` audit event on the user-detail read; the aggregate queue stays unaudited by design to avoid an audit row per dashboard refresh).
- **[minor] TOTP replay within skew window** → GAP-2 backlog item (not silently absorbed).
- **[minor] TOTP secrets plaintext at rest** → GAP-3 backlog item (needs a KMS/therapy decision — flagged).
- **[minor] exact-string ENVIRONMENT matching downgrades posture on typos ("prod", "live")** → GAP-4 (repo-wide posture inversion, needs a decision; consistent house pattern today).
- **[minor] auth-service audit sink is a process log, not the durable audit store** → GAP-5.
- **[minor] audit writes are best-effort (document served even if audit append fails)** → noted under GAP-5's scope (deny-on-audit-failure for identity-document access).
- **[minor, hygiene] ledger line-ref drift + stale bullets** — FIXED in this update.

## GAP items requiring a human decision (BLOCKED with brief)
- [GAP-3] ⚑ Encrypt TOTP secrets at rest — **BLOCKED**. Question: where does the envelope/wrapping key live (cloud KMS, an env-provided key, or Vault)? Options: (a) cloud KMS envelope encryption — strongest, adds a KMS dependency + IAM; (b) an `AUTH_MFA_ENC_KEY` env key with AES-GCM at the app layer — simplest, key-management burden on ops; (c) Postgres pgcrypto — keeps it in the DB but the key still has to live somewhere. Recommendation: (a) for production, (b) acceptable interim. Unblock: the human picks the key-management approach; then it's a bounded auth-module build (encrypt on write in `mfaUpsertPending`, decrypt on read in `mfaGet`, one-time migration of existing rows).
- [GAP-6] ⚑ Device/IP fingerprint capture — **BLOCKED**. Question: is capturing signup/login IP + user-agent + a device id acceptable under the privacy posture, and where is it ingested (auth service login/registration)? Options: (a) capture into a new `punter_signals` table from the auth service; (b) defer (email-normalization dup-detection from P1-5 is the interim). Recommendation: (a) when fraud pressure justifies the PII; needs a privacy/legal nod. Unblock: privacy sign-off + the ingestion point.
- [GAP-8] ⚑ Runtime-editable jurisdiction config — **BLOCKED**. Question: should the geo allowlist (`GEO_ALLOWED_COUNTRIES`) become runtime-editable via the config store, given it is currently env-driven and fail-closed by boot validation? A naive runtime editor would weaken the fail-closed control (guardrail #8). Options: (a) keep env-only (status quo, safest); (b) allow runtime tightening only (add-country requires redeploy, remove-country can be runtime) so the control can only get stricter at runtime; (c) full runtime with a fail-closed default + audit. Recommendation: (b). Unblock: the human's call on the fail-closed-config model (relates to the P2-1 feature-flag store).

## Backlog additions (GAP items)
- [GAP-1] (P1) ~~Responsible-gambling~~ and geo-compliance mock fallbacks in deployed environments — **RG half FIXED** post-verification (fail-closed RG service + env-aware wiring, commit follows this entry). REMAINING: mock geo service is unconditional (`internal/http/handlers.go:479`; `FailClosedGeoComplianceService` still dead code) — mitigated by the separate env-driven `GeoGate` allowlist which does the actual jurisdiction enforcement; the mock geo service backs the lat/long verification API only.
- [GAP-2] (P1) TOTP replay protection: track last-used timestep per user (column on `auth_mfa` + memory mirror) and reject reuse within the skew window — RFC 6238 §5.2; applies to login OTP, activate, disable. Justification: admin MFA hardening (verification finding).
- [GAP-3] (P1) Encrypt TOTP secrets at rest (KMS/env-key envelope) — needs a key-management decision from the human (where does the wrapping key live?). Justification: DB compromise currently yields all second factors.
- [GAP-4] (P2) Invert environment detection to a dev/test allowlist (fail closed on unknown ENVIRONMENT values) across gateway boot checks + fallback selectors — repo-wide posture change, propose alongside a canonical-env boot validation.
- [GAP-5] (P1) Durable auth-service audit: route `auth.*` events (MFA lifecycle, OAuth denials, logins) to the append-only DB-backed audit store; consider deny-on-audit-failure for identity-document serving. Justification: process logs are mutable/per-instance — the property the gateway's own boot check rejects.
- [GAP-7] (P1) Retrofit the bonus/campaign admin routes (`internal/http/bonus_handlers.go`: list/grant/forfeit/campaigns, all on the coarse `role==admin` gate) to the fine-grained RBAC permission model + audit sensitive actions — the same class as SECURITY-REVIEW #5 that migration 040 closed for wallet/settlement/KYC routes. Whole-surface change (all bonus admin routes), so not partially patched during P1-6. Justification: consistent RBAC + audit posture across admin money-adjacent routes.
- [GAP-6] (P2) Device/IP fingerprint capture for stronger fraud/duplicate-account detection: `punters` stores no signup/last-login IP, user-agent, or device id, so P1-5 can only match on email-normalization collision. Capturing these (auth-service login + registration ingestion → a `punter_signals` table) would enable IP/device-cluster and velocity detectors. Schema + ingestion change; spec §: §18 Market Integrity, Fraud, and Abuse Monitoring.

## Backlog additions — PASS B confirmed gaps (2026-07-03, GAP-9..GAP-56)
Evidence for every entry: `docs/pam/pass-b-findings-2026-07-03.md`. Merged from 67 verifier-confirmed findings. ⚑ = Blocked-Item Protocol (brief in DECISIONS_NEEDED.md); (doc) = documentation deliverable.

**P0 — compliance/security:**
- [GAP-9] (P0) Admin suspension is a dead flag — `punters.status` written by `UpdatePunterStatus` (sql_admin_repository.go:193) but never read on the trading path; status route accepts no reason and writes no audit — §11 Identity, Account Lifecycle, and Authentication + §32 Scenario 3. Fix at the http/compliance gate layer, NOT protected core (verify at build).
- [GAP-10] (P0) Self-exclusion/suspension does not block LOGIN — auth service has zero restriction awareness (§13 Responsible Gaming / Responsible Trading + §32 Scenario 6, "login and trading blocked"). Needs an auth→restrictions lookup or status mirror; design with GAP-9.
- [GAP-11] (P0) RG limit-type breadth — only deposit + stake limits exist; spec requires loss, position/exposure, and session limits — §13 + Scenario 6.
- [GAP-12] (P0) Persistent per-customer risk-profile rating (low→high) feeding gating — §12 KYC, AML, Risk, and Compliance. Sequence immediately after P0-5 (shares risk-scoring substrate).
- [GAP-13] (P0) Audit-log hash-chaining — tamper evidence today rests solely on the DB trigger; a superuser can drop it without in-band evidence — §24 Audit Logs and Compliance Evidence + Scenario 17.

**P1 — money/ops:**
- [GAP-14] (P1) Seed the §6 role personas — only 3 of 9 roles exist (027 seeds super-admin/ops/support); needs a seed migration + role-permission matrix data pass — §6 User Roles / §7 Permission Model.
- [GAP-15] (P1) MFA management surface — factor status/admin reset (lost-device recovery), WebAuthn option — §11/§25/§27.
- [GAP-16] (P1) Admin session revocation + concurrent-session steal-lock — §11.
- [GAP-17] (P1) Automated KYC re-trigger rules (volume/registration-driven) — §12.
- [GAP-18] (P1) KYC review "request more documents" action/state — §12.
- [GAP-19] (P1 ⚑) Production KYC IDV vendor — seam exists (`idv.go`), vendor choice is human — §12. Brief in DECISIONS_NEEDED.md.
- [GAP-20] (P1) Tag-gated market eligibility + Profile-360 eligibility view — §15 Prediction Market Account Functions.
- [GAP-21] (P1) Admin order view/cancel, audited — only a player-scoped cancel exists — §16. (Calls the existing service cancel; no protected-core edit — verify at build.)
- [GAP-22] (P1) Insider/abnormal-pattern surveillance detector — P1-4 ships wash/spoof/collusion only — §18.
- [GAP-23] (P1) Bonus-abuse detection rules — §18.
- [GAP-24] (P1 ⏳) Statutory/regulatory report suite — rides the same counsel regime input as P0-5 — §23.
- [GAP-25] (P1) Audit RBAC permission DENIALS — `requireRBACPermission` 403s with no audit write; small bounded fix at the chokepoint — §24/§27 + Scenario 1.
- [GAP-26] (P1) §32 scenario-evidence pass — script + capture each runnable scenario's Required Evidence and flip §36 statuses (per-capability tests exist; formal scenario evidence does not) — §36.
- [GAP-27] (P1) Reconcile shipped schema against `pam-domain-model.md` entities/state-machines (doc only landed at `54139c24`) — §30.
- [GAP-28] (P1 ⚑) Secrets-management approach beyond the MFA key (env-vars today) — §27. Brief in DECISIONS_NEEDED.md.
- [GAP-29] (org ⚑) ISO 27001-grade ISMS posture — organizational, outside the code loop's remit; needs an owner — §27. Recorded in DECISIONS_NEEDED.md.

**P2 — breadth:**
- [GAP-30] (P2) Per-country jurisdiction switch matrix (signup/login toggles, signup age, per-country deposit limits, per-country KYC/geo/AWA selection; incl. per-country KYC matrix) — §8/§12. Note: signup-not-gated is a deliberate code decision (gate_surface.go) never ledger-recorded — this entry records it.
- [GAP-31] (P2) Config registry scope dimensions (country/currency) + encryptable values — flat KV today; escalates to P1 the moment ops stores credentials in it — §8/§27.
- [GAP-32] (P2) Unified case management — shared case model (six §19 types incl. general support; assignee/SLA/notes) + cross-domain case center; build the shared model alongside P0-5's aml_cases to avoid a third divergent schema — §9/§19 + Scenario 19.
- [GAP-33] (P2) Office UIs for CRM (segments/tags/query builder/campaigns) + notification-template editor — backend APIs are DONE, UI absent; /campaigns is a guarded redirect stub (use fresh routes) — §9/§21/§29 + Scenario 13.
- [GAP-34] (P2) Player-search predicates: phone, national-ID, tag, trading-attribute — §10.
- [GAP-35] (P2) Profile-360 missing tabs: Bonuses/Rewards, Notes & Timeline, Cases — §10.
- [GAP-36] (P2) Registration: persist affiliate tag + signup country — §11.
- [GAP-37] (P2) Configurable password policy (players + staff) — §11/§25. (Account-level lockout exists per verifier refutation; policy configurability does not.)
- [GAP-38] (P2) Reality-check session reminders — §13.
- [GAP-39] (P2) Problem-trading flag on profile/RG state — §13.
- [GAP-40] (P2) National/third-party exclusion-register integration seam + admin screen — §13/§9.
- [GAP-41] (P2 ⚑ PROTECTED) Currency dimension on wallets/ledger — financial-table schema change; fold into the P0-7 design (multi-currency accounts in the postings schema), never standalone — §14.
- [GAP-42] (P2) Communication channel abstraction (SMS/push/in-app; email-only today) — §20.
- [GAP-43] (P2) Per-player sent-communication history + agent-initiated templated send (launch-safe: dispatch fail-closed) — §20 + Scenario 12.
- [GAP-44] (P2) Tag groups + segment-query export — §21.
- [GAP-45] (P2) Event-triggered lifecycle journeys (campaigns are one-shot) — §21.
- [GAP-46] (P2) Payment-methods registry/config screen — build launch-safe with P1-1 or descope-note — §22/§25.
- [GAP-47] (P2 doc) Fiat multi-PSP connectors formal descope note (crypto-native launch policy; P2-4-style) — §22/§26.
- [GAP-48] (P2) Finance/payments dashboards + daily balance/trading-volume report + wallet-ledger CSV export — §23 + Scenario 15.
- [GAP-49] (P2) PDF export (CSV exists) — §23 + Scenario 15.
- [GAP-50] (P2) Structured before/after per-field change tracking in audit details (incl. config old-value; platform_config_handlers.go:85 audits new value only) — §24 + Scenarios 17/20.
- [GAP-51] (P2) Consent capture/versioning — §25/§28.
- [GAP-52] (P2 doc) §29 UI-stack divergence note — spec prescribes React 18/MUI/Idefix Nx; shipped is Next.js+AntD/P8; record the amendment — §29.
- [GAP-53] (P2) Office-wide list-detail / filter+export-grid pattern rollout — §29.
- [GAP-54] (P2) A11y + responsive verification for the back office — §29.
- [GAP-55] (P2) Seed/demo compliance breadth: KYC/risk/RG-state trader variety, brands/jurisdictions, payment methods, pending withdrawals — §31.
- [GAP-56] (P2 doc) Annotate `pam-open-questions.md` with the 2026-07-02 decision-round answers (doc is stale, pre-decisions) — §34.
- [GAP-58] (P1) Deposit path uses the UNWRAPPED RG checker — `handlers.go` sets `payments.DepositComplianceChecker = rgService` (raw), not the GAP-9 status-wrapped one, so an admin-suspended/deactivated punter is blocked from placing orders but can still fund the account. Money-path, so only reachable when legacy money routes are enabled (not launch mode); wrap the deposit checker too, or fold into P1-1 cashier. Discovered verification #6 (2026-07-03) — §11 Identity, Account Lifecycle, and Authentication.
- [GAP-59] (P2) A reviewer's `hit_confirmed` screening verdict is not terminal — re-submitting identity overwrites it with a fresh automated verdict (still blocks approval since only clear/cleared_by_review pass, so not a bypass, but a confirmed hit becomes re-reviewable on demand). Add a reviewer-lock if `hit_confirmed` is meant to be final. Discovered verification #6 (2026-07-03) — §12 KYC, AML, Risk, and Compliance.
- [GAP-57] (P1) Auth demo-user seeding gates on exact `env != "production" && env != "staging"` (services/auth/internal/http/handlers.go ~line 369): a non-canonical deployed ENVIRONMENT ("prod", "preprod") gets demo users with KNOWN PASSWORDS seeded — the same defect class GAP-4 fixed in the gateway. Invert to the dev-allowlist (`authKnownDevEnvironments`, now available from GAP-10). Discovered during GAP-10 (2026-07-03) — §27 Security Requirements.

## Done (P1 continued)
- [P1-5] Fraud / duplicate-account detection — commit `0d752967` — files: `internal/surveillance/fraud.go` (+`fraud_test.go`), engine default-set update, live test — tests: `NormalizeEmail` unit table + live Postgres duplicate-account test green; surveillance+http `go test -race` ok — spec §: §18 Market Integrity, Fraud, and Abuse Monitoring (§32 Scenario 10, linked-accounts leg). Email-normalization collision detector (plus-addressing, gmail dot/googlemail aliasing) reusing the P1-4 alert/case pipeline + UI; no new tables/migration/permission. Device/IP fingerprinting deferred as GAP-6 (schema lacks the signals).
- [P1-6] Bonus admin UI + notification-template store + reporting/exports — commits `dae22915` (slice 1: bonus admin office UI + userId on the admin bonus response), `a2cf341f` (slice 2: `internal/notify` DB-backed template store + admin editor API + migration 052 + resolution sender using RenderOrFallback), `a95f6e5a` (slice 3: CSV export routes `report_exports_handlers.go` + `/exports` office page + migration reuse) — tests: gateway `go test -race` 27 pkgs; notify render units; report-export live test; office vitest 131/131; Playwright bonuses 5/5, reports 4/4 — spec §: §20 Notes, Documents, and Communication History + §21 Segmentation, CRM, Bonuses, Rewards, and Lifecycle + §23 Reporting, Dashboards, and Exports (§32 Scenarios 12, 14, 15-partial). All guardrail-clean (no prediction/wallet/settlement touched).
- [P1-4] Market-integrity surveillance — commits `7761823b` (slice 1: store + wash detector + admin API + migration 051), `8936142a` (slice 2: spoofing + collusion detectors), `e33045c4` (slice 3: office alerts/cases UI + DataTable key fix), hardened by `e7302348` (verification #2 fixes) — tests: gateway `go test -race` 27 pkgs; live Postgres detector tests; Playwright backoffice surveillance 4/4 — spec §: §18 Market Integrity, Fraud, and Abuse Monitoring + §19 Case Management (§32 Scenario 10). New `internal/surveillance` package, read-only over the trade/order log, writing only its own alert/case tables (guardrail-clean). RBAC `surveillance:read`/`:write`, all mutations audited.

## Lessons
- The PAM branch lives in a linked git worktree (`pam-worktree`), not the main checkout — `git worktree list` from REPO_ROOT is the fastest way to find in-flight branch work; file mtimes and directory names mislead.
- Merging `main` into the PAM branch conflicts in `services/auth/internal/http/handlers.go` whenever main hardens auth (it did: per-IP limiter P1-05). Resolution pattern: keep main's additions AND the 3-arg `Login(..., otp)` signature; `firstNonEmpty` now comes from main's copy (P0-1's trailing duplicate was dropped in the merge).
- Husky pre-commit runs the FULL frontend unit suite (`lerna run test`) + lint-staged on every commit (~2 min) and a post-commit commitlint that complains about old commits — its failure does not block the commit. Budget commit time; use timeout ≥300s.
- The auth test harness pattern: `t.Setenv` for seed users + `NewAuthService()` + `RegisterRoutes` + `httpx.Chain(...)`; `totpCode(secret, time.Now())` from totp.go generates valid codes in tests. Suite is bcrypt-slow (~3 min with -race) — run targeted `-run` filters while iterating.
- The gateway already has a fail-closed services toolkit (`internal/compliance/fail_closed.go`) that nothing uses — check for existing dead "correct" implementations before writing new ones.
- The backoffice Playwright harness had been dead since the office moved to the App Router: the auth setup drove a mock login at a route that 404s, ports 3000/3002 could never bind on this machine (other projects' dev servers), and `reuseExistingServer: true` will happily "reuse" a FOREIGN process on the expected port — always verify what is actually listening before trusting a green webServer.
- Backoffice e2e now requires the local Go stack: scratch Postgres (`docker run … -p 5436:5432 postgres:16` + `cmd/migrate up` + `cmd/seed`), auth `:18081` (`AUTH_STORE_MODE=db`), gateway `:18080` (`WALLET_STORE_MODE=db`), then `E2E_OFFICE_PORT=<free port> npx playwright test -c playwright.config.ts --project=backoffice`.
- This machine's ports are crowded (3000 LOCKIN, 3100 wellnest-sim, 5434 stale docker bind, 5435 attaboy) — always `lsof` a port before assigning it; never kill the occupants, they are the user's other projects.
- Shell cwd drifts between Bash calls after failed `cd`s — start every command from an absolute path.
- The office has a guard test `tests/app-router-legacy-routes.test.ts` asserting specific `(dashboard)` routes STAY redirect-to-dashboard stubs (`/reports`, `/campaigns`, `/risk-management` are intentionally beta-hidden sportsbook-era surfaces). Don't reactivate a guarded route to hang new work on it — use a fresh route (P1-6 exports live at `/exports`, not `/reports`). The husky pre-commit runs office `vitest` (131 tests) so this fails the commit, not just CI.
- A fresh-context verification workflow whose lens agents run out of model credits mid-run returns `confirmed:0 / majors:[] / minors:[]` with the real cause only in the `failures` array — an empty findings set is NOT a pass. Always read `failures` before trusting counts; a credit/API failure means the verification is still OWED, not clean. (Verification #6 attempt 1, Fable-5 credit exhaustion, 2026-07-03.)
- Workflow/Agent lens subagents inherit the session model; if that tier is rate/credit-limited, switch the session model (e.g. to Opus 4.8) and re-dispatch. The cheap mechanical lens (build + targeted tests + protected-core grep) can be run inline by the main loop to de-risk the judgment lenses.
- Never read a piped command's `$?` as the go-test result: `go test ./... | grep ... | head` returns HEAD's exit, not go test's — a red suite reads green. Redirect to a file and check the real exit (`go test -race ./... > log 2>&1; echo $?`). This masked a guard-test failure across two commits (fixed f6c6b5b2). Why it mattered: the guardrail "full suite green before commit" is only real if you read the right exit code.
- The gateway guard test `internal/http/launch_reason_test.go::TestGatewayHTTPHandlersDoNotEchoRawServiceErrors` greps handler files for `httpx.(BadRequest|Conflict|NotFound|Forbidden)(err.Error())` — never pass a raw service error into an httpx.* response; map sentinels to deliberate messages. It runs in the plain `go test` suite, not husky.
- Never `fmt.Sprintf` a SQL string that contains LIKE patterns: the `%` in `LIKE 'deposit%'` collides with format verbs and silently mangles the query. Build placeholder numbers as literals (`$2`,`$3`) instead. And never swallow a window/subquery error with `_ =` — a swallowed error made an AML aggregate rule read sum=0 (fail-open). Log it. (P0-5 slice 2, 2026-07-03.)
- AML rule state (`aml_rules`) is GLOBAL, unlike surveillance which is subject-scoped — live tests that load rules must reset `aml_rules` (and the scanner's `aml_scan_state` watermark + `wallet_ledger`) at start, or rules accumulate across tests and extra alerts fire. Unique subjects alone don't isolate a whole-ledger scanner.
