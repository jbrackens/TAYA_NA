apiVersion: v1
kind: ConfigMap
metadata:
  name: phoenix-platform-config
data:
  ENVIRONMENT: production
  LOG_LEVEL: warn
  OUTBOX_ENABLED: "true"
  OUTBOX_POLL_INTERVAL: 500ms
  OUTBOX_BATCH_SIZE: "250"
