import Pages from "../../../components/pages";
import { PageProps } from "../../../components/pages/types";

function Oauth2Clients(props: PageProps) {
  return <Pages.Oauth2Clients {...props} />;
}

export default Oauth2Clients;
