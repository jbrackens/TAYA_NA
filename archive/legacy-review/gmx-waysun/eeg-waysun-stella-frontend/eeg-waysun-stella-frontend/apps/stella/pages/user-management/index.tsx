import Pages from "../../components/pages";
import { PageProps } from "../../components/pages/types";

function UserManagement(props: PageProps) {
  return <Pages.UserManagement {...props} />;
}

export default UserManagement;
