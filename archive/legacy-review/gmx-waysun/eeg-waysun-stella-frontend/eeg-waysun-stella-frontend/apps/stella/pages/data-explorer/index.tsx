import Pages from "../../components/pages";
import { PageProps } from "../../components/pages/types";

function DataExplorer(props: PageProps) {
  return <Pages.DataExplorer {...props} />;
}

export default DataExplorer;
