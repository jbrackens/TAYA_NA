import Pages from "../../components/pages";
import { PageProps } from "../../components/pages/types";

function Players(props: PageProps) {
  return <Pages.Players {...props} />;
}

export default Players;
