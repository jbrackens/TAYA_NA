import Pages from "../../../components/pages";
import { PageProps } from "../../../components/pages/types";

function RsaKeys(props: PageProps) {
  return <Pages.RsaKeys {...props} />;
}

export default RsaKeys;
