import Pages from "../../components/pages";
import { PageProps } from "../../components/pages/types";

function Authentication(props: PageProps) {
  return <Pages.Authentication {...props} />;
}

export default Authentication;
