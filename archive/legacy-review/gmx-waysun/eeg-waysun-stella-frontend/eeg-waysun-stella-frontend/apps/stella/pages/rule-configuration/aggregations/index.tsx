import Pages from "../../../components/pages";
import { PageProps } from "../../../components/pages/types";

function Aggregation(props: PageProps) {
  return <Pages.Aggregations {...props} />;
}

export default Aggregation;
