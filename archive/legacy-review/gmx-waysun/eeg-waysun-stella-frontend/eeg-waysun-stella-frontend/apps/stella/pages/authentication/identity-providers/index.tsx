import Pages from "../../../components/pages";
import { PageProps } from "../../../components/pages/types";

function IdentityProviders(props: PageProps) {
  return <Pages.IdentityProviders {...props} />;
}

export default IdentityProviders;
