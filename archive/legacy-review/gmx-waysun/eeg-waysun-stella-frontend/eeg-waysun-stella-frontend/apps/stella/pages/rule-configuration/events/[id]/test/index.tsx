import Pages from "../../../../../components/pages";
import { PageProps } from "../../../../../components/pages/types";

function EditEvent(props: PageProps) {
  return <Pages.TestEvents {...props} />;
}

export default EditEvent;
