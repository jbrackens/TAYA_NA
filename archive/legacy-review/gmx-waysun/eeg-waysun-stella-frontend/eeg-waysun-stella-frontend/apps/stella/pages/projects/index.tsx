import Pages from "../../components/pages";
import { PageProps } from "../../components/pages/types";

function Projects(props: PageProps) {
  return <Pages.Projects {...props} />;
}

export default Projects;
