import { Row } from "antd";
import styled from "styled-components";
import { Button } from "ui";

export const StyledDeleteButton = styled(Button)`
  margin-top: 35px;
`;

export const AchievementConditionContainer = styled(Row)`
  margin-bottom: 20px;
`;
