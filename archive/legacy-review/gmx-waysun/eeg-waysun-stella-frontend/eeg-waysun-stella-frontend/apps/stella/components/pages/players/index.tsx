import React from "react";
import { defaultNamespaces } from "../defaults";

const Players = () => {
  return <div>Players</div>;
};

Players.namespacesRequired = [...defaultNamespaces];

export default Players;
