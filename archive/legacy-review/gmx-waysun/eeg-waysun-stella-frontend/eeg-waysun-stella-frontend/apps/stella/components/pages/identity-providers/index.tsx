import React from "react";
import { defaultNamespaces } from "../defaults";

const IdentityProviders = () => {
  return <div>IdentityProviders</div>;
};

IdentityProviders.namespacesRequired = [...defaultNamespaces];

export default IdentityProviders;
