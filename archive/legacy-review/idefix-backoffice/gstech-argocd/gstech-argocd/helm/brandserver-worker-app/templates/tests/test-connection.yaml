apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "brandserver-worker-app.fullname" . }}-test-connection"
  labels:
    {{- include "brandserver-worker-app.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "brandserver-worker-app.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
