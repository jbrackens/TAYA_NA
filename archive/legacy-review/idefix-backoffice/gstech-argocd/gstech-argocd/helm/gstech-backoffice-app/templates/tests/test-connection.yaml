apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "gstech-backoffice-app.fullname" . }}-test-connection"
  labels:
    {{- include "gstech-backoffice-app.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "gstech-backoffice-app.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
