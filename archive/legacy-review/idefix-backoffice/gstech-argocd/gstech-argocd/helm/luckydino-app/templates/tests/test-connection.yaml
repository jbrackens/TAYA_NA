apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "luckydino-app.fullname" . }}-test-connection"
  labels:
    {{- include "luckydino-app.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "luckydino-app.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
