nameOverride: "dev-vie-client-service"
fullnameOverride: "dev-vie-client-service"

replicaCount: 1

autoscaling:
  enabled: true
  minReplicas: 1
  maxReplicas: 5
  targetCPUUtilizationPercentage: 80
  targetMemoryUtilizationPercentage: 80

podAnnotations: {}

service:
  enabled: true
  type: ClusterIP
  port: 3000

resources:
  requests:
    memory: "1.5Gi"
  limits:
    memory: "1.5Gi"

ingress:
  enabled: false
  paths:
    - path: /*
      backend:
        serviceName: dev-vie-client-service
        servicePort: 3000

  name: alb-ingress-dev-vie-client-service
  namespace: dev
  annotations: {}

  hosts: []
  tls: []

env:
  SITE_NAME: vie
  NODE_ENV: production
  PORT: 3000
  API: http://dev-vie-backend-service:3000
  CAPTCHA_KEY: "6LfDVlclAAAAABWgiFIo3AwEG00yjQz9XTWYIZNR"
