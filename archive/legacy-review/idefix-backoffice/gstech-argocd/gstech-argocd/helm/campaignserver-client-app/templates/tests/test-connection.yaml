apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "campaignserver-client-app.fullname" . }}-test-connection"
  labels:
    {{- include "campaignserver-client-app.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "campaignserver-client-app.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
