/*
Stylus variables are information about icon's compiled state, stored under its original file name

.icon-home {
  width: $icon_home_width;
}

The large array-like variables contain all information about a single icon
$icon_home = x y offset_x offset_y width height total_width total_height image_path;

At the bottom of this section, we provide information about the spritesheet itself
$spritesheet = width height image $spritesheet_sprites;
*/
$redridinghood_common_girl_name = 'redridinghood_common_girl';
$redridinghood_common_girl_x = 0px;
$redridinghood_common_girl_y = 0px;
$redridinghood_common_girl_offset_x = 0px;
$redridinghood_common_girl_offset_y = 0px;
$redridinghood_common_girl_width = 205px;
$redridinghood_common_girl_height = 291px;
$redridinghood_common_girl_total_width = 717px;
$redridinghood_common_girl_total_height = 291px;
$redridinghood_common_girl_image = 'redridinghood_common_b.png';
$redridinghood_common_girl = 0px 0px 0px 0px 205px 291px 717px 291px 'redridinghood_common_b.png' 'redridinghood_common_girl';
$redridinghood_common_logo_name = 'redridinghood_common_logo';
$redridinghood_common_logo_x = 209px;
$redridinghood_common_logo_y = 0px;
$redridinghood_common_logo_offset_x = -209px;
$redridinghood_common_logo_offset_y = 0px;
$redridinghood_common_logo_width = 304px;
$redridinghood_common_logo_height = 169px;
$redridinghood_common_logo_total_width = 717px;
$redridinghood_common_logo_total_height = 291px;
$redridinghood_common_logo_image = 'redridinghood_common_b.png';
$redridinghood_common_logo = 209px 0px -209px 0px 304px 169px 717px 291px 'redridinghood_common_b.png' 'redridinghood_common_logo';
$redridinghood_common_wolf_name = 'redridinghood_common_wolf';
$redridinghood_common_wolf_x = 517px;
$redridinghood_common_wolf_y = 0px;
$redridinghood_common_wolf_offset_x = -517px;
$redridinghood_common_wolf_offset_y = 0px;
$redridinghood_common_wolf_width = 200px;
$redridinghood_common_wolf_height = 224px;
$redridinghood_common_wolf_total_width = 717px;
$redridinghood_common_wolf_total_height = 291px;
$redridinghood_common_wolf_image = 'redridinghood_common_b.png';
$redridinghood_common_wolf = 517px 0px -517px 0px 200px 224px 717px 291px 'redridinghood_common_b.png' 'redridinghood_common_wolf';
$spritesheet_width = 717px;
$spritesheet_height = 291px;
$spritesheet_image = 'redridinghood_common_b.png';
$spritesheet_sprites = $redridinghood_common_girl $redridinghood_common_logo $redridinghood_common_wolf;
$spritesheet = 717px 291px 'redridinghood_common_b.png' $spritesheet_sprites;

/*
The provided mixins are intended to be used with the array-like variables

.icon-home {
  spriteWidth($icon_home)
}

.icon-email {
  sprite($icon_email)
}

Example usage in HTML:

`display: block` sprite:
<div class="icon-home"></div>

To change `display` (e.g. `display: inline-block;`), we suggest using a common CSS class:

// CSS
.icon {
  display: inline-block;
}

// HTML
<i class="icon icon-home"></i>
*/
spriteWidth($sprite) {
  width: $sprite[4];
}

spriteHeight($sprite) {
  height: $sprite[5];
}

spritePosition($sprite) {
  background-position: $sprite[2] $sprite[3];
}

spriteImage($sprite) {
  background-image: url($sprite[8]);
}

sprite($sprite) {
  spriteImage($sprite)
  spritePosition($sprite)
  spriteWidth($sprite)
  spriteHeight($sprite)
}

/*
The `sprites` mixin generates identical output to the CSS template
  but can be overridden inside of Stylus

This must be run when you have at least 2 sprites.
  If run with a single sprite, then there will be reference errors.

sprites($spritesheet_sprites);
*/
sprites($sprites) {
  for $sprite in $sprites {
    $sprite_name = $sprite[9];
    .{$sprite_name} {
      sprite($sprite);
    }
  }
}
