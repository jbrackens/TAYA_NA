/*
Stylus variables are information about icon's compiled state, stored under its original file name

.icon-home {
  width: $icon_home_width;
}

The large array-like variables contain all information about a single icon
$icon_home = x y offset_x offset_y width height total_width total_height image_path;

At the bottom of this section, we provide information about the spritesheet itself
$spritesheet = width height image $spritesheet_sprites;
*/
$exitpage2_desktop_bg_name = 'exitpage2_desktop_bg';
$exitpage2_desktop_bg_x = 0px;
$exitpage2_desktop_bg_y = 0px;
$exitpage2_desktop_bg_offset_x = 0px;
$exitpage2_desktop_bg_offset_y = 0px;
$exitpage2_desktop_bg_width = 556px;
$exitpage2_desktop_bg_height = 348px;
$exitpage2_desktop_bg_total_width = 812px;
$exitpage2_desktop_bg_total_height = 467px;
$exitpage2_desktop_bg_image = 'exitpage2_desktop_b.png';
$exitpage2_desktop_bg = 0px 0px 0px 0px 556px 348px 812px 467px 'exitpage2_desktop_b.png' 'exitpage2_desktop_bg';
$exitpage2_desktop_dino_name = 'exitpage2_desktop_dino';
$exitpage2_desktop_dino_x = 560px;
$exitpage2_desktop_dino_y = 0px;
$exitpage2_desktop_dino_offset_x = -560px;
$exitpage2_desktop_dino_offset_y = 0px;
$exitpage2_desktop_dino_width = 252px;
$exitpage2_desktop_dino_height = 318px;
$exitpage2_desktop_dino_total_width = 812px;
$exitpage2_desktop_dino_total_height = 467px;
$exitpage2_desktop_dino_image = 'exitpage2_desktop_b.png';
$exitpage2_desktop_dino = 560px 0px -560px 0px 252px 318px 812px 467px 'exitpage2_desktop_b.png' 'exitpage2_desktop_dino';
$exitpage2_desktop_text_name = 'exitpage2_desktop_text';
$exitpage2_desktop_text_x = 0px;
$exitpage2_desktop_text_y = 352px;
$exitpage2_desktop_text_offset_x = 0px;
$exitpage2_desktop_text_offset_y = -352px;
$exitpage2_desktop_text_width = 349px;
$exitpage2_desktop_text_height = 115px;
$exitpage2_desktop_text_total_width = 812px;
$exitpage2_desktop_text_total_height = 467px;
$exitpage2_desktop_text_image = 'exitpage2_desktop_b.png';
$exitpage2_desktop_text = 0px 352px 0px -352px 349px 115px 812px 467px 'exitpage2_desktop_b.png' 'exitpage2_desktop_text';
$spritesheet_width = 812px;
$spritesheet_height = 467px;
$spritesheet_image = 'exitpage2_desktop_b.png';
$spritesheet_sprites = $exitpage2_desktop_bg $exitpage2_desktop_dino $exitpage2_desktop_text;
$spritesheet = 812px 467px 'exitpage2_desktop_b.png' $spritesheet_sprites;

/*
The provided mixins are intended to be used with the array-like variables

.icon-home {
  spriteWidth($icon_home)
}

.icon-email {
  sprite($icon_email)
}

Example usage in HTML:

`display: block` sprite:
<div class="icon-home"></div>

To change `display` (e.g. `display: inline-block;`), we suggest using a common CSS class:

// CSS
.icon {
  display: inline-block;
}

// HTML
<i class="icon icon-home"></i>
*/
spriteWidth($sprite) {
  width: $sprite[4];
}

spriteHeight($sprite) {
  height: $sprite[5];
}

spritePosition($sprite) {
  background-position: $sprite[2] $sprite[3];
}

spriteImage($sprite) {
  background-image: url($sprite[8]);
}

sprite($sprite) {
  spriteImage($sprite)
  spritePosition($sprite)
  spriteWidth($sprite)
  spriteHeight($sprite)
}

/*
The `sprites` mixin generates identical output to the CSS template
  but can be overridden inside of Stylus

This must be run when you have at least 2 sprites.
  If run with a single sprite, then there will be reference errors.

sprites($spritesheet_sprites);
*/
sprites($sprites) {
  for $sprite in $sprites {
    $sprite_name = $sprite[9];
    .{$sprite_name} {
      sprite($sprite);
    }
  }
}
