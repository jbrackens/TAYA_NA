/*
Stylus variables are information about icon's compiled state, stored under its original file name

.icon-home {
  width: $icon_home_width;
}

The large array-like variables contain all information about a single icon
$icon_home = x y offset_x offset_y width height total_width total_height image_path;

At the bottom of this section, we provide information about the spritesheet itself
$spritesheet = width height image $spritesheet_sprites;
*/
$piggyriches_common_logo_name = 'piggyriches_common_logo';
$piggyriches_common_logo_x = 0px;
$piggyriches_common_logo_y = 286px;
$piggyriches_common_logo_offset_x = 0px;
$piggyriches_common_logo_offset_y = -286px;
$piggyriches_common_logo_width = 321px;
$piggyriches_common_logo_height = 99px;
$piggyriches_common_logo_total_width = 655px;
$piggyriches_common_logo_total_height = 385px;
$piggyriches_common_logo_image = 'piggyriches_common_b.png';
$piggyriches_common_logo = 0px 286px 0px -286px 321px 99px 655px 385px 'piggyriches_common_b.png' 'piggyriches_common_logo';
$piggyriches_common_mrpig_name = 'piggyriches_common_mrpig';
$piggyriches_common_mrpig_x = 0px;
$piggyriches_common_mrpig_y = 0px;
$piggyriches_common_mrpig_offset_x = 0px;
$piggyriches_common_mrpig_offset_y = 0px;
$piggyriches_common_mrpig_width = 319px;
$piggyriches_common_mrpig_height = 282px;
$piggyriches_common_mrpig_total_width = 655px;
$piggyriches_common_mrpig_total_height = 385px;
$piggyriches_common_mrpig_image = 'piggyriches_common_b.png';
$piggyriches_common_mrpig = 0px 0px 0px 0px 319px 282px 655px 385px 'piggyriches_common_b.png' 'piggyriches_common_mrpig';
$piggyriches_common_mspig_name = 'piggyriches_common_mspig';
$piggyriches_common_mspig_x = 323px;
$piggyriches_common_mspig_y = 0px;
$piggyriches_common_mspig_offset_x = -323px;
$piggyriches_common_mspig_offset_y = 0px;
$piggyriches_common_mspig_width = 332px;
$piggyriches_common_mspig_height = 265px;
$piggyriches_common_mspig_total_width = 655px;
$piggyriches_common_mspig_total_height = 385px;
$piggyriches_common_mspig_image = 'piggyriches_common_b.png';
$piggyriches_common_mspig = 323px 0px -323px 0px 332px 265px 655px 385px 'piggyriches_common_b.png' 'piggyriches_common_mspig';
$spritesheet_width = 655px;
$spritesheet_height = 385px;
$spritesheet_image = 'piggyriches_common_b.png';
$spritesheet_sprites = $piggyriches_common_logo $piggyriches_common_mrpig $piggyriches_common_mspig;
$spritesheet = 655px 385px 'piggyriches_common_b.png' $spritesheet_sprites;

/*
The provided mixins are intended to be used with the array-like variables

.icon-home {
  spriteWidth($icon_home)
}

.icon-email {
  sprite($icon_email)
}

Example usage in HTML:

`display: block` sprite:
<div class="icon-home"></div>

To change `display` (e.g. `display: inline-block;`), we suggest using a common CSS class:

// CSS
.icon {
  display: inline-block;
}

// HTML
<i class="icon icon-home"></i>
*/
spriteWidth($sprite) {
  width: $sprite[4];
}

spriteHeight($sprite) {
  height: $sprite[5];
}

spritePosition($sprite) {
  background-position: $sprite[2] $sprite[3];
}

spriteImage($sprite) {
  background-image: url($sprite[8]);
}

sprite($sprite) {
  spriteImage($sprite)
  spritePosition($sprite)
  spriteWidth($sprite)
  spriteHeight($sprite)
}

/*
The `sprites` mixin generates identical output to the CSS template
  but can be overridden inside of Stylus

This must be run when you have at least 2 sprites.
  If run with a single sprite, then there will be reference errors.

sprites($spritesheet_sprites);
*/
sprites($sprites) {
  for $sprite in $sprites {
    $sprite_name = $sprite[9];
    .{$sprite_name} {
      sprite($sprite);
    }
  }
}
