/*
Stylus variables are information about icon's compiled state, stored under its original file name

.icon-home {
  width: $icon_home_width;
}

The large array-like variables contain all information about a single icon
$icon_home = x y offset_x offset_y width height total_width total_height image_path;

At the bottom of this section, we provide information about the spritesheet itself
$spritesheet = width height image $spritesheet_sprites;
*/
$immortalromance_common_dude_name = 'immortalromance_common_dude';
$immortalromance_common_dude_x = 310px;
$immortalromance_common_dude_y = 0px;
$immortalromance_common_dude_offset_x = -310px;
$immortalromance_common_dude_offset_y = 0px;
$immortalromance_common_dude_width = 283px;
$immortalromance_common_dude_height = 291px;
$immortalromance_common_dude_total_width = 593px;
$immortalromance_common_dude_total_height = 495px;
$immortalromance_common_dude_image = 'immortalromance_common_b.png';
$immortalromance_common_dude = 310px 0px -310px 0px 283px 291px 593px 495px 'immortalromance_common_b.png' 'immortalromance_common_dude';
$immortalromance_common_lady_name = 'immortalromance_common_lady';
$immortalromance_common_lady_x = 0px;
$immortalromance_common_lady_y = 0px;
$immortalromance_common_lady_offset_x = 0px;
$immortalromance_common_lady_offset_y = 0px;
$immortalromance_common_lady_width = 306px;
$immortalromance_common_lady_height = 300px;
$immortalromance_common_lady_total_width = 593px;
$immortalromance_common_lady_total_height = 495px;
$immortalromance_common_lady_image = 'immortalromance_common_b.png';
$immortalromance_common_lady = 0px 0px 0px 0px 306px 300px 593px 495px 'immortalromance_common_b.png' 'immortalromance_common_lady';
$immortalromance_common_logo_name = 'immortalromance_common_logo';
$immortalromance_common_logo_x = 0px;
$immortalromance_common_logo_y = 304px;
$immortalromance_common_logo_offset_x = 0px;
$immortalromance_common_logo_offset_y = -304px;
$immortalromance_common_logo_width = 318px;
$immortalromance_common_logo_height = 191px;
$immortalromance_common_logo_total_width = 593px;
$immortalromance_common_logo_total_height = 495px;
$immortalromance_common_logo_image = 'immortalromance_common_b.png';
$immortalromance_common_logo = 0px 304px 0px -304px 318px 191px 593px 495px 'immortalromance_common_b.png' 'immortalromance_common_logo';
$spritesheet_width = 593px;
$spritesheet_height = 495px;
$spritesheet_image = 'immortalromance_common_b.png';
$spritesheet_sprites = $immortalromance_common_dude $immortalromance_common_lady $immortalromance_common_logo;
$spritesheet = 593px 495px 'immortalromance_common_b.png' $spritesheet_sprites;

/*
The provided mixins are intended to be used with the array-like variables

.icon-home {
  spriteWidth($icon_home)
}

.icon-email {
  sprite($icon_email)
}

Example usage in HTML:

`display: block` sprite:
<div class="icon-home"></div>

To change `display` (e.g. `display: inline-block;`), we suggest using a common CSS class:

// CSS
.icon {
  display: inline-block;
}

// HTML
<i class="icon icon-home"></i>
*/
spriteWidth($sprite) {
  width: $sprite[4];
}

spriteHeight($sprite) {
  height: $sprite[5];
}

spritePosition($sprite) {
  background-position: $sprite[2] $sprite[3];
}

spriteImage($sprite) {
  background-image: url($sprite[8]);
}

sprite($sprite) {
  spriteImage($sprite)
  spritePosition($sprite)
  spriteWidth($sprite)
  spriteHeight($sprite)
}

/*
The `sprites` mixin generates identical output to the CSS template
  but can be overridden inside of Stylus

This must be run when you have at least 2 sprites.
  If run with a single sprite, then there will be reference errors.

sprites($spritesheet_sprites);
*/
sprites($sprites) {
  for $sprite in $sprites {
    $sprite_name = $sprite[9];
    .{$sprite_name} {
      sprite($sprite);
    }
  }
}
