/*
Stylus variables are information about icon's compiled state, stored under its original file name

.icon-home {
  width: $icon_home_width;
}

The large array-like variables contain all information about a single icon
$icon_home = x y offset_x offset_y width height total_width total_height image_path;

At the bottom of this section, we provide information about the spritesheet itself
$spritesheet = width height image $spritesheet_sprites;
*/
$exitpage2_mobile_bg_name = 'exitpage2_mobile_bg';
$exitpage2_mobile_bg_x = 0px;
$exitpage2_mobile_bg_y = 0px;
$exitpage2_mobile_bg_offset_x = 0px;
$exitpage2_mobile_bg_offset_y = 0px;
$exitpage2_mobile_bg_width = 556px;
$exitpage2_mobile_bg_height = 348px;
$exitpage2_mobile_bg_total_width = 556px;
$exitpage2_mobile_bg_total_height = 454px;
$exitpage2_mobile_bg_image = 'exitpage2_mobile_b.png';
$exitpage2_mobile_bg = 0px 0px 0px 0px 556px 348px 556px 454px 'exitpage2_mobile_b.png' 'exitpage2_mobile_bg';
$exitpage2_mobile_text_name = 'exitpage2_mobile_text';
$exitpage2_mobile_text_x = 0px;
$exitpage2_mobile_text_y = 352px;
$exitpage2_mobile_text_offset_x = 0px;
$exitpage2_mobile_text_offset_y = -352px;
$exitpage2_mobile_text_width = 310px;
$exitpage2_mobile_text_height = 102px;
$exitpage2_mobile_text_total_width = 556px;
$exitpage2_mobile_text_total_height = 454px;
$exitpage2_mobile_text_image = 'exitpage2_mobile_b.png';
$exitpage2_mobile_text = 0px 352px 0px -352px 310px 102px 556px 454px 'exitpage2_mobile_b.png' 'exitpage2_mobile_text';
$spritesheet_width = 556px;
$spritesheet_height = 454px;
$spritesheet_image = 'exitpage2_mobile_b.png';
$spritesheet_sprites = $exitpage2_mobile_bg $exitpage2_mobile_text;
$spritesheet = 556px 454px 'exitpage2_mobile_b.png' $spritesheet_sprites;

/*
The provided mixins are intended to be used with the array-like variables

.icon-home {
  spriteWidth($icon_home)
}

.icon-email {
  sprite($icon_email)
}

Example usage in HTML:

`display: block` sprite:
<div class="icon-home"></div>

To change `display` (e.g. `display: inline-block;`), we suggest using a common CSS class:

// CSS
.icon {
  display: inline-block;
}

// HTML
<i class="icon icon-home"></i>
*/
spriteWidth($sprite) {
  width: $sprite[4];
}

spriteHeight($sprite) {
  height: $sprite[5];
}

spritePosition($sprite) {
  background-position: $sprite[2] $sprite[3];
}

spriteImage($sprite) {
  background-image: url($sprite[8]);
}

sprite($sprite) {
  spriteImage($sprite)
  spritePosition($sprite)
  spriteWidth($sprite)
  spriteHeight($sprite)
}

/*
The `sprites` mixin generates identical output to the CSS template
  but can be overridden inside of Stylus

This must be run when you have at least 2 sprites.
  If run with a single sprite, then there will be reference errors.

sprites($spritesheet_sprites);
*/
sprites($sprites) {
  for $sprite in $sprites {
    $sprite_name = $sprite[9];
    .{$sprite_name} {
      sprite($sprite);
    }
  }
}
