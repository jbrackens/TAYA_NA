apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "gmx-user-service.fullname" . }}-test-connection"
  labels:
    {{- include "gmx-user-service.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "gmx-user-service.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
