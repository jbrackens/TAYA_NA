akka {
  loggers = [akka.testkit.TestEventListener]
  loglevel = "DEBUG"
  //  log-config-on-start = on
  actor {
    debug {
      receive = on
      autoreceive = on
      lifecycle = on
      unhandled = on
    }
  }
}