include "application-base"

akka {
  loglevel = INFO
  loglevel = ${?WALLET_AKKA_LOG_LEVEL}

  remote.artery {
    canonical.hostname = 127.0.0.1
    canonical.port = 25520
  }

  cluster {
    seed-nodes = [
      "akka://Wallet@127.0.0.1:25520"
    ]
  }
}
