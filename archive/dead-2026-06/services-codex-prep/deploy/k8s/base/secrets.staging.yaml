apiVersion: v1
kind: Secret
metadata:
  name: phoenix-platform-secrets
  namespace: phoenix-platform-staging
type: Opaque
stringData:
  DATABASE_URL: "postgresql://phoenix:REPLACE_ME@STAGING_PG_HOST:5432/phoenix?sslmode=require"
  JWT_SECRET: "REPLACE_ME"
  JWT_SECRET_KEY: "REPLACE_ME"
  REDIS_PASSWORD: "REPLACE_ME"
  SMTP_HOST: "REPLACE_ME"
  SMTP_PORT: "587"
  SMTP_USER: "REPLACE_ME"
  SMTP_PASSWORD: "REPLACE_ME"
  EMAIL_FROM: "noreply@phoenix-platform.example"
