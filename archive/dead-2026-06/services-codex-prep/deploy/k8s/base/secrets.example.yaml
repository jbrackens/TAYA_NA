apiVersion: v1
kind: Secret
metadata:
  name: phoenix-platform-secrets
type: Opaque
stringData:
  DATABASE_URL: postgresql://phoenix:replace-me@postgres-rw:5432/phoenix?sslmode=require
  JWT_SECRET: replace-me
  JWT_SECRET_KEY: replace-me
  REDIS_PASSWORD: replace-me
  SMTP_HOST: smtp.example.com
  SMTP_PORT: "587"
  SMTP_USER: replace-me
  SMTP_PASSWORD: replace-me
  EMAIL_FROM: noreply@phoenix-platform.example
